<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>内存管理</title>
    <url>/2021/12/05/%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h2 id="内存使用与分段"><a href="#内存使用与分段" class="headerlink" title="内存使用与分段"></a>内存使用与分段</h2><h3 id="从计算机如何工作开始讲起"><a href="#从计算机如何工作开始讲起" class="headerlink" title="从计算机如何工作开始讲起"></a>从计算机如何工作开始讲起</h3><p><img src="95.png"></p>
<ul>
<li>内存使用：将程序放入内存中，PC指向开始地址</li>
</ul>
<h3 id="首先让程序进入内存"><a href="#首先让程序进入内存" class="headerlink" title="首先让程序进入内存"></a>首先让程序进入内存</h3><p><img src="96.png"></p>
<p>​    用磁盘将对应的机械代码读入内存，call 40时需要在地址总线上发送40，然后main函数的入口就放在40地址处。而物理内存40正好是_entry到_main的偏移地址，所以_entry就在地址0处。</p>
<p><img src="97.png"></p>
<p>但如果有0地址处被占用了，或多个程序在内存中，就不能都放在0地址处。</p>
<p><img src="98.png"></p>
<p>但一旦IP修改到一个空闲的地址，那么call 40就不再是_main的地址。</p>
<h3 id="重定位：修改程序中的地址-相对地址-逻辑地址"><a href="#重定位：修改程序中的地址-相对地址-逻辑地址" class="headerlink" title="重定位：修改程序中的地址(相对地址/逻辑地址)"></a>重定位：修改程序中的地址(相对地址/逻辑地址)</h3><p><img src="99.png"></p>
<p>什么时候完成重定位？</p>
<ol>
<li>编译时（编译时不知道执行时哪些地址是空闲的，在实际系统中何难实现。但例如嵌入式系统烧录后不再改变，就可以实现）</li>
<li>载入时（在载入内存时重定位，执行前分配空闲的地址）</li>
<li>运行时（支持进程换入和换出）</li>
</ol>
<h3 id="程序载入后还需要移动"><a href="#程序载入后还需要移动" class="headerlink" title="程序载入后还需要移动"></a>程序载入后还需要移动</h3><ul>
<li>一个重要概念：交换（swap）<ul>
<li><img src="100.png"></li>
<li>如果一个进程阻塞了，那么需要将睡眠的进程换出，将就绪的进程换入。</li>
<li>一旦进程在载入后移动，地址就又发生了该百年。</li>
</ul>
</li>
</ul>
<h3 id="重定位最合适的时机-运行时重定位"><a href="#重定位最合适的时机-运行时重定位" class="headerlink" title="重定位最合适的时机-运行时重定位"></a>重定位最合适的时机-运行时重定位</h3><ul>
<li>在运行每条指令时才完成重定位<ul>
<li><img src="101.png"></li>
<li>base放在哪里？怎么找到？<ul>
<li>在PCB中，一旦进程执行，执行指令翻译时，就从PCB中找基地址</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="引入分段：是将整个程序一起载入内存中吗？"><a href="#引入分段：是将整个程序一起载入内存中吗？" class="headerlink" title="引入分段：是将整个程序一起载入内存中吗？"></a>引入分段：是将整个程序一起载入内存中吗？</h3><ul>
<li>程序员眼中的程序<ul>
<li>由若干部分（段）组成，每个段有各自的特点、用途：代码段只读，代码段/数据段不会动态增长</li>
<li><img src="102.png"></li>
</ul>
</li>
<li>符合用户观点：用户可独立考虑每个分段（分治）</li>
<li>怎么具体定位具体指令（数据）：&lt;段号，段内偏移&gt;（&lt;cs:ip&gt;）</li>
</ul>
<h3 id="不是将整个程序，是将各个段分别放入内存"><a href="#不是将整个程序，是将各个段分别放入内存" class="headerlink" title="不是将整个程序，是将各个段分别放入内存"></a>不是将整个程序，是将各个段分别放入内存</h3><p>由高地址到低地址分别是：</p>
<ol>
<li><p>栈：是一种用来存储函数调用时的临时信息的结构，如函数调用所传递的参数、函数的返回地址、函数的局部变量等。 在程序运行时由编译器在需要的时候分配，在不需要的时候自动清除。</p>
</li>
<li><p>堆：用来存储程序运行时分配的变量。</p>
<p>堆的大小并不固定，可动态扩张或缩减。其分配由malloc()、new()等这类实时内存分配函数来实现。当进程调用malloc等函数分配内存时，新分配的内存就被动态添加到堆上（堆被扩张）；当利用free()等函数释放内存时，被释放的内存从堆中被剔除（堆被缩减）  堆的内存释放由应用程序去控制，通常一个new()就要对应一个delete()，如果程序员没有释放掉，那么在程序结束后操作系统会自动回收。</p>
</li>
<li><p>BSS段：用来保存全局的和静态的未初始化变量</p>
</li>
<li><p>data段：用来存放保存全局的和静态的已初始化变量</p>
</li>
<li><p>text段：存放着程序的机器码和只读数据，可执行指令就是从这里取得的。</p>
</li>
</ol>
<p>需要一张表，来记录每个段的<strong>基址、长度、读写权限</strong>等，即<strong>进程段表</strong>（LDT）</p>
<p>而操作系统的那张段表就是（GDT）</p>
<h3 id="真正的故事：GDT-LDT"><a href="#真正的故事：GDT-LDT" class="headerlink" title="真正的故事：GDT+LDT"></a>真正的故事：GDT+LDT</h3><p>GDT表项：</p>
<p><img src="103.png"></p>
<p>通过GDT表分开各个进程的基址，通过LDT表来分开进程中各个段的基址</p>
<p><img src="104.png"></p>
<h2 id="内存分区与分页"><a href="#内存分区与分页" class="headerlink" title="内存分区与分页"></a>内存分区与分页</h2>]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
  </entry>
  <entry>
    <title>Fallout: Leaking Data on Meltdown-resistant CPUs论文笔记</title>
    <url>/2021/12/09/Fallout-Leaking-Data-on-Meltdown-resistant-CPUs/</url>
    <content><![CDATA[<h1 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h1><h2 id="缓存"><a href="#缓存" class="headerlink" title="缓存"></a>缓存</h2><p>​        为了提高从内存里读取数据的速度，我们设立了多级缓存。缓存速度比内存要快很多，可以用来存储一些经常用到的数据，这样可以调高数据的利用效率。</p>
<p>​        CPU会认为当前访问的某个地址的数据时，它周围的数据随后也会大概率被访问到，所以周围数据会一并放到缓存中，以便于取用。</p>
<h2 id="乱序执行"><a href="#乱序执行" class="headerlink" title="乱序执行"></a>乱序执行</h2><p>​        某些指令的准备工作，和一些不依赖前面指令运行结果的指令，都可以提前运行，然后把结果放入缓存。如果轮到该指令运行，就直接将缓存内数据写入内存。</p>
<h2 id="分支预测"><a href="#分支预测" class="headerlink" title="分支预测"></a>分支预测</h2><p>​        遇到分支跳转时，按照之前的经验，如果某个分支经常被执行，那后续再去这个分支的概率一定很大，那这样咱们预测后面会去到这个分支，就提前把这个分支后面指令能做的工作先做了。</p>
<h2 id="幽灵与熔断"><a href="#幽灵与熔断" class="headerlink" title="幽灵与熔断"></a>幽灵与熔断</h2><p>​        如果分支预测被恶意利用，收到恶意程序欺骗，那么就会在关键时候预测失败，从而执行一些本不该执行的指令。虽然只将数据存储在缓存中，但通过访问内存时间的不同（放入缓存的数据访问时间会明显较短），就可以知道哪一块地址被缓存了。    </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (x &lt; array1_size)</span><br><span class="line">&#123;</span><br><span class="line">   temp &amp;= array2[array1[x] * <span class="number">512</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一开始的x都小于array1_size，欺骗CPU不断提前进行分支预测。但当真正的恶意x（某个地址单元）（大于array1_size）到来时，CPU还是把if里面的执行结果放入了缓存。只是在指令真正开始执行时，虽然缓存不被输出，但访问的时间会明显较短，数一下缓存的内存到数组开头的偏移距离，可以推出这个地址单元的值。</p>
<h2 id="缓解机制"><a href="#缓解机制" class="headerlink" title="缓解机制"></a>缓解机制</h2><p>Meltdown攻击最有效的方式就是KAISER/KPTI。KAISER/KPTI方案中要求操作系统维护两个页表，一个页表给用户程序使用，一个给kernel自己使用。并且确保程序所使用的页表不会映射高优先级的页面，即不会映射kernel的页面。</p>
<p>两个页表的切换，会导致CR3（控制寄存器：用于控制和确定处理器的操作模式以及当前执行任务的特性）的重新加载，从而引起TLB刷新，进而降低内存的访问速度。如果某些应用场景需要大量的内核和用户空间切换（两个页表之间的切换），会造成较高的性能开销。</p>
<h1 id="最新的漏洞"><a href="#最新的漏洞" class="headerlink" title="最新的漏洞"></a>最新的漏洞</h1><p>​        硬件修复后，上述缓解机制被禁用，该文章说明了最新的抗熔断处理器仍是具有数据泄漏的潜在风险，仍易受到熔断攻击。</p>
<p>​        待更新</p>
]]></content>
      <tags>
        <tag>幽灵与熔断</tag>
      </tags>
  </entry>
  <entry>
    <title>数据链路层</title>
    <url>/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/</url>
    <content><![CDATA[<h2 id="第三章"><a href="#第三章" class="headerlink" title="第三章"></a>第三章</h2><h3 id="3-1-数据链路层的基本概念"><a href="#3-1-数据链路层的基本概念" class="headerlink" title="3.1 数据链路层的基本概念"></a>3.1 数据链路层的基本概念</h3><p>结点：主机、路由器</p>
<p>链路：网络中两个结点之间的<strong>物理通道</strong>，链路的传输介质主要有双绞线、光纤和微波。分为有线链路和无线链路。</p>
<p>数据链路：网络中两个结点之间的<strong>逻辑通道</strong>，把实现控制数据传输协议的硬件和软件加到链路上就构成数据链路。</p>
<p><strong>数据链路层</strong>负责通过一条链路从一个结点向另一个物理链路直接相连的相邻结点传送数据报。</p>
<p><strong>数据链路层功能概述</strong></p>
<p>数据链路层在物理层提供服务的基础上<strong>向网络层提供服务</strong>，其最基本的服务是将源自网络层来的数据<strong>可靠地传输到</strong>相邻结点的目标机网络层。其主要作用是<strong>加强物理层传输原始比特流的功能</strong>，将物理层提供的可能出错的物理连接改造为逻辑上无差错的数据链路，使之对网络层表现为一条无差错的链路。</p>
<p>功能一：为网络层提供服务。</p>
<ul>
<li>无确认无连接服务</li>
<li>有确认无连接服务</li>
<li>有确认面向连接服务</li>
</ul>
<p>功能二：链路管理，即连接的建立、维持、释放</p>
<p>功能三：组帧</p>
<p>功能四：流量控制</p>
<ul>
<li>限制发送方</li>
</ul>
<p>功能五：差错控制（帧错/位错）</p>
<h3 id="3-2-封装成帧-amp-透明传输"><a href="#3-2-封装成帧-amp-透明传输" class="headerlink" title="3.2 封装成帧&amp;透明传输"></a>3.2 封装成帧&amp;透明传输</h3><p><strong>封装成帧</strong>就是在一段数据的前后部分添加首部和尾部，这样就构成了一个帧。接收端在受到物理层上交的比特流后，就能根据首部和尾部的标记，从收到的比特流中识别帧的开始和结束。</p>
<p>首部和尾部包含许多控制信息，他们的一个重要作用：<strong>帧定界</strong>（确定帧的界限）</p>
<p>帧同步：接收方应当能从接收到的二进制比特流中区分出帧的起始和终止。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/43.png" class="">

<p>组帧的四种方法：</p>
<ul>
<li>字符计数法</li>
<li>字符填充法</li>
<li>零比特填充法</li>
<li>违规编码法</li>
</ul>
<h4 id="透明传输"><a href="#透明传输" class="headerlink" title="透明传输"></a>透明传输</h4><p>透明传输是值不管所传数据是什么样的比特组合，都应当在链路上传送。因此，链路层就”看不见“有什么妨碍数据传输的东西。</p>
<p>当所传数据中的比特组合恰巧与某一个控制信息一样时，就必须采取适当的措施，使接收方不会将这样的数据误认为某种控制信息。这样才能保证数据链路层的传输是透明的</p>
<h4 id="字符计数法"><a href="#字符计数法" class="headerlink" title="字符计数法"></a>字符计数法</h4><p>帧首部使用一个计数字段（第一个<strong>字节</strong>，八位）来表明帧内字符数。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/44.png" class="">

<p>缺点：一旦技术字段错了，后面就全错。</p>
<h4 id="字符填充法"><a href="#字符填充法" class="headerlink" title="字符填充法"></a>字符填充法</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/45.png" class="">

<ol>
<li>当传送的帧是由文本文件组成时（文本文件的字符都是从键盘上输入的，都是ascii码）。不管从键盘上输入什么字符都可以放在帧里传过去，即<strong>透明传输</strong>。</li>
<li>当传送的帧是由ascii码的文本文件组成时（二进制代码的程序或图像等）。<strong>就要采用字符填充方法实现透明传输</strong>。</li>
</ol>
<p>当数据中含有很多控制信息时，在控制信息前加入一个字节填充。   </p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/46.png" class="">

<p>接收时，将填充字符删去，</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/47.png" class="">

<h4 id="零比特填充法"><a href="#零比特填充法" class="headerlink" title="零比特填充法"></a>零比特填充法</h4><p>操作：</p>
<ol>
<li>在发送端，扫描整个信息字段，只要连续5个1，就立即填入1个0</li>
<li>在接收端收到一个帧时，先找到标志字段确定边界，再用硬件对比特流进行扫描。发现连续5个1时，就把后面的0删掉。</li>
</ol>
<p>保证了透明传输：在传送的比特流中可以传送任意比特组合，而不会对帧边界的判断错误。</p>
<h4 id="违规编码法"><a href="#违规编码法" class="headerlink" title="违规编码法"></a>违规编码法</h4><p>比如：</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/48.png" class="">

<p>可以用<strong>”高-高</strong>“，”<strong>低-低</strong>“来定界帧的起始和终止。</p>
<p>目前较为普遍运用哦的帧同步法是<strong>比特填充法</strong>和<strong>违规编码法</strong></p>
<h3 id="3-3-1-差错控制（检错编码）"><a href="#3-3-1-差错控制（检错编码）" class="headerlink" title="3.3.1 差错控制（检错编码）"></a>3.3.1 差错控制（检错编码）</h3><p>概况来说，传输中的差错都是由于噪声引起的。</p>
<ol>
<li><p>全局性，由于线路本身电气特性所产生的随机噪声（热噪声），是信道固有的，随机存在的。</p>
<p>解决方法：提高信噪比来减少或避免干扰（对传感器下手）</p>
</li>
<li><p>外界特定的短暂原因所造成的冲击噪声，是产生差错的主要原因。</p>
<p>解决办法：通常利用编码技术来解决。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/54.png" class=""></li>
</ol>
<p>无确认无连接服务：通信质量<strong>好</strong>的<strong>有线</strong>传输链路</p>
<p>有确认无连接服务：通信质量<strong>差</strong>的<strong>无线</strong>传输链路</p>
<p>有确认面向连接服务：通信质量<strong>差</strong>的<strong>无线</strong>传输链路</p>
<h4 id="数据链路层的差错控制"><a href="#数据链路层的差错控制" class="headerlink" title="数据链路层的差错控制"></a>数据链路层的差错控制</h4><p>比特错：</p>
<ol>
<li>检错编码<ul>
<li>奇偶校验码</li>
<li>循环冗余码CRC</li>
</ul>
</li>
<li>纠错编码<ul>
<li>海明码</li>
</ul>
</li>
</ol>
<p>数据链路层编码和物理层的数据编码与调制<strong>不同</strong>。物理层编码针对的是<strong>单个比特</strong>，解决传输过程中比特的同步问题，如曼彻斯特编码。而数据链路层的编码针对的是<strong>一组比特</strong>，它通过冗余码的技术实现一组二进制比特串在传输过程中是否出现了差错。</p>
<p>在数据发送之前，先按某种关系<strong>附加</strong>上一定的<strong>冗余位</strong>，构成一个符合某一规则的码字后再发送。当要发送的有效数据变化时，相应的冗余位也随之变化，使码字遵从不变的规则。接收端根据收到码字是否仍符合原规则，从而判断是否出错。</p>
<h4 id="检错编码–奇偶校验码"><a href="#检错编码–奇偶校验码" class="headerlink" title="检错编码–奇偶校验码"></a>检错编码–奇偶校验码</h4><p>奇偶校验码：n-1位信息元和1位校验元</p>
<ul>
<li>奇校验码</li>
<li>偶校验码</li>
</ul>
<p>只能检查出奇数个比特错误，检错能力为50%</p>
<h4 id="检错编码–CRC循环冗余码"><a href="#检错编码–CRC循环冗余码" class="headerlink" title="检错编码–CRC循环冗余码"></a>检错编码–CRC循环冗余码</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/55.png" class="">

<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/56.png" class="">

<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/57.png" class="">

<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/58.png" class="">

<p><strong>接收端检错过程</strong></p>
<p>把接收端收到的每一个帧都除以同样的除数，然后检查得到的余数R。</p>
<ol>
<li>余数为0，判定这个帧没有出错，接受。</li>
<li>余数不为0，判定这个帧有差错（无法确定到位），丢弃。</li>
</ol>
<h3 id="3-3-2-差错控制（纠错编码）"><a href="#3-3-2-差错控制（纠错编码）" class="headerlink" title="3.3.2 差错控制（纠错编码）"></a>3.3.2 差错控制（纠错编码）</h3><h4 id="纠错编码–海明码"><a href="#纠错编码–海明码" class="headerlink" title="纠错编码–海明码"></a>纠错编码–海明码</h4><ul>
<li>发现双比特错，纠正单比特错</li>
</ul>
<p>工作流程：</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/59.png" class="">

<p><strong>海明不等式：</strong></p>
<p>2^r &gt;= k+r+1</p>
<p>r是冗余信息位，k是信息位 </p>
<p><strong>对于D=101101</strong></p>
<ol>
<li><p>根据海明不等式可得，r=4。</p>
</li>
<li><p>假设这4位校验码分别为P1,P2,P3,P4；<strong>Pi放在2的i-1次方位置</strong></p>
<p>数据从左到右为D1,D2,D3,D4,D5,D6。<strong>然后依次将空余位置填满</strong></p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/60.png" class=""></li>
<li><p>求出校验码的值</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/61.png" class="">

<p>Pi是用来校验二进制第i位为1的数据，Pi与所有被校验数据的异或后得0，从而求出Pi的值。</p>
<p>得到0010011101</p>
</li>
<li><p>检错并纠错</p>
<p>假设第五位出错，因此收到的数据为0010111101，令所有要校验的位异或运算。</p>
<p>二进制序列算出来为0101，恰好对应5</p>
</li>
</ol>
<h3 id="3-4-1-流量控制与可靠传输机制"><a href="#3-4-1-流量控制与可靠传输机制" class="headerlink" title="3.4.1 流量控制与可靠传输机制"></a>3.4.1 流量控制与可靠传输机制</h3><h4 id="数据链路层的流量控制"><a href="#数据链路层的流量控制" class="headerlink" title="数据链路层的流量控制"></a>数据链路层的流量控制</h4><p><strong>较高的发送速度和较低的接受能力不匹配</strong>，会造成传输出错，因此流量控制也是数据链路层的一项重要工作。</p>
<p>数据链路层的流量控制是点对点的，而传输层的流量控制是端到端的。</p>
<p>数据链路层流量控制手段：<strong>接收方收不下就不回复确认</strong></p>
<p>传输层流量控制手段：<strong>接收端给发送端一个窗口公告</strong></p>
<h4 id="流量控制方法"><a href="#流量控制方法" class="headerlink" title="流量控制方法"></a>流量控制方法</h4><ul>
<li>停止-等待协议<ul>
<li>每发送完一个帧就停止发送，等待对方的确认，在收到确认后再发送下一个帧。</li>
<li><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/62.png" class=""></li>
</ul>
</li>
<li>滑动窗口协议<ul>
<li><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/63.png" class=""></li>
<li>后退N帧协议（GBN）</li>
<li>选择重传协议（SR）</li>
</ul>
</li>
</ul>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/64.png" class="">

<h4 id="可靠传输、滑动窗口、流量控制"><a href="#可靠传输、滑动窗口、流量控制" class="headerlink" title="可靠传输、滑动窗口、流量控制"></a>可靠传输、滑动窗口、流量控制</h4><p>可靠传输：发送端发什么，接收端接收什么</p>
<p>流量控制：控制发送速率，使接收方有足够的缓冲空间来接收每一个帧</p>
<p>滑动窗口解决：</p>
<ol>
<li>流量控制（收不下就不确认，发送方就不会发送）</li>
<li>可靠传输（发送方自动重传）</li>
</ol>
<h3 id="3-4-2-停止-等待协议"><a href="#3-4-2-停止-等待协议" class="headerlink" title="3.4.2 停止-等待协议"></a>3.4.2 停止-等待协议</h3><p>为什么要有停止-等待协议？</p>
<p>除了比特出差错，底层信道还会出现丢包问题。</p>
<ul>
<li>丢包：物理线路故障、设备故障、病毒攻击、路由信息错误等原因，会导致数据包的丢失。</li>
</ul>
<h4 id="停等协议–无差错情况"><a href="#停等协议–无差错情况" class="headerlink" title="停等协议–无差错情况"></a>停等协议–无差错情况</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/65.png" class="">

<h4 id="停等协议–有差错情况"><a href="#停等协议–有差错情况" class="headerlink" title="停等协议–有差错情况"></a>停等协议–有差错情况</h4><ol>
<li><p>数据帧丢失或检测到帧出错</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/66.png" class="">

<ul>
<li>发完一个帧后，必须保留它的副本</li>
<li>数据帧和确认帧必须编号</li>
</ul>
</li>
<li><p>ACK丢失</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/67.png" class=""></li>
<li><p>ACK迟到</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/68.png" class=""></li>
</ol>
<h4 id="停等协议性能分析"><a href="#停等协议性能分析" class="headerlink" title="停等协议性能分析"></a>停等协议性能分析</h4><p>优点：简单</p>
<p>缺点：</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/69.png" class="">

<h5 id="信道利用率："><a href="#信道利用率：" class="headerlink" title="信道利用率："></a>信道利用率：</h5><p>发送方在一个发送周期内，有效地发送数据所需要的时间占整个发送周期的比率=<strong>（L/C）/T</strong></p>
<p><strong>T发送周期</strong>，从发送数据开始，到收到第一个确认帧为止</p>
<p>此周期内发送<strong>L比特数据</strong></p>
<p><strong>C发送方数据传输率</strong></p>
<ul>
<li>信道吞吐量=信道利用率*发送方的发送速率</li>
</ul>
<h3 id="3-4-3-选择重传协议"><a href="#3-4-3-选择重传协议" class="headerlink" title="3.4.3 选择重传协议"></a>3.4.3 选择重传协议</h3><h4 id="GBN协议的弊端"><a href="#GBN协议的弊端" class="headerlink" title="GBN协议的弊端"></a>GBN协议的弊端</h4><p>累计确认-&gt;批量重传</p>
<p><strong>可不可以只重传出错的帧？</strong></p>
<p>解决方法：设置单个确认，同时加大接受窗口，设置接受缓存，缓存乱序到达的帧。</p>
<h4 id="选择重传协议中的滑动窗口"><a href="#选择重传协议中的滑动窗口" class="headerlink" title="选择重传协议中的滑动窗口"></a>选择重传协议中的滑动窗口</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/70.png" class="">

<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/71.png" class="">

<h4 id="SR发送方必须响应的三件事"><a href="#SR发送方必须响应的三件事" class="headerlink" title="SR发送方必须响应的三件事"></a>SR发送方必须响应的三件事</h4><ol>
<li><p>上层的调用</p>
<p>从上层收到数据后，SR发送方检测下一个可用于该帧的序号，如果序号位于发送窗口内，则发送数据帧；否则就像GBN一样，要么将数据<strong>缓存</strong>，要么<strong>返回给上层</strong>之后再传输。</p>
</li>
<li><p>收到了一个ACK</p>
<p>如果收到了ACK，加入该帧序号在窗口内，则SR发送方将那个被确认的帧标记为已接收。如果该帧序号是窗口的下界（最左边第一个窗口对应的序号），则窗口向前移动到具有最小序号的未确认帧处。如果窗口移动了并且有序号在窗口内的未发送帧，则发送这些帧。</p>
</li>
<li><p>超时事件</p>
<p>每个帧都有自己的定时器，一个超时事件发生后<strong>只重传一个帧</strong></p>
</li>
</ol>
<h4 id="SR接收方要做的事"><a href="#SR接收方要做的事" class="headerlink" title="SR接收方要做的事"></a>SR接收方要做的事</h4><ul>
<li>来着不拒<ul>
<li>SR接收方将<strong>确认一个正确接收的帧</strong>而<strong>不管其是否按序</strong>。失序的帧将被缓存，并返回给发送方一个该帧的确认帧<strong>【收谁确认谁】</strong>，直到所有帧（即序号更小的帧）皆被收到为止，这时才可以将一批帧按序交付给上层，然后<strong>向前移动滑动窗口</strong>。</li>
<li>如果收到了窗口外（小于窗口下界）的帧，就返回ACK</li>
<li>其他情况，忽略</li>
</ul>
</li>
</ul>
<h4 id="运行中的SR"><a href="#运行中的SR" class="headerlink" title="运行中的SR"></a>运行中的SR</h4><p>假设发送窗口和接收窗口尺寸都为4。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/72.png" class="">

<h4 id="滑动窗口长度"><a href="#滑动窗口长度" class="headerlink" title="滑动窗口长度"></a>滑动窗口长度</h4><p>发送窗口最好等于接收窗口。（大了会溢出，小了没意义）</p>
<p>WTmax=WRmax=2^(n-1)</p>
<p>序号是用n个比特表示，滑动窗口是序号数的一半。</p>
<h3 id="3-4-4-后退n帧协议（GBN）"><a href="#3-4-4-后退n帧协议（GBN）" class="headerlink" title="3.4.4 后退n帧协议（GBN）"></a>3.4.4 后退n帧协议（GBN）</h3><h4 id="后退n帧协议的滑动窗口"><a href="#后退n帧协议的滑动窗口" class="headerlink" title="后退n帧协议的滑动窗口"></a>后退n帧协议的滑动窗口</h4><p>发送窗口：发送方维持一组连续的允许发送的帧的序号。如果发送窗口最左侧的帧收到了ACK，则发送窗口向前移动1帧。</p>
<p>接收窗口：接收方维持一组允许接收帧的序号。（后退n帧协议的接收窗口长度是1），当接收窗口的帧接收到后，返回一个ACK，然后接收窗口向前移动1帧</p>
<h4 id="GBN发送方必须响应的三件事"><a href="#GBN发送方必须响应的三件事" class="headerlink" title="GBN发送方必须响应的三件事"></a>GBN发送方必须响应的三件事</h4><ol>
<li><p>上层的调用</p>
<p>上层要发送数据时，发送方先检查发送窗口是否已满，如果未满，则产生一个帧并将其发送；如果窗口已满，发送方只需将数据返回给上层，暗示上层窗口已满，上一层等一会再发送。（实际上，发送方可以缓存这些数据，窗口不满时再发送帧）。</p>
</li>
<li><p>收到一个ACK</p>
<p>GBN协议中，对n号帧的确认采用<strong>累计确认</strong>的方式，标明接收方已经收到n号帧和它之前的全部帧。</p>
</li>
<li><p>超时事件</p>
<p>出现丢失和时延过长帧时发送方重传所有已发送但没有确认的帧。</p>
</li>
</ol>
<h4 id="GBN接收方要做的事"><a href="#GBN接收方要做的事" class="headerlink" title="GBN接收方要做的事"></a>GBN接收方要做的事</h4><p>正常情况：</p>
<p>如果正确收到了n号帧，并且按序，那么接收方为n帧发送一个ACK，并将该帧中的数据部分交付给上层。</p>
<p>其余情况：</p>
<p>其余情况都丢弃帧，并为最近按序接收的帧重新发送ACK。接收方无需缓存任何失序帧，只需要维护一个信息：下一个按序接收的帧序号）。</p>
<h4 id="运行时的GBN协议"><a href="#运行时的GBN协议" class="headerlink" title="运行时的GBN协议"></a>运行时的GBN协议</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/76.png" class="">

<h4 id="滑动窗口长度-1"><a href="#滑动窗口长度-1" class="headerlink" title="滑动窗口长度"></a>滑动窗口长度</h4><p>发送窗口尺寸过大，就会使得接收方无法区别新帧和旧帧。</p>
<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><ol>
<li>累计确认：标明接收方已经收到n号帧和它之前的全部帧。 超时事件</li>
<li>接收方只按序接收帧，不按序就被丢弃</li>
<li>确认序列号最大的、按序到达的帧</li>
<li>发送窗口最大为2^n-1,n个比特对帧编号。</li>
</ol>
<h4 id="性能分析"><a href="#性能分析" class="headerlink" title="性能分析"></a>性能分析</h4><p>优点：因连续发送数据帧而提高了信道利用率。</p>
<p>缺点：重传时需要把原来已经正确传送的数据帧重传，使传送效率降低。</p>
<h3 id="3-5-1-信道划分介质访问控制"><a href="#3-5-1-信道划分介质访问控制" class="headerlink" title="3.5.1 信道划分介质访问控制"></a>3.5.1 信道划分介质访问控制</h3><h4 id="传输数据使用的两种链路"><a href="#传输数据使用的两种链路" class="headerlink" title="传输数据使用的两种链路"></a>传输数据使用的两种链路</h4><p>点对点链路 </p>
<p>两个相邻节点通过一个链路相连，没有第三者。</p>
<p>应用：PPP协议，常用于<strong>广域网</strong></p>
<p>广播式链路</p>
<p>所有主机共享通信介质</p>
<p>应用：早期的总线以太网、无线局域网，常用于<strong>局域网</strong>。</p>
<p>典型拓扑结构：总线型、星形（逻辑总线型）</p>
<h4 id="介质访问控制"><a href="#介质访问控制" class="headerlink" title="介质访问控制"></a>介质访问控制</h4><p>内容：采取一定的措施，使得两对节点之间的通信不会发生相互干扰的情况。</p>
<p>介质访问控制：</p>
<ul>
<li><strong>静态划分信道–信道划分介质访问控制</strong><ul>
<li>频分多路复用FDM</li>
<li>时分多路复用TDM</li>
<li>波分多路复用WDM</li>
<li>码分多路复用CDM</li>
</ul>
</li>
<li><strong>动态分配信道</strong><ul>
<li>轮询访问介质访问控制<ul>
<li>令牌传递协议</li>
</ul>
</li>
<li>随机访问介质访问控制<ul>
<li>ALOHA协议</li>
<li>CSMA协议</li>
<li>CSMA/CD协议</li>
<li>CSMA/CA协议</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>信道划分介质访问控制：将使用介质的每个设备与来自同一信道的其他设备的通信隔离开，把<strong>时域和频域资源</strong>合理地分配给网络上的设备。</p>
<p>多路复用技术：把多个信号组合在一条物理信道进行传输，使得多个计算机或终端设备<strong>共享信道资源</strong>，提高信道利用率。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/75.png" class="">

<p>把一条广播信道，逻辑上分成几条用于两个节点之间通信的互不干扰的子信道，实际就是把<strong>广播信道转变为点对点信道</strong>。</p>
<h4 id="静态划分信道"><a href="#静态划分信道" class="headerlink" title="静态划分信道"></a>静态划分信道</h4><h5 id="频分多路复用FDM"><a href="#频分多路复用FDM" class="headerlink" title="频分多路复用FDM"></a>频分多路复用FDM</h5><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/74.png" class="">

<p>用户在分配到一定的频带后，在通信过程中自始自种都占用这个频带。<strong>频分复用的所有用户在同样的时间占用不同的带宽（频率带宽）资源。</strong></p>
<p>优点：</p>
<ol>
<li>充分利用传输介质带宽，系统效率较高；</li>
<li>由于技术比较成熟，实现也比较容易</li>
</ol>
<h5 id="时分多路复用TDM"><a href="#时分多路复用TDM" class="headerlink" title="时分多路复用TDM"></a>时分多路复用TDM</h5><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/73.png" class="">

<p>将时间划分一段段等长的时分复用（TDM帧）。每一个时分复用的用户在每一个TDM帧中占用<strong>固定序号的时隙</strong>，所有用户轮流占用道。</p>
<p>TDM帧是在物理层传送的比特流所划分的帧，标志一个周期。</p>
<h5 id="改进的时分复用–统计时分复用STDM"><a href="#改进的时分复用–统计时分复用STDM" class="headerlink" title="改进的时分复用–统计时分复用STDM"></a>改进的时分复用–统计时分复用STDM</h5><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/77.png" class="">

<p>每一个STDM帧中的<strong>时隙数</strong>是<strong>小于</strong>连接在集中器上的<strong>用户数</strong>的。各用户有了数据就随时发送集中器的<strong>输入缓存</strong>，然后集中器按顺序依次扫描输入缓存，把缓存中的输入数据放入STDM帧中，一个STDM帧满了就发出。<strong>STDM帧不是固定分配时隙，而是按需动态分配时隙。</strong></p>
<h5 id="波分多路复用WDM"><a href="#波分多路复用WDM" class="headerlink" title="波分多路复用WDM"></a>波分多路复用WDM</h5><p>波分多路复用就是<strong>光的频分多路复用</strong>。在一根光纤中传输多种不同波长（频率）的光信号，由于波长（频率）不同，所以各路光信号互不干扰，最后再用波长分解复用器将各路波长分解出来。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/78.png" class="">

<h5 id="码分多路复用CDM"><a href="#码分多路复用CDM" class="headerlink" title="码分多路复用CDM"></a>码分多路复用CDM</h5><p><strong>码分多址（CDMA）</strong>是码反复用的一种方式。</p>
<p>1个比特分为多个码片/芯片（chip），每一个站点都指定一个唯一的m位的芯片序列。</p>
<p>发送1时站点发送芯片序列，发送0时发送芯片序列反码。（通常把0写成-1）</p>
<p>多个站点同时发送数据的时候，要求各个站点芯片序列相互正交。</p>
<p>如何合并：各路数据在信道中被线性相加</p>
<p>如何分离：合并的数据和源站的芯片序列规格化内积。</p>
<h4 id="动态划分信道"><a href="#动态划分信道" class="headerlink" title="动态划分信道"></a>动态划分信道</h4><h5 id="3-5-2-ALOHA协议"><a href="#3-5-2-ALOHA协议" class="headerlink" title="3.5.2 ALOHA协议"></a>3.5.2 ALOHA协议</h5><h6 id="纯ALOHA"><a href="#纯ALOHA" class="headerlink" title="纯ALOHA"></a>纯ALOHA</h6><p><strong>协议思想：</strong></p>
<p>不监听信道，不按时间槽发送，随机重发。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/79.png" class="">

<p><strong>冲突检测：</strong></p>
<p>如果发生冲突，接收方就会检测出差错，然后不予确认，发送方在一定时间内收不到就判断发生冲突。检查发送时间前后T0时间内是否有人发送数据，没有就说明没有发生冲突。</p>
<p><strong>冲突解决：</strong></p>
<p>超时后等一随机时间再重传。</p>
<h6 id="时隙ALOHA协议"><a href="#时隙ALOHA协议" class="headerlink" title="时隙ALOHA协议"></a>时隙ALOHA协议</h6><p><strong>协议思想：</strong></p>
<p>把时间分成若干个相同的时间片，所有用户再时间在时间片开始时刻同步接入网络信道，若发送冲突，则必须等到下一个时间片开始时刻再发送。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/80.png" class="">

<p>优点：时隙ALOHA协议让所有站点都在时间片开始的时候发送数据，使得每个帧只有不冲突和完全冲突两种可能，从而避免了部分冲突。</p>
<h5 id="3-5-3-CSMA协议（载波监听多路访问协议）"><a href="#3-5-3-CSMA协议（载波监听多路访问协议）" class="headerlink" title="3.5.3 CSMA协议（载波监听多路访问协议）"></a>3.5.3 CSMA协议（载波监听多路访问协议）</h5><p>ALOHA协议缺点：所有用户想发就发，不管别的用户是否想要发送数据。</p>
<p><strong>CS(载波监听</strong>)：每一个站在<strong>发送数据之前</strong>要检测一下总线上是否有其他计算机在发送数据。</p>
<ul>
<li>当几个站同时在总线上发送数据时，总线上的信号<strong>电压摆动值</strong>将会增大（互相叠加）。当一个站检测到的信号电压摆动值超过一定门限时，就认为总线上至少有两个站同时发送数据，表明产生了冲突。</li>
</ul>
<p><strong>MA（多点接入）</strong>：表示许多计算机以多点接入的方式连接在一根总线上。</p>
<h6 id="协议思想：发送帧之前，监听信道"><a href="#协议思想：发送帧之前，监听信道" class="headerlink" title="协议思想：发送帧之前，监听信道"></a>协议思想：发送帧之前，监听信道</h6><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/81.png" class="">

<h6 id="坚持CSMA"><a href="#坚持CSMA" class="headerlink" title="坚持CSMA"></a>坚持CSMA</h6><ul>
<li>空闲则直接传输，不必等待。</li>
<li>忙则一直监听，直到空闲马上传输</li>
<li>如果有冲突（即有两个用户都发现信道空闲，一起发送数据而冲突），则等待一个随机长的时间再监听</li>
</ul>
<p>优点：只要媒体空闲，站点马上发送，避免了媒体利用率的损失</p>
<p>缺点：冲突不可避免</p>
<h6 id="非坚持CSMA"><a href="#非坚持CSMA" class="headerlink" title="非坚持CSMA"></a>非坚持CSMA</h6><ul>
<li>空闲则直接传输，不必等待</li>
<li>忙则等待一个随机的时间之后再进行监听</li>
</ul>
<p>优点：采用随机的重发延迟时间可以减少冲突发生的可能性。</p>
<p>缺点：可能会存在大家都在随机时间的等待，导致媒体的空闲。</p>
<h6 id="p-坚持CSMA"><a href="#p-坚持CSMA" class="headerlink" title="p-坚持CSMA"></a>p-坚持CSMA</h6><ul>
<li>空闲则以p概率直接传输，概率1-p等到到下一个时间槽再传输</li>
<li>如果忙则等待下一个时间槽再进行监听</li>
</ul>
<p>优点：既能减少冲突，也能减少媒体空闲时间</p>
<p>缺点：发生冲突后还是坚持把数据帧发送完，造成浪费</p>
<h6 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h6><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/82.png" class="">

<h5 id="3-6-4-CSMA-CD协议"><a href="#3-6-4-CSMA-CD协议" class="headerlink" title="3.6.4 CSMA/CD协议"></a>3.6.4 CSMA/CD协议</h5><p>载波监听多点接入/碰撞检测</p>
<p><strong>CS(载波监听</strong>)：每一个站在<strong>发送数据之前</strong>以及<strong>发送数据时</strong>要检测一下总线上是否有其他计算机在发送数据。</p>
<p><strong>MA（多点接入）</strong>：表示许多计算机以多点接入的方式连接在一根总线上。<strong>总线型网络</strong></p>
<p><strong>CD:碰撞检测</strong>，“边发送边监听”，适配器边发送数据边检测信道上信道电压的变化情况，以便于判断自己发送数据时其他站是否也在发送数据。（<strong>半双工网络</strong>）</p>
<h6 id="传播时延对载波监听的影响"><a href="#传播时延对载波监听的影响" class="headerlink" title="传播时延对载波监听的影响"></a>传播时延对载波监听的影响</h6><p>当站点发送的数据在传播过程中，其他站点因为数据还没到达，所以以为信道是空闲的，所以也会发送数据，从而造成冲突。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/83.png" class="">

<p>只要进过2t的时间还没有检测到碰撞，就能肯定这次发送不会发生碰撞</p>
<h6 id="如何确定碰撞后的重传时机？"><a href="#如何确定碰撞后的重传时机？" class="headerlink" title="如何确定碰撞后的重传时机？"></a>如何确定碰撞后的重传时机？</h6><ol>
<li>确定基本退避（推迟）时间为争用期2t</li>
<li>定义参数k，它等于重传次数，但k不超过10，即k=[重传次数，10]。当重传次数不超过10，k等于重传次数。当重传次数大于10时，k就不再增大而一直等于10。</li>
<li>从离散的整数集合[0,1, ,2^k-1]中随机取出一个数r，重传所需的时间就是<strong>r倍的基本退避时间</strong>，即<strong>2rt</strong>。</li>
<li>当重传16次仍不能成功，说明网络太拥挤，认为此帧永远无法正确发出，抛弃此帧并向高层报告出错。</li>
</ol>
<h6 id="最小帧长问题"><a href="#最小帧长问题" class="headerlink" title="最小帧长问题"></a>最小帧长问题</h6><p>希望检测到碰撞的时候，帧还没有发送完。</p>
<p>帧的传播时延至少两倍于信号在总线中的传播时延。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/84.png" class="">

<h5 id="3-5-5-CSMA-CA协议"><a href="#3-5-5-CSMA-CA协议" class="headerlink" title="3.5.5 CSMA/CA协议"></a>3.5.5 CSMA/CA协议</h5><p><strong>CA</strong>：碰撞避免</p>
<p><strong>为什么要有CSMA/CA？</strong>因为CSMA/CA常用于无线局域网，而CSMA/CD协议常用于以太网</p>
<p><strong>无线局域网</strong></p>
<ul>
<li>无法做到360度全面碰撞检测</li>
<li>隐蔽站<ul>
<li>当A和C都检测不到信号，认为信道空闲时，同时向终端B发送数据帧，就会导致冲突。 </li>
</ul>
</li>
</ul>
<h6 id="工作原理："><a href="#工作原理：" class="headerlink" title="工作原理："></a>工作原理：</h6><p>发送数据前，先检测信道是否空闲。</p>
<ul>
<li>如果是空闲，则发送<strong>RTS</strong>，RTS包括发射端地址、接收端地址、下一份数据将持续发送的时间等信息</li>
<li>信道忙则等待</li>
<li>接收端收到RTS，将响应<strong>CTS</strong></li>
<li>发送端收到CTS后，开始发送数据帧（同时<strong>预约信道</strong>：发送方告知其他站点自己要传多久数据）</li>
<li>接收端收到数据帧后，将<strong>CRC（循环冗余校验）</strong>来检验数据是否正确，正确则响应<strong>ACK帧</strong></li>
<li>发送方收到ACK就可以下一个数据帧的发送，若没有则一直重传至规定重发次数为止（采用<strong>二进制指数退避法</strong>来确定随机的推迟时间）</li>
</ul>
<h5 id="CSMA-CA与CSMA-CD"><a href="#CSMA-CA与CSMA-CD" class="headerlink" title="CSMA/CA与CSMA/CD"></a>CSMA/CA与CSMA/CD</h5><p><strong>相同点：</strong></p>
<ul>
<li>都属于CSMA的思路，都是<strong>先听再说</strong>。两个在接入信道之前都需要对信道进行监听。当发现信道空闲后，才能接入</li>
</ul>
<p><strong>不同点：</strong></p>
<ul>
<li>传播介质不同：<ul>
<li>CSMA/CA用于以太网（有线）</li>
<li>CSMA/CD用于无线局域网（无线）</li>
</ul>
</li>
<li>载波检测方式不同：<ul>
<li>CSMA/CA采用能量检测（ED）、载波检测（CS）、能量载波混合检测三种检测信道空闲的方式</li>
<li>CSMA/CD通过电缆中电压的变化来检测</li>
</ul>
</li>
<li>CSMA/CA是检测冲突，CSMA/CD是避免冲突<ul>
<li>都有重传的上限</li>
</ul>
</li>
</ul>
<h3 id="3-5-4-轮询访问介质访问控制"><a href="#3-5-4-轮询访问介质访问控制" class="headerlink" title="3.5.4 轮询访问介质访问控制"></a>3.5.4 轮询访问介质访问控制</h3><h4 id="介质访问控制-1"><a href="#介质访问控制-1" class="headerlink" title="介质访问控制"></a>介质访问控制</h4><p>信道划分介质访问控制（MAC）协议：基于<strong>多路复用</strong>技术划分资源</p>
<ul>
<li>当网络负载重：共享信道效率搞，且公平</li>
<li>当网络负载低：共享效率低</li>
</ul>
<p>随机访问MAC协议：用户根据意愿随机发送信息，发送信息时可独占信道带宽。</p>
<ul>
<li>当网络负载重：产生冲突开销</li>
<li>当网络负载轻：共享信道效率高，单个节点可利用信道全部带宽</li>
</ul>
<p>轮询访问MAC协议:即不产生冲突，又要发送时占全部带宽</p>
<h4 id="轮询协议"><a href="#轮询协议" class="headerlink" title="轮询协议"></a>轮询协议</h4><p>主节点轮流”邀请“从属结点发送数据。</p>
<p><strong>问题：</strong></p>
<ol>
<li>轮询开销</li>
<li>等待延迟</li>
<li>单点故障 </li>
</ol>
<h4 id="令牌传递协议"><a href="#令牌传递协议" class="headerlink" title="令牌传递协议"></a>令牌传递协议</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/85.png" class="">

<p>令牌：一个特殊格式的MAC控制帧，不含任何信息。</p>
<p>控制信道的使用，确保同一时刻只有一个结点独占信道。</p>
<p><strong>令牌环网无碰撞</strong></p>
<p>每一个结点都可以在一定时间内（令牌持有时间）获得发送数据的权力，并不是无限制地持有令牌</p>
<p><strong>问题：</strong></p>
<ol>
<li>令牌开销</li>
<li>等待延迟</li>
<li>单点故障</li>
</ol>
<p>应用于令牌环网（物理星型拓扑，逻辑环形拓扑）</p>
<p>采用令牌传送协议常用于<strong>负载较重、通信量较大</strong>的网络中</p>
<h3 id="3-6-1-局域网基本概念和体系结构"><a href="#3-6-1-局域网基本概念和体系结构" class="headerlink" title="3.6.1 局域网基本概念和体系结构"></a>3.6.1 局域网基本概念和体系结构</h3><p><strong>局域网：</strong>简称LAN，是指<strong>某一区域内</strong>由多台计算机互联成的计算机组，使用<strong>广播信道</strong>。</p>
<p>特点：</p>
<ol>
<li>覆盖的地理范围较小，只在一个相对独立的局部范围内联，如一座或集中的建筑群内。</li>
<li>使用专门铺设的传输介质（双绞线、同轴电缆）进行联网，数据传输率高。</li>
<li>通信延迟时间短，误码率低，可靠性高。</li>
<li>各站为平等关系，共享传输信道。</li>
<li>多采用分布式控制和广播式通信，能进行广播和组播。</li>
</ol>
<p>主要要素：<strong>网络拓扑</strong>、<strong>传输介质</strong>、<strong>介质访问方法</strong></p>
<h4 id="局域网拓扑结构"><a href="#局域网拓扑结构" class="headerlink" title="局域网拓扑结构"></a>局域网拓扑结构</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/86.png" class="">

<h4 id="局域网传输介质"><a href="#局域网传输介质" class="headerlink" title="局域网传输介质"></a>局域网传输介质</h4><ul>
<li>局域网<ul>
<li>有线局域网<ul>
<li>常用介质：双绞线、同轴电缆、光纤</li>
</ul>
</li>
<li>无线局域网<ul>
<li>电磁波</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="局域网介质访问控制方法"><a href="#局域网介质访问控制方法" class="headerlink" title="局域网介质访问控制方法"></a>局域网介质访问控制方法</h4><ul>
<li>SCMA/CD</li>
<li>令牌总线</li>
<li>令牌环</li>
</ul>
<h3 id="3-6-2-以太网"><a href="#3-6-2-以太网" class="headerlink" title="3.6.2 以太网"></a>3.6.2 以太网</h3><p>以太网（Ethernet）指的是由Xerox、Intel和DEC公司联合开发的基<strong>带总线局域网规范</strong>，是当今现有局域网采用的通信协议标准。以太网使用CSMA/CD技术。</p>
<p>以太网在局域网各种技术中占<strong>统治性</strong>地位：</p>
<ol>
<li>造价低廉</li>
<li>是应用最广泛的局域网技术</li>
<li>比令牌环网、ATM网便宜、简单</li>
<li>满足网络速率要求</li>
</ol>
<p>以太网的两个标准：</p>
<ul>
<li>DIX Ethernet V2：第一个局域网（以太网）规约</li>
<li>IEEE 802.3：IEEE 802委员会802.3工作组制定的第一个IEEE的以太网标准。</li>
</ul>
<h4 id="以太网提供的无连接、不可靠的服务"><a href="#以太网提供的无连接、不可靠的服务" class="headerlink" title="以太网提供的无连接、不可靠的服务"></a>以太网提供的无连接、不可靠的服务</h4><p>无连接：发送方和接收方之间无“握手过程”。</p>
<p>不可靠：不对发送方的数据帧编号，接收方不向发送方进行确认，差错帧直接丢弃，差错纠正由高层负责。</p>
<p>以太网只实现无差错接收、不实现可靠传输。</p>
<h4 id="以太网传输介质与拓扑结构的发展"><a href="#以太网传输介质与拓扑结构的发展" class="headerlink" title="以太网传输介质与拓扑结构的发展"></a>以太网传输介质与拓扑结构的发展</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/87.png" class="">

<p>以太网拓扑：逻辑上总线型，物理上星型。</p>
<h4 id="10BASE-T以太网"><a href="#10BASE-T以太网" class="headerlink" title="10BASE-T以太网"></a>10BASE-T以太网</h4><p>10BASE-T是传送<strong>基带信号</strong>的<strong>双绞线</strong>以太网，T表示采用双绞线，现10BASE-T采用的是<strong>无屏蔽双绞线（UTP）</strong>，传输速率是10Mb/s。</p>
<p>逻辑上总线型，物理上星型，采用曼彻斯特编码，使用CSMA/CD技术。</p>
<h4 id="适配器与MAC地址"><a href="#适配器与MAC地址" class="headerlink" title="适配器与MAC地址"></a>适配器与MAC地址</h4><p>计算机与外界有局域网的连接是通过<strong>通信适配器</strong>的。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/88.png" class="">

<ul>
<li>ROM上有计算机硬件地址MAC地址</li>
</ul>
<p>MAC地址：每个适配器有一个全球唯一的48位二进制地址，前24位代表厂家（由IEEE规定），后24位厂家自己指定。常用6个十六进制数表示。</p>
<h4 id="以太网MAC帧"><a href="#以太网MAC帧" class="headerlink" title="以太网MAC帧"></a>以太网MAC帧</h4><p>最常用的MAC帧是以太网V2的格式</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/89.png" class="">

<p>插入的8B被称为前导码，不是MAC帧的一部分。</p>
<p>类型：表示IP数据包所使用的协议。</p>
<p>FCS：CRC循环冗余检验的检验序列。</p>
<p>与IEEE802.3的区别：</p>
<ol>
<li>第三个字段是长度/类型</li>
<li>当长度/类型字段值小于0x0600时，数据字段必须装入LLC子层。</li>
</ol>
<h4 id="高速以太网"><a href="#高速以太网" class="headerlink" title="高速以太网"></a>高速以太网</h4><p>速率大于等于100Mb/s的以太网称为高速以太网。</p>
<ol>
<li><p>10BASE-T以太网</p>
<p>在<strong>双绞线</strong>上传送<strong>100Mb/s基带信号</strong>的<strong>星型拓扑</strong>以太网，仍使用IEEE802.3的CSMA/CD协议。</p>
<p>支持全双工和半双工，可在全双工方式下工作而无冲突。</p>
</li>
<li><p>吉比特以太网</p>
<p>在光纤或双绞线上传送1Gb/s信号。</p>
<p>支持全双工和半双工，可在全双工方式下工作而无冲突。</p>
</li>
<li><p>10吉比特</p>
<p>在光纤上传送10Gb/s信号。</p>
<p>只支持全双工，无争用问题</p>
</li>
</ol>
<h3 id="3-6-3-IEEE-802-11"><a href="#3-6-3-IEEE-802-11" class="headerlink" title="3.6.3 IEEE 802.11"></a>3.6.3 IEEE 802.11</h3><p>IEEE 802.11是无线局域网通用的标准，它是由IEEE所定义的无线网络通信的标准。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/90.png" class="">

<h4 id="802-11的MAC帧头格式"><a href="#802-11的MAC帧头格式" class="headerlink" title="802.11的MAC帧头格式"></a>802.11的MAC帧头格式</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/91.png" class="">

<p>源地址和目的地址：是两个设备的MAC地址</p>
<p>接收端和发送端：设备连接的网络基站的MAC地址</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/92.png" class="">

<h4 id="有固定基础设施无线局域网"><a href="#有固定基础设施无线局域网" class="headerlink" title="有固定基础设施无线局域网"></a>有固定基础设施无线局域网</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/93.png" class="">

<h4 id="无固定基础设施无线局域网的自组织网络"><a href="#无固定基础设施无线局域网的自组织网络" class="headerlink" title="无固定基础设施无线局域网的自组织网络"></a>无固定基础设施无线局域网的自组织网络</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/94.png" class="">

<h3 id="3-7-PPP协议-amp-HDLC协议"><a href="#3-7-PPP协议-amp-HDLC协议" class="headerlink" title="3.7 PPP协议&amp;HDLC协议"></a>3.7 PPP协议&amp;HDLC协议</h3><h4 id="广域网"><a href="#广域网" class="headerlink" title="广域网"></a>广域网</h4><p>广域网（WAN，Wide Area Nerwork），通常跨接很大的物理范围，所覆盖的范围从几十公里到几千公里，它能连接多个城市或国家，或横跨几个洲并能提供远距离通信，形成国际性的远程网络。</p>
<p>广域网的通信子网主要使用<strong>分组交换</strong>技术。广域网的通信子网可以利用公用分组交换网、卫星通信网和无线分组交换网，它将分布在不同地区的<strong>局域网和计算机系统</strong>互联起来，达到<strong>资源共享</strong>的目的。如因特网是世界范围内最大的广域网。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/95.png" class="">

<h4 id="PPP协议的特点"><a href="#PPP协议的特点" class="headerlink" title="PPP协议的特点"></a>PPP协议的特点</h4><p>点对点协议PPP（Point-to-Point）是目前使用最广泛的数据链路层协议，用户使用拨号电话接入因特网时一般都使用PPP协议。</p>
<p>PPP协议只支持全双工链路。</p>
<h4 id="PPP协议应满足的要求"><a href="#PPP协议应满足的要求" class="headerlink" title="PPP协议应满足的要求"></a>PPP协议应满足的要求</h4><ul>
<li>简单：对于链路层的帧，无需纠错，无需序号，无需流量控制</li>
<li>封装成帧：帧定界符</li>
<li>透明传输：与帧定界符一样的比特组合的数据应该如何处理：异步线路用字节填充，同步线路用比特填充。</li>
<li>多种网络层协议：封装的IP数据报可以采用多种协议</li>
<li>多种类型链路 串/并行、同/异步、电/光</li>
<li>差错检测：错就丢弃</li>
<li>检测连接状态：链路是否正常工作</li>
<li>最大传送单元：数据部分最大长度MTU</li>
<li>网络层地址协商：知道通信双方的网络层地址</li>
<li>数据压缩协商</li>
</ul>
<h4 id="PPP协议无需满足的要求"><a href="#PPP协议无需满足的要求" class="headerlink" title="PPP协议无需满足的要求"></a>PPP协议无需满足的要求</h4><ul>
<li>无需纠错</li>
<li>无需流量控制</li>
<li>无需序号</li>
<li>不支持多点线路</li>
</ul>
<h4 id="PPP协议的三个组成部分"><a href="#PPP协议的三个组成部分" class="headerlink" title="PPP协议的三个组成部分"></a>PPP协议的三个组成部分</h4><ol>
<li>一个将IP数据报封装到串行链路（同步串行/异步串行）的方法</li>
<li>链路控制协议LCP：建立并维护数据链路连接。（身份验证）</li>
<li>网络控制协议NCP：PPP可支持多种网络层协议，每个不同的网络层协议都要一个相应的NCP来配置，为网络层协议建立和配置逻辑连接。</li>
</ol>
<h4 id="PPP协议状态图"><a href="#PPP协议状态图" class="headerlink" title="PPP协议状态图"></a>PPP协议状态图</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/96.png" class="">

<h4 id="PPP协议的帧格式"><a href="#PPP协议的帧格式" class="headerlink" title="PPP协议的帧格式"></a>PPP协议的帧格式</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/97.png" class="">

<p>帧定界法：7E</p>
<p>当信息部分出现帧定界符时插入转义字符：7D</p>
<p>协议部分：</p>
<ol>
<li>IP数据报</li>
<li>LCP的数据</li>
<li>网络层控制数据</li>
<li>。。。。。</li>
</ol>
<h4 id="HDLC协议"><a href="#HDLC协议" class="headerlink" title="HDLC协议"></a>HDLC协议</h4><p>高级数据链路控制（High-Level Data Link Control）是一个在同步网上传输数据、面向比特的数据链路层协议，它是由国际标准化组织（ISO）根据IBM公司的SDLC协议扩展开发而成的。</p>
<ul>
<li>数据报文可透明传输，用于实现透明传输的“0比特插入法”易于硬件实现</li>
<li>采用全双工通信</li>
<li>所有帧采用CRC检验，对信息帧进行顺序编号，可防止漏收或重复，传输可靠性高。</li>
</ul>
<h4 id="HDLC的站"><a href="#HDLC的站" class="headerlink" title="HDLC的站"></a>HDLC的站</h4><ol>
<li>主站的主要功能是发送命令（包括数据信息）帧、接收响应帧。并负责对整个链路的控制系统的初启、流程的控制、差错检测或恢复。</li>
<li>从站的主要功能是接收主站发来的命令帧，向主站发送响应帧，并且配合主站参与差错恢复等链路控制。</li>
<li>复合站的主要功能是既能发送，又能接收命令帧和响应帧，并且负责整个链路的控制。</li>
</ol>
<h5 id="三种数据操作方式："><a href="#三种数据操作方式：" class="headerlink" title="三种数据操作方式："></a>三种数据操作方式：</h5><ol>
<li>正常响应方式</li>
<li>异步平衡方式</li>
<li>异步响应方式</li>
</ol>
<p>HDLC的帧格式</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/98.png" class="">

<ol>
<li>信息帧（I）第一位是0，用来传输数据信息，或使用捎带技术对数据进行确认。</li>
<li>监督帧（S）10，用于流量控制和差错控制，执行对信息帧的确认、请求重发和请求暂停发送等功能。</li>
<li>无编号帧（U）11，用于提供对链路的建立、拆除等多种控制功能。</li>
</ol>
<h4 id="PPP协议-amp-HDLC协议"><a href="#PPP协议-amp-HDLC协议" class="headerlink" title="PPP协议&amp;HDLC协议"></a>PPP协议&amp;HDLC协议</h4><p>HDLC、PPP只支持全双工链路。</p>
<p>都可以实现透明传输。</p>
<p>都可以实现差错控制，但不纠正差错。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/99.png" class="">

<h3 id="3-8-链路层设备"><a href="#3-8-链路层设备" class="headerlink" title="3.8 链路层设备"></a>3.8 链路层设备</h3><h4 id="物理层扩展以太网"><a href="#物理层扩展以太网" class="headerlink" title="物理层扩展以太网"></a>物理层扩展以太网</h4><img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/100.png" class="">

<p>集线器连接的设备长度是有限的。</p>
<p>通过电信号转化为光信号，使得传输距离更长。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/101.png" class="">

<p>通过主干集线器将区域网扩大，更容易冲突了。</p>
<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/102.png" class="">

<img src="/2021/11/06/%E6%95%B0%E6%8D%AE%E9%93%BE%E8%B7%AF%E5%B1%82/103.png" class="">

<h4 id="链路层扩展以太网（网桥-amp-交换机）"><a href="#链路层扩展以太网（网桥-amp-交换机）" class="headerlink" title="链路层扩展以太网（网桥&amp;交换机）"></a>链路层扩展以太网（网桥&amp;交换机）</h4><p>网桥根据MAC帧的目的地址对帧进行转发和过滤。当网桥收到一个帧时，并不向所有接口转发此帧</p>
]]></content>
      <categories>
        <category>计算机网络</category>
      </categories>
  </entry>
  <entry>
    <title>基础知识</title>
    <url>/2021/07/13/%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/</url>
    <content><![CDATA[<h2 id="什么是操作系统？"><a href="#什么是操作系统？" class="headerlink" title="什么是操作系统？"></a>什么是操作系统？</h2><p>只有计算机硬件的东西叫裸机。</p>
<p>在计算机硬件和应用之间的一层软件就是操作系统。</p>
<ul>
<li>方便我们使用硬件</li>
<li>高效的使用硬件</li>
</ul>
<p>操作系统管理这些硬件</p>
<ul>
<li><strong>CPU管理</strong></li>
<li><strong>内存管理</strong></li>
<li><strong>终端管理</strong></li>
<li><strong>磁盘管理</strong></li>
<li><strong>文件管理</strong></li>
<li>网络管理</li>
<li>电源管理</li>
<li>多核管理</li>
</ul>
<span id="more"></span>

<h3 id="学习操作系统可以有很多层次"><a href="#学习操作系统可以有很多层次" class="headerlink" title="学习操作系统可以有很多层次"></a>学习操作系统可以有很多层次</h3><ul>
<li>从应用软件出发“探到操作系统”<ul>
<li>集中在使用计算机的接口上</li>
<li>使用显示器：printf；使用CPU：fork，使用文件：open、read……</li>
</ul>
</li>
<li>从应用软件触发“进入操作系统”<ul>
<li>一段文字是如何写到磁盘上的…</li>
</ul>
</li>
<li>从硬件触发“设计并实现操作系统”<ul>
<li>给一个板子，配一个操作系统</li>
</ul>
</li>
</ul>
<h2 id="揭开钢琴的盖子"><a href="#揭开钢琴的盖子" class="headerlink" title="揭开钢琴的盖子"></a>揭开钢琴的盖子</h2><h3 id="从图灵机到通用图灵机"><a href="#从图灵机到通用图灵机" class="headerlink" title="从图灵机到通用图灵机"></a>从图灵机到通用图灵机</h3><p><img src="1.png"></p>
<p>图灵机只能做一件事，就像一个只会做一道菜的厨师。</p>
<p><img src="2.png"></p>
<p>通用图灵机可以进行编程了，就如同给厨师不同的菜谱可以做出不同的菜。</p>
<h3 id="从通用图灵机到计算机"><a href="#从通用图灵机到计算机" class="headerlink" title="从通用图灵机到计算机"></a>从通用图灵机到计算机</h3><ul>
<li>伟大想法的工程实现<ul>
<li>冯诺依曼存储程序思想（1946年提出）</li>
<li>主要思想：将程序和数据放在计算机内部的存储器中，计算机在程序控制下一步一步进行处理</li>
<li>计算机由五大部件组成：输入设备、输出设备、存储器、运算器、控制器。</li>
</ul>
</li>
</ul>
<h3 id="打开电源，计算机执行的第一条指令是什么？"><a href="#打开电源，计算机执行的第一条指令是什么？" class="headerlink" title="打开电源，计算机执行的第一条指令是什么？"></a>打开电源，计算机执行的第一条指令是什么？</h3><ul>
<li><p>计算模型=&gt;我们要关注指针IP及其指向的内容</p>
<ul>
<li>计算机刚打开电源时，IP=？</li>
<li>由硬件设计者决定！</li>
</ul>
</li>
<li><p>看x86 PC</p>
<ol>
<li><p>x86刚开机时CPU处于实模式。</p>
<p>实模式和保护模式对应，实模式的寻址CS:IP(CS左移4位+IP)，和保护模式不同。</p>
</li>
<li><p>开机时，CS=0xFFFF；IP=0x0000</p>
</li>
<li><p>寻址0xFFFF0(ROM BIOS映射区)</p>
</li>
<li><p>检查RAM，键盘，显示器，软硬磁盘，主板……</p>
</li>
<li><p>将磁盘0磁道0扇区（MBR）读入0x7c00处（一个扇区524字节）</p>
</li>
<li><p>设置CS=0x07c0,IP=0x0000</p>
</li>
</ol>
</li>
</ul>
<h3 id="0x7c00处存放的代码"><a href="#0x7c00处存放的代码" class="headerlink" title="0x7c00处存放的代码"></a>0x7c00处存放的代码</h3><ul>
<li>就是从磁盘引导扇区读入的那512字节<ul>
<li>引导扇区就是启动设备的第一个扇区（开机时按住del健可进入启动设备设置界面，可以设置为光盘启动）</li>
<li>启动设备信息被设置在CMOS中…（CMOS：互补金属氧化物半导体。用来存储时钟和硬件配置信息）</li>
<li>因此，硬盘的第一个扇区上存放着开机后执行的第一段我们可以控制的程序</li>
<li>操作系统的故事从这里开始</li>
</ul>
</li>
</ul>
<h3 id="引导扇区代码：bootsect-s"><a href="#引导扇区代码：bootsect-s" class="headerlink" title="引导扇区代码：bootsect.s"></a>引导扇区代码：bootsect.s</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.globl begtext,begdata,begbss,endtext,enddata,endbss</span><br><span class="line">.text	//文本段</span><br><span class="line">begtext:</span><br><span class="line">.data	//数据段</span><br><span class="line">begdata:</span><br><span class="line">.bss	//为初始化数据段</span><br><span class="line">begbss:</span><br><span class="line">entry start	//关键字entry告诉链接器“程序入口”</span><br><span class="line">start:</span><br><span class="line">	mov ax.#BOOTSEG</span><br><span class="line">	//0x07c0,启动代码所在位置放入ax</span><br><span class="line">	mov ds,ax</span><br><span class="line">	//将启动代码与ds寄存器关联</span><br><span class="line">	mov ax.#INITSEG</span><br><span class="line">	//启动代码要被复制到的目的地址</span><br><span class="line">	mov es,ax</span><br><span class="line">	//将目的地址与es寄存器关联</span><br><span class="line">	mov cx,#256</span><br><span class="line">	</span><br><span class="line">	sub si,si</span><br><span class="line">	sub di,di</span><br><span class="line">	//si清零,ds:si即0x07c00</span><br><span class="line">	//di清零,es:di即0x90000</span><br><span class="line">	rep</span><br><span class="line">	//循环移动cx（256）个字,512字节</span><br><span class="line">	//将0x07c0 : 0x0000处的256个字移到0x9000 : 0x0000处</span><br><span class="line">	movw</span><br><span class="line">	jmpi go,INITSEG	//CS=INITSEG	IP=go</span><br><span class="line">	//jmpi 段间跳转</span><br><span class="line">/*</span><br><span class="line">SETUPLEN = 4	nomber of setup-sectors</span><br><span class="line">BOOTSEG  = 0x07c0	original address of boot-sector</span><br><span class="line">INITSEG  = 0x9000	we move boot here - out of the way</span><br><span class="line">SETUPSEG = 0x9020	setup starts here</span><br><span class="line">SYSSEG   = 0x1000	system loaded at 0x10000 (65536).</span><br><span class="line">ENDSEG   = SYSSEG + SYSSIZE	where to stop loading</span><br><span class="line">*/</span><br><span class="line">//由于启动代码复制到了新位置，需要更改相应寄存器的值</span><br><span class="line">go:	mov ax,cs	</span><br><span class="line">	//cs=0x9000</span><br><span class="line">	mov ds,ax </span><br><span class="line">	mov	es,ax</span><br><span class="line">	mov ss,ax</span><br><span class="line">	//开始引入栈</span><br><span class="line">	mov sp,#0xff00</span><br><span class="line">	//为call做准备，栈空间为0x9ff00</span><br><span class="line">//开始加载setup块</span><br><span class="line">load_setup:	//载入setup模块</span><br><span class="line">	mov	dx,#0x0000</span><br><span class="line">	//设置驱动器和磁头(drive 0, head 0): 软盘 0 磁头 </span><br><span class="line">	mov cx,#0x0002</span><br><span class="line">	//设置扇区号和磁道(sector 2, track 0): 0 磁头、0 磁道、2 扇区</span><br><span class="line">	mov bx,#0x0200</span><br><span class="line">	//设置读入的内存地址：BOOTSEG+address = 512，偏移512字节</span><br><span class="line">	mov ax,#0x0200+SETUPLEN</span><br><span class="line">	//SETUPLEN是读入的扇区个数，Linux 0.11 设置的是 4</span><br><span class="line">	int 0x13	</span><br><span class="line">	//BIOS中断,将setup.s对应的程序加载至内存指定地址</span><br><span class="line">	jnc ok_load_setup</span><br><span class="line">	//cf标志寄存器为0就跳转至ok_load_setup块   </span><br><span class="line">	mov dx,#0x0000</span><br><span class="line">	mov ax,#0x0000</span><br><span class="line">	int 0x13</span><br><span class="line">	//复位,cf!=０则重新设置传入信息,进入中断 </span><br><span class="line">	j	load_setup	//重读</span><br></pre></td></tr></table></figure>

<p><img src="3.png"></p>
<ul>
<li>0x13是BIOS读磁盘扇区的中断：<ul>
<li>ah=0x02    读磁盘</li>
<li>al=扇区数量（SETUPLEN=4）</li>
<li>ch=柱面号</li>
<li>cl=开始扇区（0x02，第二个扇区，因为第一个扇区boot扇区）</li>
<li>dh=磁头号</li>
<li>dl=驱动器号</li>
<li>es（0x9000）:bx（0x0200）=内存地址(0x90200)</li>
</ul>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ok_load_setup:	//载入setup模块</span><br><span class="line">	mov dl,#0x00</span><br><span class="line">	mov ax,#0x0800</span><br><span class="line">	//ah=0x08获得磁盘参数</span><br><span class="line">	int 0x13</span><br><span class="line">	mov ch，#0x00</span><br><span class="line">	mov sectors，cx</span><br><span class="line">	//保存每磁道扇区数</span><br><span class="line">	mov ah，#0x03</span><br><span class="line">	//读光标位置</span><br><span class="line">	xor bh,bh</span><br><span class="line">	int 0x10</span><br><span class="line">	mov cx,#24</span><br><span class="line">	//要显示24个字符</span><br><span class="line">	mov bx,#0x0007</span><br><span class="line">	//7是显示属性</span><br><span class="line">	</span><br><span class="line">	mov bp，#msg1</span><br><span class="line">	mov ax，#1301</span><br><span class="line">	int 0x10	//显示字符</span><br><span class="line">	//将msg1的字符显示到光标位置</span><br><span class="line">	/*</span><br><span class="line">	bootsect.s中的数据，在文件末尾</span><br><span class="line">	.sectors: .word 0	//磁道扇区数</span><br><span class="line">	msg1:.byte 13,10</span><br><span class="line">		 .ascii &quot;Loading system...&quot;</span><br><span class="line">		 .byte 13,10,13,10</span><br><span class="line">	*/</span><br><span class="line">	mov    ax,#SYSSEG    </span><br><span class="line">	//内核代码被加载到的地址</span><br><span class="line">	mov es,ax</span><br><span class="line">	call read_it	//读入system模块</span><br><span class="line">	jumpi 0，SETUPSEG</span><br><span class="line">	//转入0x9020 : 0x0000执行setup.s</span><br><span class="line">read_it:</span><br><span class="line">	mov ax,es</span><br><span class="line">	cmp ax,#ENDSEG</span><br><span class="line">	//system模块很大，要跨越磁道！</span><br><span class="line">	//ENDSEG=SYSSEG+SYSSIZE</span><br><span class="line">	//SYSSIZE=0x8000(该变量可根据Image大小设定)</span><br><span class="line">	jb ok1_read</span><br><span class="line">	ret</span><br><span class="line">ok1_read:</span><br><span class="line">	mov ax,sectors</span><br><span class="line">	sub ax,sread	//sread是当前磁道已读扇区数，ax未读扇区数</span><br><span class="line">	call read_track	//读磁道...</span><br></pre></td></tr></table></figure>

<p>引导扇区的末尾//BIOS用以识别引导扇区</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.org 510</span><br><span class="line">	.word 0xAA55 //扇区的最后两个字节</span><br></pre></td></tr></table></figure>

<p>可以转入setup执行了，jmpi 0，SETUPSEG</p>
<h2 id="操作系统启动"><a href="#操作系统启动" class="headerlink" title="操作系统启动"></a>操作系统启动</h2><h3 id="setup模块，即setup-s"><a href="#setup模块，即setup-s" class="headerlink" title="setup模块，即setup.s"></a>setup模块，即setup.s</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">start:</span><br><span class="line">	mov ax,#INITSEG</span><br><span class="line">	mov ds,ax</span><br><span class="line">	mov ah,#0x03</span><br><span class="line">	//获取光标位置</span><br><span class="line">	xor bh,bh</span><br><span class="line">	//bh=页号=0（输入）</span><br><span class="line">	int 0x10</span><br><span class="line">	//输出： DH=行号，DL=列号</span><br><span class="line">	mov [0],dx</span><br><span class="line">	mov ah,#0x88</span><br><span class="line">	int 0x15</span><br><span class="line">	//获取内存大小</span><br><span class="line">	mov [2],ax</span><br><span class="line">	cli	//不允许中断</span><br><span class="line">	mov ah,#0x0000</span><br><span class="line">	//保存光标的行号和列号到 0x90000，共占2字节</span><br><span class="line">	cld</span><br><span class="line">do_move:</span><br><span class="line">	mov es,ax</span><br><span class="line">	add ax,#0x1000</span><br><span class="line">	cmp ax,#0x9000</span><br><span class="line">	jz	end_move</span><br><span class="line">	mov	ds,ax</span><br><span class="line">	sub	di,di</span><br><span class="line">	sub si,si</span><br><span class="line">	mov	cx,#0x8000</span><br><span class="line">	rep</span><br><span class="line">	movsw</span><br><span class="line">	jmp	do_move</span><br></pre></td></tr></table></figure>

<p><img src="4.png"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">do_move的C翻译:</span><br><span class="line"></span><br><span class="line">ax = <span class="number">0</span>;</span><br><span class="line">cld;</span><br><span class="line"><span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">    es = ax;</span><br><span class="line">    ax += <span class="number">0x1000</span>;</span><br><span class="line">    <span class="keyword">if</span>(ax == <span class="number">0x9000</span>)</span><br><span class="line">        <span class="keyword">break</span>;  <span class="comment">//结束移动</span></span><br><span class="line">    ds = ax;</span><br><span class="line">    di = si = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;<span class="number">0x8000</span>; ++i)&#123;</span><br><span class="line">       <span class="built_in">memcpy</span>(es:di, ds:si, <span class="number">2</span>);</span><br><span class="line">       di += <span class="number">2</span>;</span><br><span class="line">       si += <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="保护模式下的地址翻译和中断处理"><a href="#保护模式下的地址翻译和中断处理" class="headerlink" title="保护模式下的地址翻译和中断处理"></a>保护模式下的地址翻译和中断处理</h3><ul>
<li><p>保护模式下的地址翻译</p>
<p>即gdt（全局描述符表）的作用：</p>
<p>t是table，所以实模式下:cs左移4+ip。保护模式下：根据cs查表+ip。cs这里被称为选择子。</p>
<p><img src="5.png"></p>
</li>
<li><p>保护模式下中断函数入口</p>
<p>即idt（中断描述符表）的作用：</p>
<p>t是table，仿照gdt，通过int n的n进行查表</p>
</li>
</ul>
<p><img src="6.png"></p>
<h3 id="将setup移到0地址处"><a href="#将setup移到0地址处" class="headerlink" title="将setup移到0地址处"></a>将setup移到0地址处</h3><ul>
<li>但0地址处是有重要内容的<ul>
<li>此处存储的是中断向量表</li>
</ul>
</li>
<li>以后不调用int指令了吗？</li>
<li>因为操作系统要让硬件进入保护模式了</li>
<li>保护模式下int n和cs：ip的解释不再和实模式一样</li>
</ul>
<p>我们需要初始化这些表</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">end_move:</span><br><span class="line">	mov ax,#SETUPSEG</span><br><span class="line">	mov ds,ax</span><br><span class="line">	lidt idt_48</span><br><span class="line">	lgdt gdt_48</span><br><span class="line">	//设置保护模式下的中断和寻址</span><br><span class="line">idt_48:</span><br><span class="line">	.word 0</span><br><span class="line">	.word 0,0	//保护模式中断函数表</span><br><span class="line">gdt_48:</span><br><span class="line">	.word 0x800</span><br><span class="line">	.word 512+gdt,0x9</span><br><span class="line">gdt:</span><br><span class="line">	.word 0,0,0,0</span><br><span class="line">	.word 0x07FF,0x0000,9x9A00,0x00C0</span><br><span class="line">	.word 0x07FF,0x0000,9x9200,0x00C0</span><br></pre></td></tr></table></figure>

<h3 id="进入保护模式"><a href="#进入保护模式" class="headerlink" title="进入保护模式"></a>进入保护模式</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">	call empty_8042</span><br><span class="line">//8042是键盘控制器，</span><br><span class="line">	mov	al,#0xD1</span><br><span class="line">	out #0x64,al</span><br><span class="line">	call empty_8042</span><br><span class="line">	mov al,#0xDF</span><br><span class="line">	out #0x60,al</span><br><span class="line">	call empty_8042</span><br><span class="line">	mov ax,#0x0001</span><br><span class="line">	mov cr0,ax</span><br><span class="line">	//cr0寄存器中PE=1启动保护模式，PG=1启动分页</span><br><span class="line">	jmpi 0,8</span><br></pre></td></tr></table></figure>

<p><img src="7.png"></p>
<p><img src="8.png"></p>
<p><img src="9.png"></p>
<h3 id="跳到system模块执行"><a href="#跳到system模块执行" class="headerlink" title="跳到system模块执行"></a>跳到system模块执行</h3><p>system模块（目标代码）中的第一部分代码：head.s</p>
<p>system由许多文件编译而成，第一个就是head.s</p>
<p><img src="s49.png"></p>
<h3 id="head-s-一段在保护模式下运行的代码"><a href="#head-s-一段在保护模式下运行的代码" class="headerlink" title="head.s//一段在保护模式下运行的代码"></a>head.s//一段在保护模式下运行的代码</h3><ul>
<li>setup是进入保护模式，head是进入之后的初始化</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">stratup_32:</span><br><span class="line">	movl $0x10,%eax</span><br><span class="line">	mov %ax,%ds</span><br><span class="line">	mov %ax,%es</span><br><span class="line">	</span><br><span class="line">	mov	%as,%fs</span><br><span class="line">	mov	%as,%gs</span><br><span class="line">	//指向gdt的0x10项(数据段)</span><br><span class="line">	</span><br><span class="line">	lss _start,%esp//设置栈(系统栈)</span><br><span class="line">	call setup_idt</span><br><span class="line">	call setup_gdt</span><br><span class="line">	//设置q和GDT</span><br><span class="line">	xorl %eax,%eax</span><br><span class="line">l:</span><br><span class="line">	incl %eax</span><br><span class="line">	movl %eax,0x000000</span><br><span class="line">	cmpl %eax,0x100000</span><br><span class="line">	je 1b	//0地址和1M地址处相同（A20没开启），就死循环</span><br><span class="line">	//检测A20开启否</span><br><span class="line">	jmp	after_page_tables//页表</span><br><span class="line">setup_idt:</span><br><span class="line">	lea ignore_int,%edx</span><br><span class="line">	movl $0x00080000,%eax</span><br><span class="line">	movw %dx,%ax</span><br><span class="line">	lea _idt,%edi</span><br><span class="line">	movl %eax,(%edi)</span><br></pre></td></tr></table></figure>

<h3 id="关于汇编…head-s的汇编和之前不一样？"><a href="#关于汇编…head-s的汇编和之前不一样？" class="headerlink" title="关于汇编…head.s的汇编和之前不一样？"></a>关于汇编…head.s的汇编和之前不一样？</h3><ol>
<li><p>as86汇编：能产生16位代码的Intel 8086汇编</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mov ax,cs	//cs-&gt;ax，目标操作数在前</span><br></pre></td></tr></table></figure></li>
<li><p>GNU as汇编：产生32位代码，使用AT&amp;T语法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">movl var,%eax	//（var）—&gt;%eax</span><br><span class="line">movb -4(%ebp),%al	//取出一字节</span><br></pre></td></tr></table></figure></li>
<li><p>内敛汇编，gcc编译x.c会产生中间结果as汇编文件x.s</p>
</li>
</ol>
<p><img src="s50.png"></p>
<p><img src="s51.png"></p>
<h3 id="after-page-tables-设置页表之后"><a href="#after-page-tables-设置页表之后" class="headerlink" title="after_page_tables    //设置页表之后"></a>after_page_tables    //设置页表之后</h3><ul>
<li><p>setup是进入保护模式，head是进入之后的初始化</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">after_page_tables:</span><br><span class="line">	pushl $0</span><br><span class="line">	pushl $0</span><br><span class="line">	pushl $0</span><br><span class="line">	pushl $L6</span><br><span class="line">	//压栈</span><br><span class="line">	pushl $_main//将_main压入栈中</span><br><span class="line">	jmp set_paging</span><br><span class="line">L6:</span><br><span class="line">	jmp	L6//main返回时死循环，死机。</span><br><span class="line">setup_paging: 设置页表 ret</span><br></pre></td></tr></table></figure>

<p>setup_paging执行ret后，会执行main.c。</p>
<p>从汇编跳到执行C函数，和函数跳转一样。</p>
</li>
</ul>
<p><img src="s52.png"></p>
<p>三个参数分别为envp,argv,argc，此处都是0。</p>
<p>此处的main只保留了传统的形式和命名。</p>
<p>main的工作就是xx_init:内存、中断、设备、时钟、CPU等内容的初始化。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    mem_init();</span><br><span class="line">    trap_init();</span><br><span class="line">    blk_dev_init();</span><br><span class="line">    chr_dev_init();</span><br><span class="line">    tty_init();</span><br><span class="line">    time_init();</span><br><span class="line">    sched_init();</span><br><span class="line">    buffer_init();</span><br><span class="line">    hd_init();</span><br><span class="line">    floppy_init();</span><br><span class="line">    sti();</span><br><span class="line">    move_to_user_mode();</span><br><span class="line">    <span class="keyword">if</span>(!fork())&#123;init();&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="看一看mem-init…可以在视图菜单中关闭"><a href="#看一看mem-init…可以在视图菜单中关闭" class="headerlink" title="看一看mem_init…可以在视图菜单中关闭"></a>看一看mem_init…可以在<code>视图</code>菜单中关闭</h3><p>执行内存的初始化。</p>
<p><img src="s53.png"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mem_init</span><span class="params">(<span class="keyword">long</span> start_mem,<span class="keyword">long</span> end_mem)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;PAGING_PAGS;i++)<span class="comment">//操作系统是使用着的</span></span><br><span class="line">        mem_map[i]=USED;</span><br><span class="line">    i=MAP_NR(start_mem);</span><br><span class="line">    end_mem-=start_mem;</span><br><span class="line">    end_mem&gt;&gt;=<span class="number">12</span>;	<span class="comment">//4k</span></span><br><span class="line">    <span class="keyword">while</span>(end_mem --&gt;<span class="number">0</span>)</span><br><span class="line">        mem_map[i++]=<span class="number">0</span>;<span class="comment">//每4k大小的区域放在一起置为0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="操作系统接口"><a href="#操作系统接口" class="headerlink" title="操作系统接口"></a>操作系统接口</h2><h3 id="接口（Interface）"><a href="#接口（Interface）" class="headerlink" title="接口（Interface）"></a>接口（Interface）</h3><ul>
<li>连接两个东西、信号转换、屏蔽细节</li>
</ul>
<h4 id="什么是操作系统接口？"><a href="#什么是操作系统接口？" class="headerlink" title="什么是操作系统接口？"></a>什么是操作系统接口？</h4><p>连接上层用户和操作系统软件。</p>
<h5 id="命令行？"><a href="#命令行？" class="headerlink" title="命令行？"></a>命令行？</h5><p>命令：一段程序，shell也是一段程序。</p>
<p><img src="10.png"></p>
<p><img src="11.png"></p>
<p><img src="12.png"></p>
<h5 id="图形按钮？"><a href="#图形按钮？" class="headerlink" title="图形按钮？"></a>图形按钮？</h5><ul>
<li>鼠标点击、键盘按下后</li>
</ul>
<p><img src="13.png"></p>
<p><img src="14.png"></p>
<p><img src="15.png"></p>
<h5 id="操作系统接口-1"><a href="#操作系统接口-1" class="headerlink" title="操作系统接口"></a>操作系统接口</h5><p>通过C语言程序，连接操作系统和应用软件。</p>
<p>接口表现为函数调用，又由系统提供，所以称为系统调用。</p>
<h3 id="系统调用"><a href="#系统调用" class="headerlink" title="系统调用"></a>系统调用</h3><ul>
<li>POSIX：Portable Operating System Interface of Unix（IEEE制定的一个标准族）</li>
</ul>
<p><img src="16.png"></p>
<h2 id="系统调用的实现"><a href="#系统调用的实现" class="headerlink" title="系统调用的实现"></a>系统调用的实现</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">main()&#123;whoami();&#125;</span><br><span class="line">whoami()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="number">100</span>,<span class="number">8</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="number">100</span>:<span class="string">&quot;lizhijun&quot;</span></span><br></pre></td></tr></table></figure>

<p>应用程序为什么不能直接jmp到操作系统中执行函数？</p>
<p>不允许，会有安全问题。</p>
<h3 id="内核（用户）态，内核（用户）段"><a href="#内核（用户）态，内核（用户）段" class="headerlink" title="内核（用户）态，内核（用户）段"></a>内核（用户）态，内核（用户）段</h3><ul>
<li><p>将内核程序和用户程序隔离</p>
<ul>
<li><p>区分内核态和用户态：通过一种处理器的硬件设计。</p>
<img src="17.png"></li>
<li><p><img src="18.png"></p>
</li>
<li><p>DPL用于描述目标内存段的特权级别（GDT中），CPL（当前的特权级，取决于执行的指令）</p>
</li>
</ul>
</li>
</ul>
<h3 id="硬件提供了“主动进入内核的唯一方法”"><a href="#硬件提供了“主动进入内核的唯一方法”" class="headerlink" title="硬件提供了“主动进入内核的唯一方法”"></a>硬件提供了“主动进入内核的唯一方法”</h3><ul>
<li>int指令将使CS中的CPL改为0，“进入内核”</li>
<li>这是用户程序发起调用内核代码的唯一方式</li>
<li>系统调用的核心：<ol>
<li>用户程序中包含一段包含int指令的代码</li>
<li>操作系统写中断处理，获取想调程序的编号</li>
<li>操作系统根据编号执行相应代码</li>
</ol>
</li>
</ul>
<h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><p><img src="19.png"></p>
<ul>
<li><p>最终展开成包含int指令的代码… </p>
<p><img src="20.png"></p>
</li>
</ul>
<h3 id="Write实现"><a href="#Write实现" class="headerlink" title="Write实现"></a>Write实现</h3><p><img src="21.png"></p>
<p>先int 0x80，调用0x80号中断，再把_NR_write(4)赋给eax，eax又赋给了__res作为返回值。最后把a赋给ebx，把b赋给ecx,把c赋给edx。</p>
<p>NR_write是系统调用号，放在eax中。</p>
<p><img src="22.png"></p>
<h3 id="int-0x80中断的处理"><a href="#int-0x80中断的处理" class="headerlink" title="int 0x80中断的处理"></a>int 0x80中断的处理</h3><p>通过IDT表来找到中断处理函数。</p>
<ul>
<li>显然，set_system_gate用来设置0x80的中断处理</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sched_init</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;	set_system_gate(<span class="number">0x80</span>,&amp;system_call)&#125;<span class="comment">//中断处理门</span></span><br><span class="line"><span class="comment">//遇到0x80中断，就跳到system_call的中断处理函数</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> set_system_gate(n,addr)	<span class="comment">//第n号中断对应处理函数入口点偏移地址addr</span></span></span><br><span class="line">_set_gate(&amp;idt[n],<span class="number">15</span>,<span class="number">3</span>,addr);<span class="comment">//idt是中断向量表基址</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> _set_gate(gate_addr, type, dpl, addr)</span></span><br><span class="line">_asm_(</span><br><span class="line">    <span class="string">&quot;movw %%dx,%%ax\n\t&quot;</span> <span class="string">&quot;movw %0,%%dx\n\t&quot;</span></span><br><span class="line">    <span class="string">&quot;movl %%eax, %1\n\t&quot;</span> <span class="string">&quot;movl %%edx, %2&quot;</span>:</span><br><span class="line">     :<span class="string">&quot;i&quot;</span>((<span class="keyword">short</span>)(<span class="number">0x8000</span>+(dpl&lt;&lt;<span class="number">13</span>)+type&lt;&lt;<span class="number">8</span>))</span><br><span class="line">	),<span class="string">&quot;o&quot;</span>(*((<span class="keyword">char</span>*)(gate_addr))),</span><br><span class="line">	<span class="string">&quot;o&quot;</span>(*(<span class="number">4</span>+(<span class="keyword">char</span>*)(gate_addr))),</span><br><span class="line">	<span class="string">&quot;d&quot;</span>((<span class="keyword">char</span>*)(addr),<span class="string">&quot;a&quot;</span>(<span class="number">0x00080000</span>))</span><br></pre></td></tr></table></figure>

<p><img src="23.png"></p>
<p>段选择符0x8000,处理函数入口点偏移&amp;system_call。</p>
<p>cs=8，ip=&amp;system_call</p>
<p>cpl是cs最后两位，cpl=00</p>
<h3 id="中断处理程序：system"><a href="#中断处理程序：system" class="headerlink" title="中断处理程序：system"></a>中断处理程序：system</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">nr_system_call=72</span><br><span class="line">.globl _system_call</span><br><span class="line">_system_call:</span><br><span class="line">	cmpl $nr_system_call-1,%eax	</span><br><span class="line">	//eax存放的是系统调用号</span><br><span class="line">	//eax=nr_write,系统调用号</span><br><span class="line">	ja bad_sys_call</span><br><span class="line">	push %ds</span><br><span class="line">	push %es</span><br><span class="line">	push %fs</span><br><span class="line">	</span><br><span class="line">	pushl %edx</span><br><span class="line">	pushl %ecx</span><br><span class="line">	pushl %ebx</span><br><span class="line">	//调用的参数</span><br><span class="line">	movl $0x10,%edx</span><br><span class="line">	mov %dx,%ds</span><br><span class="line">	mov %dx,%es</span><br><span class="line">	//内核数据</span><br><span class="line">	movl $0x17,%edx</span><br><span class="line">	mov %dx,fs</span><br><span class="line">	//fs可以找到用户数据</span><br><span class="line">	call _sys_call_table(,%eax,4)</span><br><span class="line">	//_sys_call_table+4*eax就是相应系统调用处理函数入口</span><br><span class="line">	//这里乘4是因为这个表里每一项都是一个函数地址4个字节</span><br><span class="line">	pushl %eax</span><br><span class="line">	//返回值压栈，留着ret_from_sys_call时用</span><br><span class="line">	...</span><br><span class="line">ret_from_sys_call:</span><br><span class="line">	popl %eax,其他pop,iret</span><br></pre></td></tr></table></figure>

<h3 id="sys-call-table"><a href="#sys-call-table" class="headerlink" title="_sys_call_table"></a>_sys_call_table</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">fn_ptr_call_table[]=</span><br><span class="line">&#123;</span><br><span class="line">    sys_setup,sys_exit,sys_fork,sys_read,sys_write,</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>call _sys_call_table(,%eax,4)依据上表就是call sys_write</p>
<p><img src="24.png"></p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p><img src="25.png"></p>
<h2 id="操作系统历史"><a href="#操作系统历史" class="headerlink" title="操作系统历史"></a>操作系统历史</h2><h3 id="操作系统的开端"><a href="#操作系统的开端" class="headerlink" title="操作系统的开端"></a>操作系统的开端</h3><ul>
<li><p>（1955-1965）计算机非常昂贵，上古神机IBM7094,造价250万美元以上</p>
<ul>
<li>计算机使用原则：只专注于计算</li>
<li>批处理操作系统（Batch syste）</li>
<li>典型代表：<strong>IBSYS监控系统</strong></li>
</ul>
<p><img src="26.png"></p>
</li>
<li><p>计算机开始进入多个行业：科学计算（IBM 7094）,银行（IBM1401）</p>
<ul>
<li>需要一台计算机干多个事情</li>
<li>多道程序：在计算机内存中同时存放几道相互独立的程序，使它们在管理程序控制下，相互穿插运行，两个或两个以上程序在计算机系统中同处于开始到结束之间的状态, 这些程序共享计算机系统资源。</li>
<li>作业之间的切换和调度成为核心：因为既有IO认为，又有计算任务，需要让CPU忙碌，例如：读写磁盘时不让CPU闲置，执行下一任务，等读写结束，回到上一任务。<strong>多进程结构和进程管理萌芽。</strong></li>
<li>典型代表：<strong>IBM OS/360</strong>（360待变全方位服务），开发周期5000个人年</li>
</ul>
</li>
<li><p>计算机进入多个行业，使用人数增加</p>
<ul>
<li>如果每个人启动一个作业，作业之间快速切换</li>
<li>分时系统：分时操作系统将系统<a href="https://baike.baidu.com/item/%E5%A4%84%E7%90%86%E6%9C%BA/128842">处理机</a>时间与内存空间按一定的时间间隔，轮流地切换给各终端用户的程序使用。由于时间间隔很短，每个用户的感觉就像他独占计算机一样。</li>
<li>代表：MIT MULTICS</li>
<li>核心仍然是任务切换，但是资源复用的思想对操作系统影响很大，虚拟内存就是一种复用。</li>
</ul>
</li>
<li><p>小型计算机出现，PDP-1每台售价120000美元</p>
<ul>
<li>越来越多的人可以使用计算机</li>
<li>1969年：贝尔实验室的Ken Thompson、Dennis Ritchi等在一台没人使用的PDP-7上开发了一个简化的MULTICS，就是后来的UNIX。</li>
</ul>
</li>
<li><p>1981，IBM推出IBM PC。个人计算机开始普及</p>
<ul>
<li>很多人可以用计算机并接触UNIX</li>
<li>1987年Andrew Tanenbaum发布了MINIX用于教学</li>
<li>Linus Torvalds在386sx兼容微机上学习minix，做出了小Linux于1991年发布</li>
<li>1994年，Linux1.0发布并采用GPL协议，1998年以后互联网世界开展了一场Linux产业化运动</li>
</ul>
</li>
</ul>
<h4 id="核心思想、技术："><a href="#核心思想、技术：" class="headerlink" title="核心思想、技术："></a>核心思想、技术：</h4><ol>
<li>用户通过执行程序来使用计算机（与冯诺依曼的思想吻合）</li>
<li>作为管理者，操作系统要让多个程序合理推进，就是<strong>进程管理</strong></li>
<li>多进程（用户）推进时需要内存复用</li>
<li>等等…</li>
</ol>
<p>多进程结构是操作系统基本图谱。</p>
<h3 id="PC与DOS"><a href="#PC与DOS" class="headerlink" title="PC与DOS"></a>PC与DOS</h3><ul>
<li>PC机的诞生一定会导致百花齐放。IBM推出PC，自然要给机器配一个操作系统<ul>
<li>1975年Digital Research为Altair 8800开发了操作系统CP/M</li>
<li>CP/M：写命令给用户用，执行命令对应的程序，单任务执行</li>
<li>1980出现了8086 16位芯片，在CP/M基础上开发了QDOS</li>
</ul>
</li>
<li>Bill Gates进入历史舞台<ul>
<li>1975年与Paul Allen为Altair 8800开发了BASIC解释器，开创了微软</li>
<li>1977年开发FAT管理磁盘</li>
<li>QDOS的成功在于以CP/M为基础将BASIC和FAT包含了进来<ul>
<li>文件管理和编程环境都是用户所关心的。</li>
</ul>
</li>
<li>1981年微软买下了QDOS，改名为MS-DOS，与IBM PC打包出售</li>
</ul>
</li>
</ul>
<h4 id="从MS-DOS到Windows"><a href="#从MS-DOS到Windows" class="headerlink" title="从MS-DOS到Windows"></a>从MS-DOS到Windows</h4><ul>
<li>MS-DOS的磁盘、文件、命令让用起来更方便，但可以更方便<ul>
<li>1989，MS-DOS4.0出现，支持了鼠标和键盘</li>
<li>Windows3.0大获成功</li>
<li>之后出现了95，XP，Vista，Win 7。。。</li>
<li>文件、开发环境、图形界面对于OS的重要性</li>
</ul>
</li>
</ul>
<h4 id="核心思想、技术：-1"><a href="#核心思想、技术：-1" class="headerlink" title="核心思想、技术："></a>核心思想、技术：</h4><ol>
<li>仍然是程序执行、多进程、程序带动其他设备使用的基本结构</li>
<li>开始重视用户的体验：各种文件、编程环境、图形界面</li>
</ol>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
  </entry>
  <entry>
    <title>无线DOS之取消验证洪水攻击</title>
    <url>/2021/12/07/%E6%97%A0%E7%BA%BFDOS%E4%B9%8B%E5%8F%96%E6%B6%88%E9%AA%8C%E8%AF%81%E6%B4%AA%E6%B0%B4%E6%94%BB%E5%87%BB/</url>
    <content><![CDATA[<h1 id="攻击原理"><a href="#攻击原理" class="headerlink" title="攻击原理"></a>攻击原理</h1><p>​    取消验证洪水攻击，全程为De-authenticationFloodAttack,简称为Deauth攻击。首先，攻击者通过扫描周围的WIFI网络，获取WIFI站点的SSID。然后扫描连接周围连接该网络的设备。最后，对周边网络进行抓包（嗅探），寻找要攻击的WIFI站点和连接该站点的设备，以获取的包头信息重新构造一个取消验证包，发给连接设备。设备会被欺骗，从而取消对AP的连接。</p>
<img src="/2021/12/07/%E6%97%A0%E7%BA%BFDOS%E4%B9%8B%E5%8F%96%E6%B6%88%E9%AA%8C%E8%AF%81%E6%B4%AA%E6%B0%B4%E6%94%BB%E5%87%BB/0.png" class="">

<h1 id="攻击实现"><a href="#攻击实现" class="headerlink" title="攻击实现"></a>攻击实现</h1><p>​    参考已有的张馆长的工程，将其烧录到esp8266模块上。<img src="/2021/12/07/%E6%97%A0%E7%BA%BFDOS%E4%B9%8B%E5%8F%96%E6%B6%88%E9%AA%8C%E8%AF%81%E6%B4%AA%E6%B0%B4%E6%94%BB%E5%87%BB/1.jpg" class=""></p>
<p>简单配置一下</p>
<img src="/2021/12/07/%E6%97%A0%E7%BA%BFDOS%E4%B9%8B%E5%8F%96%E6%B6%88%E9%AA%8C%E8%AF%81%E6%B4%AA%E6%B0%B4%E6%94%BB%E5%87%BB/2.jpg" class="">

<p>烧录</p>
<img src="/2021/12/07/%E6%97%A0%E7%BA%BFDOS%E4%B9%8B%E5%8F%96%E6%B6%88%E9%AA%8C%E8%AF%81%E6%B4%AA%E6%B0%B4%E6%94%BB%E5%87%BB/3.jpg" class="">

<p>这时，esp8266会建立一个WIFI，</p>
<img src="/2021/12/07/%E6%97%A0%E7%BA%BFDOS%E4%B9%8B%E5%8F%96%E6%B6%88%E9%AA%8C%E8%AF%81%E6%B4%AA%E6%B0%B4%E6%94%BB%E5%87%BB/4.jpg" class="">

<p>我们连接这个wifi，进入192.168.4.1，可以看到攻击界面，里面会显示当前的AP，我们选取一个作为攻击对象。这里我们设置的是无差别攻击。当我们要定点攻击时，需要扫描连接该WiFi的设备，选择一个攻击。</p>
<img src="/2021/12/07/%E6%97%A0%E7%BA%BFDOS%E4%B9%8B%E5%8F%96%E6%B6%88%E9%AA%8C%E8%AF%81%E6%B4%AA%E6%B0%B4%E6%94%BB%E5%87%BB/5.jpg" class="">

<img src="/2021/12/07/%E6%97%A0%E7%BA%BFDOS%E4%B9%8B%E5%8F%96%E6%B6%88%E9%AA%8C%E8%AF%81%E6%B4%AA%E6%B0%B4%E6%94%BB%E5%87%BB/6.jpg" class="">

<p>​        选择Deauth攻击，然后我们就可以观察到连接该网的设备断连的现象，手机上方的WiFi图标会不断闪烁。</p>
<h1 id="代码架构"><a href="#代码架构" class="headerlink" title="代码架构"></a>代码架构</h1><p>首先，WIFI模块有两种工作模式：</p>
<p>ap是无线发射端，station是接收端，两种模式组建成一个无线局域网。</p>
<ol>
<li>ap mode 通常应用在无线局域网成员设备（即客户端）的加入，即<strong>网络下行</strong>。它提供以无线方式组建无线局域网 WLAN，相当际 <strong>WLAN 的中心设备</strong>。AP和AP之间允许相互连接。</li>
<li>station mode即工作站模式，可以理解为某个网格中的一个工作站即<strong>客户端</strong>，<strong>网络上行</strong>。那当一个 WIFI 芯片提供这个功能时，它就可以连到另外的一个网络当中，如家用路由器。通常用于提供网络的数据上行服务。</li>
</ol>
<p>代码的核心是扫描、嗅探和伪造。扫描站点，嗅探（抓包）和通过。</p>
<p>首先是扫描：</p>
<p>​    AP模式扫描周围的AP。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#scan.cpp</span></span><br><span class="line">	<span class="comment">/* AP Scan */</span></span><br><span class="line">    <span class="keyword">if</span> ((mode == SCAN_MODE_APS) || (mode == SCAN_MODE_ALL)) &#123;</span><br><span class="line">        <span class="comment">// remove old results</span></span><br><span class="line">        accesspoints.removeAll();</span><br><span class="line">        stations.removeAll();</span><br><span class="line">        <span class="comment">// start AP scan</span></span><br><span class="line">        prntln(SC_START_AP);</span><br><span class="line">        WiFi.scanNetworks(<span class="literal">true</span>, <span class="literal">true</span>);</span><br><span class="line">        <span class="comment">/*该函数可以扫描到ESP8266开发板所在环境中的可用WIFI网络，并且将WiFi网络信息保存到内存中。通过调用SSID、RSSI等函数，我们还可以得到这些扫描到的WIFI的更多信息。*/</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>​    station模式开启后可以开启混杂模式（混杂模式（英语：promiscuous mode）是电脑网络中的术语。是指一台机器的网卡能够接收所有经过它的数据流，而不论其目的地址是否是它。），从而为嗅探作准备</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#scan.cpp</span></span><br><span class="line"><span class="comment">/* Station Scan */</span></span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (mode == SCAN_MODE_STATIONS) &#123;</span><br><span class="line">        <span class="comment">// start station scan</span></span><br><span class="line">        <span class="keyword">if</span> (accesspoints.count() &lt; <span class="number">1</span>) &#123;</span><br><span class="line">            start(SCAN_MODE_ALL);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;<span class="comment">//如果当前网络没有AP,返回重新扫描，因为我们需要先确定攻击的AP再确定攻击的设备，如果在嗅探前AP已经不存在了，则没有继续的必要</span></span><br><span class="line">        snifferStartTime = currentTime;</span><br><span class="line">        prnt(SC_START_CLIENT);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (sniffTime &gt; <span class="number">0</span>) prnt(String(sniffTime / <span class="number">1000</span>) + S);</span><br><span class="line">        <span class="keyword">else</span> prnt(SC_INFINITELY);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!channelHop) &#123;</span><br><span class="line">            prnt(SC_ON_CHANNEL);</span><br><span class="line">            prnt(wifi_channel);</span><br><span class="line">        &#125;</span><br><span class="line">        prntln();</span><br><span class="line">        wifi::stopAP();</span><br><span class="line">        wifi_promiscuous_enable(<span class="literal">true</span>);<span class="comment">//开启混杂模式</span></span><br><span class="line">    	<span class="comment">/*混杂模式就是接收所有经过网卡的数据包，包括不是发给本机的包。默认情况下网卡只把发给本机的包（包括广播包）传递给上层程序，其它的包一律丢弃。但是混杂模式就是指网卡能接受所有通过它的数据流，不管是什么格式，什么地址的。事实上，计算机收到数据包后，由网络层进行判断（这里是802.11系协议），确定是递交上层（传输层），还是丢弃，还是递交下层（数据链路层、MAC子层）转发。*/</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>进入嗅探模式：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Scan::sniffer</span><span class="params">(<span class="keyword">uint8_t</span>* buf, <span class="keyword">uint16_t</span> len)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!isSniffing()) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    packets++;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (len &lt; <span class="number">28</span>) <span class="keyword">return</span>;  <span class="comment">// 丢弃掉过短的数据包</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((buf[<span class="number">12</span>] == <span class="number">0xc0</span>) || (buf[<span class="number">12</span>] == <span class="number">0xa0</span>)) &#123;</span><br><span class="line">        tmpDeauths++;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 丢弃信标帧、探测请求/响应和解除授权/解除关联帧</span></span><br><span class="line">    <span class="keyword">if</span> ((buf[<span class="number">12</span>] == <span class="number">0x80</span>) || (buf[<span class="number">12</span>] == <span class="number">0x40</span>) || (buf[<span class="number">12</span>] == <span class="number">0x50</span>) <span class="comment">/* || buf[12] == 0xc0 || buf[12] == 0xa0*/</span>) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 只允许数据帧</span></span><br><span class="line">    <span class="comment">// if(buf[12] != 0x08 &amp;&amp; buf[12] != 0x88) return;数据帧12位为08和88的时候说明是数据帧，如果都不是，可以丢弃，但作者将这句话注释了，不知道原因。。。</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">uint8_t</span>* macTo   = &amp;buf[<span class="number">16</span>];<span class="comment">//该数据包要去哪</span></span><br><span class="line">    <span class="keyword">uint8_t</span>* macFrom = &amp;buf[<span class="number">22</span>];<span class="comment">//该数据包从哪来</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (macBroadcast(macTo) || macBroadcast(macFrom) || !macValid(macTo) || !macValid(macFrom) || macMulticast(macTo) ||</span><br><span class="line">        macMulticast(macFrom)) <span class="keyword">return</span>;</span><br><span class="line">	<span class="comment">//Broadcast所谓广播地址指同时向网上所有的主机发送报文，也就是说，不管物理网络特性如何，internet网都支持广播传输。这里表示如果收发地址为广播地址，则丢弃。</span></span><br><span class="line">    <span class="comment">//Multicast是一个群组传播模式。。。其他的看不懂了，和上面区别不大。也丢弃。</span></span><br><span class="line">    <span class="comment">//上面两种的特点都是一对多模式，而我们只需要要截获一对一的数据包，并认为该mac为有效mac地址。</span></span><br><span class="line">    <span class="keyword">int</span> accesspointNum = findAccesspoint(macFrom);<span class="comment">//查看该mac地址有几个AP。这个如果为0说明该mac地址是连接AP的设备</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (accesspointNum &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        stations.add(macTo, accesspoints.getID(accesspointNum));</span><br><span class="line">        <span class="comment">//macForm为AP了，那么macTo肯定为设备，我们将该设备的mac加入列表。</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;  <span class="comment">//如果AP数量为0，那么macTo是AP，macForm为设备，</span></span><br><span class="line">        accesspointNum = findAccesspoint(macTo);</span><br><span class="line">        <span class="comment">//这两个if判断谁是AP谁是设备，写的很垃圾</span></span><br><span class="line">        <span class="keyword">if</span> (accesspointNum &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">            stations.add(macFrom, accesspoints.getID(accesspointNum));</span><br><span class="line">            <span class="comment">//我们将该设备的mac加入列表。</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="802-11协议"><a href="#802-11协议" class="headerlink" title="802.11协议"></a>802.11协议</h2><img src="/2021/12/07/%E6%97%A0%E7%BA%BFDOS%E4%B9%8B%E5%8F%96%E6%B6%88%E9%AA%8C%E8%AF%81%E6%B4%AA%E6%B0%B4%E6%94%BB%E5%87%BB/7.png" class="">

<p>以上是我们需要构造的mac帧头格式</p>
<img src="/2021/12/07/%E6%97%A0%E7%BA%BFDOS%E4%B9%8B%E5%8F%96%E6%B6%88%E9%AA%8C%E8%AF%81%E6%B4%AA%E6%B0%B4%E6%94%BB%E5%87%BB/8.png" class="">

<p>准确的说，我们需要向设备发送取消验证包。地址1应该是DA（设备mac），地址2是BSSID（伪造成APmac），地址3源地址（还是APmac）</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#attack.cpp</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">Attack::deauthDevice</span><span class="params">(<span class="keyword">uint8_t</span>* apMac, <span class="keyword">uint8_t</span>* stMac, <span class="keyword">uint8_t</span> reason, <span class="keyword">uint8_t</span> ch)</span> </span>&#123;</span><br><span class="line">	······</span><br><span class="line">    <span class="comment">// build deauth packet</span></span><br><span class="line">    packetSize = <span class="keyword">sizeof</span>(deauthPacket);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">uint8_t</span> deauthpkt[packetSize];</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memcpy</span>(deauthpkt, deauthPacket, packetSize);<span class="comment">//构造mac帧头，初始数据见下</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">memcpy</span>(&amp;deauthpkt[<span class="number">4</span>], stMac, <span class="number">6</span>);</span><br><span class="line">    <span class="built_in">memcpy</span>(&amp;deauthpkt[<span class="number">10</span>], apMac, <span class="number">6</span>);</span><br><span class="line">    <span class="built_in">memcpy</span>(&amp;deauthpkt[<span class="number">16</span>], apMac, <span class="number">6</span>);</span><br><span class="line">    deauthpkt[<span class="number">24</span>] = reason;</span><br><span class="line">    ······</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#attack.h</span></span><br><span class="line"><span class="keyword">uint8_t</span> deauthPacket[<span class="number">26</span>] = &#123;</span><br><span class="line">            <span class="comment">/*  0 - 1  */</span> <span class="number">0xC0</span>, <span class="number">0x00</span>,</span><br><span class="line">            <span class="comment">/*  2 - 3  */</span> <span class="number">0x00</span>, <span class="number">0x00</span>, </span><br><span class="line">            <span class="comment">/*  4 - 9  */</span> <span class="number">0xFF</span>, <span class="number">0xFF</span>, <span class="number">0xFF</span>, <span class="number">0xFF</span>, <span class="number">0xFF</span>, <span class="number">0xFF</span>,</span><br><span class="line">            <span class="comment">/* 10 - 15 */</span> <span class="number">0xCC</span>, <span class="number">0xCC</span>, <span class="number">0xCC</span>, <span class="number">0xCC</span>, <span class="number">0xCC</span>, <span class="number">0xCC</span>,</span><br><span class="line">            <span class="comment">/* 16 - 21 */</span> <span class="number">0xCC</span>, <span class="number">0xCC</span>, <span class="number">0xCC</span>, <span class="number">0xCC</span>, <span class="number">0xCC</span>, <span class="number">0xCC</span>,</span><br><span class="line">            <span class="comment">/* 22 - 23 */</span> <span class="number">0x00</span>, <span class="number">0x00</span>,                         </span><br><span class="line">            <span class="comment">/* 24 - 25 */</span> <span class="number">0x01</span>, <span class="number">0x00</span>                         </span><br><span class="line">        &#125;;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>软件安全</category>
      </categories>
  </entry>
  <entry>
    <title>深入理解计算机系统一</title>
    <url>/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/</url>
    <content><![CDATA[<h4 id="位："><a href="#位：" class="headerlink" title="位："></a>位：</h4><p>​        计算机中数据储存的基本单位，源程序实际上就是由0和1组成的位序列，8位等于一个字节，系统中的所有信息都是由一串比特位来表示的。</p>
<h4 id="上下文："><a href="#上下文：" class="headerlink" title="上下文："></a>上下文：</h4><p>​        系统中的信息表达方式都十分类似，所以难以区分。帮助我们区分信息类型的方法就是观察信息所在的上下文（类似语境）不同上下文中的相同含义也会不同，计算机的理解方式也不同。</p>
<span id="more"></span>

<h3 id="1-2-程序被其他程序翻译成不同的格式"><a href="#1-2-程序被其他程序翻译成不同的格式" class="headerlink" title="1.2 程序被其他程序翻译成不同的格式"></a>1.2 程序被其他程序翻译成不同的格式</h3><p>​    源程序文件变成可执行文件需要经历四个阶段：预处理阶段 编译阶段 汇编阶段 链接阶段。</p>
<h4 id="1：预处理阶段"><a href="#1：预处理阶段" class="headerlink" title="1：预处理阶段"></a>1：预处理阶段</h4><p>​        预处理器根据以字符# 开头的命令，修改原始的C程序。</p>
<p>​        程序扩展名由 **.c ** -&gt; <strong>.i</strong>。</p>
<h4 id="2：编译阶段"><a href="#2：编译阶段" class="headerlink" title="2：编译阶段"></a>2：编译阶段</h4><p>​        汇编语言为不同语言的不同编译器提供了通用的输出语言。</p>
<p>​        程序扩展名由 <strong>.i</strong> -&gt; <strong>.s</strong></p>
<h4 id="3：汇编阶段"><a href="#3：汇编阶段" class="headerlink" title="3：汇编阶段"></a>3：汇编阶段</h4><p>​        汇编器将.s翻译成机器语言指令，并把这些指令打包成一种可重定位目标程序，将结果保存在.o中。</p>
<p>​        程序扩展名由 <strong>.s</strong> -&gt; <strong>.o</strong></p>
<h4 id="4：链接阶段"><a href="#4：链接阶段" class="headerlink" title="4：链接阶段"></a>4：链接阶段</h4><p>​        目标文件中出现的函数（例如：printf函数ut）它们都存在于一个单独编译好的文件中，链接器就负责将函数所在文件与目标文件合并在一起，处理之后便得到一个可执行目标文件。</p>
<p>​        程序扩展名由 <strong>.o</strong> -&gt; 可执行文件</p>
<h3 id="1-3-了解编译系统工作的益处"><a href="#1-3-了解编译系统工作的益处" class="headerlink" title="1.3 了解编译系统工作的益处"></a>1.3 了解编译系统工作的益处</h3><ol>
<li>优化程序性能，提高程序运行速度</li>
<li>理解链接时出现的错误，便于及时解决问题，节省时间</li>
<li>避免安全漏洞</li>
</ol>
<h3 id="1-4-处理器读并解释存储在内存中的指令"><a href="#1-4-处理器读并解释存储在内存中的指令" class="headerlink" title="1.4 处理器读并解释存储在内存中的指令"></a>1.4 处理器读并解释存储在内存中的指令</h3><h4 id="1-总线"><a href="#1-总线" class="headerlink" title="1. 总线"></a>1. 总线</h4><p>​        贯穿整个系统的一组电子管道（总线通常被设计成传送定长的字节块）</p>
<h4 id="2-I-O设备"><a href="#2-I-O设备" class="headerlink" title="2. I/O设备"></a>2. I/O设备</h4><p>​        系统与外界世界的联系通道（每个I/O设备都通过一个适配器或控制器与I/O总线相连）</p>
<h4 id="3-主存"><a href="#3-主存" class="headerlink" title="3. 主存"></a>3. 主存</h4><p>​        临时存储设备，由一组动态随机存取存储器芯片组成</p>
<h4 id="4-处理器"><a href="#4-处理器" class="headerlink" title="4. 处理器"></a>4. 处理器</h4><p>​        中央处理单元，简称处理器。是解释（或执行）存储在内存中的指令的引擎。处理器的核心是一个大小为一个字的存储设备（或寄存器），称为程序计数器（PC）。在任何时刻，PC都指向内存中的某条机器指令。</p>
<h3 id="1-5-高速缓存至关重要"><a href="#1-5-高速缓存至关重要" class="headerlink" title="1.5 高速缓存至关重要"></a>1.5 高速缓存至关重要</h3><p>​    一般处理器从寄存器文件中读数据比从主存中读数据几乎快100倍。针对这种处理器与主存之间的差异，我们采用告诉缓存作为暂时的集结区域，可以将常用的一些数据存放在告诉缓存中，使程序的性能提高一个数量级。</p>
<h3 id="1-6-存储设备形成层次结构"><a href="#1-6-存储设备形成层次结构" class="headerlink" title="1.6 存储设备形成层次结构"></a>1.6 存储设备形成层次结构</h3><img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/1.png" class="">


<h3 id="1-7-操作系统管理硬件"><a href="#1-7-操作系统管理硬件" class="headerlink" title="1.7 操作系统管理硬件"></a>1.7 操作系统管理硬件</h3><h4 id="1-操作系统的两个基本功能："><a href="#1-操作系统的两个基本功能：" class="headerlink" title="1. 操作系统的两个基本功能："></a>1. 操作系统的两个基本功能：</h4><ol>
<li>防止硬件被失控的应用程序滥用。</li>
<li>向应用程序提供简单一致的机制来控制复杂而又通常大不相同的低级硬件设备。</li>
</ol>
<h4 id="2-操作系统的几个抽象概念："><a href="#2-操作系统的几个抽象概念：" class="headerlink" title="2. 操作系统的几个抽象概念："></a>2. 操作系统的几个抽象概念：</h4><ol>
<li><h5 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h5><p>​    进程是操作系统对一个正在运行的程序的抽象，在一个系统上可以同时运送多个进程，看上去每个进程都在独立地占用硬件，但实际上每个进程的指令是交错执行的。</p>
<p>​    操作系统保持跟踪进程所需的所有状态信息，也就是上下文。在任何一个时刻，切换进程都要通过转换上下文来实现。</p>
<p>​    进程之间的转换是通过操作系统内核进行管理的，内核是操作系统代码常驻主存的部分。当应用程序需要操作系统的某些操作时，它就执行一条特殊的系统调用指令，将控制权交给内核。</p>
<p>​    内核不是一个独立的进程，而是系统管理全部进程所用代码和数据结构的集合。</p>
<p>​    一个进程实际上可以由多个线程执行单元组成，每个线程都运行在进程的上下文中，并共享同样的代码和全局数据。采用多线程的方法可以使程序运行更加高效。</p>
</li>
<li><h5 id="虚拟内存"><a href="#虚拟内存" class="headerlink" title="虚拟内存"></a>虚拟内存</h5><p>​    虚拟内存不同于物理内存，它看上去好像每个进程都在独立地使用内存，每个进程看到的内存都是一致的，称为虚拟地址空间。</p>
<h6 id="分区："><a href="#分区：" class="headerlink" title="分区："></a>分区：</h6><p>​        程序代码和数据。</p>
<p>​        堆：当使用malloc和free函数时可以用于动态的分配内存空间。</p>
<p>​        共享库：用于存放C标准库和数学库这样的共享库的一片区域。</p>
<p>​        栈：也是一片可以动态扩展的区域。</p>
<p>​        内核虚拟空间</p>
</li>
<li><h5 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h5><p>​    文件是字节序列，每个输入输出设备都可以看成文件。他为应用程序提供了一个统一的视图，来看待系统中可能含有的所有各式的输入输出设备。</p>
</li>
</ol>
<h3 id="1-8-系统之间利用网络通信"><a href="#1-8-系统之间利用网络通信" class="headerlink" title="1.8 系统之间利用网络通信"></a>1.8 系统之间利用网络通信</h3><p>​    系统不是一个孤立的硬件和软件的集合体，世界上现代系统通常通过网络和其他系统连接到一起。网络可以看成一个I/O设备，可以将信息从一台设备复制到另一台设备。</p>
<h3 id="1-9-并发和并行"><a href="#1-9-并发和并行" class="headerlink" title="1.9 并发和并行"></a>1.9 并发和并行</h3><p>​    顺序：上一个开始执行的任务完成后，当前任务才能开始执行并发：无论上一个开始执行的任务是否完成，当前任务都可以开始执行</p>
<p>​    并发：无论上一个开始执行的任务是否完成，当前任务都可以开始执行</p>
<p>​    串行：有一个任务执行单元，从物理上就只能一个任务、一个任务地执行</p>
<p>​    并行：有多个任务执行单元，从物理上就可以多个任务一起执行</p>
<h3 id="1-10-计算机系统中的抽象"><a href="#1-10-计算机系统中的抽象" class="headerlink" title="1.10 计算机系统中的抽象"></a>1.10 计算机系统中的抽象</h3><ol>
<li>文件是对I/O设备的抽象</li>
<li>虚拟内存是对出现处理器的抽象</li>
<li>进程是对一个正在运行的程序的抽象</li>
<li>虚拟机是对整个计算机的抽象</li>
</ol>
<h2 id="第二章：信息的表示和处理"><a href="#第二章：信息的表示和处理" class="headerlink" title="第二章：信息的表示和处理"></a>第二章：信息的表示和处理</h2><h3 id="2-1-消息存储"><a href="#2-1-消息存储" class="headerlink" title="2.1 消息存储"></a>2.1 消息存储</h3><h4 id="2-1-1-十六进制"><a href="#2-1-1-十六进制" class="headerlink" title="2.1.1 十六进制"></a>2.1.1 十六进制</h4><p>​        一个字节由8位组成。二进制有32个数，十进制转化为二进制很麻烦。所有用十六进制表示。一个十六进制数可以表示4个二进制数。</p>
<h4 id="2-1-2-寻址和字节顺序"><a href="#2-1-2-寻址和字节顺序" class="headerlink" title="2.1.2 寻址和字节顺序"></a>2.1.2 寻址和字节顺序</h4><p>​        多字节对线都被存储为连续的字节序列，对象的地址为所用的字节中最小的地址。</p>
<p>​        大端法：最高有效字节存储在低地址的方法。</p>
<p>​        小端法：最高有效字节存储在高地址的方法。</p>
<h3 id="2-2-整数表示"><a href="#2-2-整数表示" class="headerlink" title="2.2 整数表示"></a>2.2 整数表示</h3><h4 id="2-2-1-整数的分类"><a href="#2-2-1-整数的分类" class="headerlink" title="2.2.1 整数的分类"></a>2.2.1 整数的分类</h4><p>​        有符号数和无符号数。</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/2.png" class="">


<h4 id="2-2-2-C语言中的转换"><a href="#2-2-2-C语言中的转换" class="headerlink" title="2.2.2 C语言中的转换"></a>2.2.2 C语言中的转换</h4><p>​        强制类型转换时，数的位表示不变。</p>
<p>​        C语言执行一个运算时，一个无符号数和有符号数执行运算，会将有符号数强制转换成无符号数。</p>
<h4 id="2-2-3-扩展一个数字的位表示"><a href="#2-2-3-扩展一个数字的位表示" class="headerlink" title="2.2.3 扩展一个数字的位表示"></a>2.2.3 扩展一个数字的位表示</h4><p>​        无符号数的零扩展：前补0。</p>
<p>​        补码数的符号扩展：前补符号位。</p>
<p>​        截断无符号数：一个数x，将其截断为k位的结果为：x‘= x mod 2^k。</p>
<p>​        截断补码数值：先按无符号数截断，再转为补码形式。</p>
<h3 id="2-3-整数加法"><a href="#2-3-整数加法" class="headerlink" title="2.3 整数加法"></a>2.3 整数加法</h3><h4 id="2-3-1-无符号数加法"><a href="#2-3-1-无符号数加法" class="headerlink" title="2.3.1 无符号数加法"></a>2.3.1 无符号数加法</h4><p>​        二进制加法，会溢出。</p>
<p>​        加法逆元原理：无符号数求反。</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/3.png" class="">


<h4 id="2-3-2-补码加法"><a href="#2-3-2-补码加法" class="headerlink" title="2.3.2 补码加法"></a>2.3.2 补码加法</h4><p>​        存在正溢出和负溢出。        </p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/4.png" class="">


<p>​        对TMin来说，自己是自己加法的逆，其他任何数都有-x作为加法的逆。</p>
<p>​        对任何数x，都有-x=~x+1。</p>
<h3 id="2-4-浮点数"><a href="#2-4-浮点数" class="headerlink" title="2.4 浮点数"></a>2.4 浮点数</h3><h4 id="2-4-1-IEEE浮点表示"><a href="#2-4-1-IEEE浮点表示" class="headerlink" title="2.4.1 IEEE浮点表示"></a>2.4.1 IEEE浮点表示</h4><p>​        公式：V=(-1)^s * M * 2^E</p>
<p>​        s符号</p>
<p>​        M尾数是一个二进制小数</p>
<p>​        E阶码对浮点数加权，权重是2的E次幂</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/5.png" class="">


<p>​        单精度浮点数（float）是1位s，8位阶码，23位尾码。</p>
<p>​        双精度浮点数（double）是1位s，11位阶码，52位尾码。</p>
<h5 id="规范化的值："><a href="#规范化的值：" class="headerlink" title="规范化的值："></a>规范化的值：</h5><p>​                E=e-Bias。Bias是对n位E来说是2^（n-1）-1。float的Bias是127，double是1023。</p>
<p>​                当二进制小数点再最高有效位的左边是，会补上一个1，这也叫做隐含的以1开头表示。</p>
<h5 id="非规格化数的值："><a href="#非规格化数的值：" class="headerlink" title="非规格化数的值："></a>非规格化数的值：</h5><p>​                当阶码域全0时，所表示的数是非规格化形式，这是E=1-Bias，M=f是小数字段的值，不包含隐含的1。</p>
<p>​                非规格化数表示的是那些非常接近于0的数。</p>
<h5 id="特殊值："><a href="#特殊值：" class="headerlink" title="特殊值："></a>特殊值：</h5><h5 id="无穷和NaN。"><a href="#无穷和NaN。" class="headerlink" title="无穷和NaN。"></a>无穷和NaN。</h5><img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/6.png" class="">


<p>​            非规格化数和规格化数的平滑转变，归功于非规格化数的E=1-Bias。</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/7.png" class="">


<h4 id="2-4-2-舍入"><a href="#2-4-2-舍入" class="headerlink" title="2.4.2 舍入"></a>2.4.2 舍入</h4><p>​        <strong>四舍六入五向偶。</strong></p>
<h2 id="第三章：程序的机器级表示"><a href="#第三章：程序的机器级表示" class="headerlink" title="第三章：程序的机器级表示"></a>第三章：程序的机器级表示</h2><h3 id="3-1-数据格式"><a href="#3-1-数据格式" class="headerlink" title="3.1 数据格式"></a>3.1 数据格式</h3><p>​    Inter称呼16位数字类型为“字（word）”32位是双字，64位是四字。</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/8.png" class="">


<h3 id="3-2-访问信息"><a href="#3-2-访问信息" class="headerlink" title="3.2 访问信息"></a>3.2 访问信息</h3><p>​    一个x86-64的中央处理单元（CPU）包含一组16个存储64位值的通用目的寄存器，用来存储数据类型和指针。</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/9.png" class="">


<h4 id="3-2-1-操作数指示符"><a href="#3-2-1-操作数指示符" class="headerlink" title="3.2.1 操作数指示符"></a>3.2.1 操作数指示符</h4><p>​        操作数分为：</p>
<ol>
<li>立即数——表示常数值</li>
<li>寄存器——表示某个寄存器的内容</li>
<li>内存引用——它会根据计算出来的地址访问某个内存位置。</li>
</ol>
<p>内存引用寻址有很多方式：</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/10.png" class="">


<h4 id="3-2-2-数据传送指令"><a href="#3-2-2-数据传送指令" class="headerlink" title="3.2.2 数据传送指令"></a>3.2.2 数据传送指令</h4><p>​    MOV类：</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/11.png" class="">


<p>​        MOV的五种可能组合：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">movl $0x4050,%eax		立即数-&gt;寄存器	4 bytes</span><br><span class="line">movw %bp,%sp			寄存器-&gt;寄存器	2 bytes</span><br><span class="line">movb (%rdi,%rcx),%al	内存-&gt;寄存器		 1 bytes</span><br><span class="line">movb $17,(%rsp)			立即数-&gt;内存		 1 bytes</span><br><span class="line">movq %rax，-12(%rbp)		寄存器-&gt;内存	  `	8 bytes</span><br></pre></td></tr></table></figure>

<p>​        零扩展和符号扩展：</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/12.png" class="">




<h4 id="3-2-3-压入和弹出栈数据"><a href="#3-2-3-压入和弹出栈数据" class="headerlink" title="3.2.3 压入和弹出栈数据"></a>3.2.3 压入和弹出栈数据</h4><p>​    栈：后进先出，通过push将数据压入栈中，通过pop将数据弹出。</p>
<p>​    栈向下增长，栈顶元素的地址是所有栈元素地址中最低的。</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/13.png" class="">


<h3 id="3-3-算术和逻辑操作"><a href="#3-3-算术和逻辑操作" class="headerlink" title="3.3 算术和逻辑操作"></a>3.3 算术和逻辑操作</h3><img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/14.png" class="">


<h4 id="3-3-1-加载有效地址"><a href="#3-3-1-加载有效地址" class="headerlink" title="3.3.1 加载有效地址"></a>3.3.1 加载有效地址</h4><p>​        lea：加载有效地址。</p>
<p>​                    即将有效地址写入目的操作数。</p>
<p>​        可以为内存引入指针。</p>
<p>​        mov和lea:</p>
<p>​            lea eax,[eax+2*eax]的效果是eax = eax + eax * 2</p>
<p>​            mov edx [ebp+16]的效果是edx=*(ebp+16)</p>
<h4 id="3-3-2-一元和二元操作"><a href="#3-3-2-一元和二元操作" class="headerlink" title="3.3.2 一元和二元操作"></a>3.3.2 一元和二元操作</h4><p>​    第二组中的操作是一元操作，只有一个操作数，既是源操作数又是目的操作数，既可以是寄存器也可以是内存位置。</p>
<p>​    第三组中的操作是二元操作，第二个操作数既是源操作数又是目的操作数。第一个操作数可以是立即数也可以是寄存器和内存，第二个操作数可以是寄存器也可以是内存位置。</p>
<h4 id="3-3-3-移位操作"><a href="#3-3-3-移位操作" class="headerlink" title="3.3.3 移位操作"></a>3.3.3 移位操作</h4><p>​    逻辑左移和算术左移，没有区别，都是右边补0。</p>
<p>​    逻辑右移：左边补0。</p>
<p>​    算术右移：左边补符号位。</p>
<h4 id="3-3-4-特殊的算术操作"><a href="#3-3-4-特殊的算术操作" class="headerlink" title="3.3.4 特殊的算术操作"></a>3.3.4 特殊的算术操作</h4><img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/15.png" class="">


<h3 id="3-4-控制"><a href="#3-4-控制" class="headerlink" title="3.4 控制"></a>3.4 控制</h3><h4 id="3-4-1-条件码"><a href="#3-4-1-条件码" class="headerlink" title="3.4.1 条件码"></a>3.4.1 条件码</h4><p>​    除了整数寄存器，CPU还维护者一组单个位的条件码寄存器</p>
<p>​    他们描述了最近的逻辑或算术操作的属性</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/16.png" class="">


<h4 id="3-4-2-SET指令"><a href="#3-4-2-SET指令" class="headerlink" title="3.4.2 SET指令"></a>3.4.2 SET指令</h4><img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/17.png" class="">


<h4 id="3-4-3-跳转指令"><a href="#3-4-3-跳转指令" class="headerlink" title="3.4.3 跳转指令"></a>3.4.3 跳转指令</h4><img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/18.png" class="">


<h4 id="3-4-4-实现条件分支"><a href="#3-4-4-实现条件分支" class="headerlink" title="3.4.4 实现条件分支"></a>3.4.4 实现条件分支</h4><ul>
<li><p>条件控制</p>
<p>当条件满足时，程序按照一种方式进行，不满足时切换。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">diff</span><span class="params">(<span class="keyword">int</span> a,<span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(a&lt;b)</span><br><span class="line">        <span class="keyword">return</span> b-a;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> a-b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>条件传送</p>
<p>同时计算程序的两种结果，再根据程序是否满足选择其中的一种。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">diff</span><span class="params">(<span class="keyword">int</span> a,<span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> x1=a-b;</span><br><span class="line">    <span class="keyword">int</span> x2=b-a;</span><br><span class="line">    <span class="keyword">if</span>(a&lt;b)</span><br><span class="line">        <span class="keyword">return</span> x2;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> x1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="3-4-5-循环"><a href="#3-4-5-循环" class="headerlink" title="3.4.5 循环"></a>3.4.5 循环</h4><p>C语言代码</p>
<p>do{}while()语句</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">long</span> <span class="title">fact_do</span><span class="params">(<span class="keyword">long</span> n)</span></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> result = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        result *= n;</span><br><span class="line">        n = n - <span class="number">1</span>;</span><br><span class="line">    &#125; <span class="keyword">while</span> (n &gt; <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其对应的汇编代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fact_do:</span><br><span class="line">	movl	$1,%eax</span><br><span class="line">.L2:</span><br><span class="line">	imulq	%rdi,%rax</span><br><span class="line">	subq	$1,%rdi</span><br><span class="line">	cmpq	$1,%rdi</span><br><span class="line">	jg		.L2</span><br><span class="line">	rep:ret</span><br></pre></td></tr></table></figure>

<p>C语言代码while（）语句</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">long</span> <span class="title">fact_while</span><span class="params">(<span class="keyword">long</span> n)</span></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> result = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span> (n &gt; <span class="number">1</span>)&#123;</span><br><span class="line">        result *= n;</span><br><span class="line">        n = n - <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对应的汇编代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fact_while:</span><br><span class="line">	movl	$1,%eax</span><br><span class="line">	jmp		,L5</span><br><span class="line">.L6:</span><br><span class="line">	imulq	%rdi,%rax</span><br><span class="line">	subq	$1,%rdi</span><br><span class="line">.L5:</span><br><span class="line">	cmp		$1,%rdi</span><br><span class="line">	jg		.L6</span><br><span class="line">	rep:ret</span><br></pre></td></tr></table></figure>

<h4 id="3-4-6-switch语句"><a href="#3-4-6-switch语句" class="headerlink" title="3.4.6 switch语句"></a>3.4.6 switch语句</h4><p>C语言描述</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">switch</span><span class="params">(<span class="keyword">long</span> x,<span class="keyword">long</span> n,<span class="keyword">long</span>* dest)</span></span>&#123;</span><br><span class="line">        <span class="keyword">long</span> val = x;</span><br><span class="line">        <span class="keyword">switch</span>(n)&#123;</span><br><span class="line">            <span class="keyword">case</span> <span class="number">100</span>:</span><br><span class="line">            	val *= <span class="number">13</span>;</span><br><span class="line">            	<span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> <span class="number">102</span>:</span><br><span class="line">            	val += <span class="number">10</span>;</span><br><span class="line">            <span class="keyword">case</span> <span class="number">103</span>:</span><br><span class="line">            	val += <span class="number">11</span>;</span><br><span class="line">            <span class="keyword">case</span> <span class="number">104</span>:</span><br><span class="line">            <span class="keyword">case</span> <span class="number">106</span>：</span><br><span class="line">                val *= val;</span><br><span class="line">            	<span class="keyword">break</span>;</span><br><span class="line">           	dafault:</span><br><span class="line">            	val = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        *dest = val;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>汇编语言描述</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#void switch(long x,long n,long* dest)</span><br><span class="line">#x in %rdi	y in %rsi	dest in %rdx</span><br><span class="line">switch:</span><br><span class="line">	subq	$100,%rsi	#index = n-100</span><br><span class="line">	cmpq	$6	,%rsi	#compare index:6</span><br><span class="line">	ja		.L8			#if &gt; ，goto loc_def</span><br><span class="line">	jmp		*.L4(,%rsi,8)	#跳转到&amp;.L4+%rsi*8（一个指针占8个字节）</span><br><span class="line">.L3:</span><br><span class="line">	leaq	(%rdi,%rdi,2),%rax	#	3*x</span><br><span class="line">	leaq	(%rdi,%rax,4),%rdi	#	13*x</span><br><span class="line">	jmp		.L2</span><br><span class="line">.L5:</span><br><span class="line">	addq	$10	,%rdi	#val=val+10</span><br><span class="line">.L6:</span><br><span class="line">	addq	$11	,%rdi	#val=val+11</span><br><span class="line">	jmp		.L2</span><br><span class="line">.L7:</span><br><span class="line">	imulq	%rdi,%rdi	#val=val*val</span><br><span class="line">	jmp		,L2</span><br><span class="line">.L8:</span><br><span class="line">	movl	$0	,%edi	#val=0</span><br><span class="line">.L2:</span><br><span class="line">	movq	%rdi,(%rdx)	#*dest=val</span><br><span class="line">	ret</span><br></pre></td></tr></table></figure>

<h3 id="3-5-进程"><a href="#3-5-进程" class="headerlink" title="3.5 进程"></a>3.5 进程</h3><p>​    进程是软件中一种很重要的抽象。为了讨论方便，假设过程P调用过程Q，Q执行后返回P。这些动作包括以下几个机制：</p>
<ul>
<li>传递控制。进入过程Q的时候，程序计数器必须被设置为Q的代码的起始地址，然后在返回时，要把程序计数器设置为P中调用Q后面的那条指令地址。</li>
<li>传递数据。P必须能够向Q提供一个或多个参数，Q必须能够向P返回一个值。</li>
<li>分配和释放内存。在开始时，Q可能需要为局部变量分配空间空间，而在返回前，又必须释放这些存储空间。</li>
</ul>
<h4 id="3-5-1-运行时栈"><a href="#3-5-1-运行时栈" class="headerlink" title="3.5.1 运行时栈"></a>3.5.1 运行时栈</h4><ol>
<li>压栈: 函数参数压栈，返回地址压栈</li>
<li>跳转: 跳转到函数所在代码处执行</li>
<li>执行: 执行函数代码</li>
<li>返回: 平衡堆栈，找出之前的返回地址，跳转回之前的调用点之后，完成函数调用</li>
</ol>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/19.png" class="">


<p>下面我们看一下函数调用指令call：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0x210000 call swap</span><br><span class="line">0x210005 mov ecx,eax </span><br></pre></td></tr></table></figure>

<p>我们可以把它理解为2个指令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">push 0x210005	//call指令的下一条指令地址作为本次函数调用的返回地址压栈</span><br><span class="line">jmp  swap		//然后使用jmp指令修改指令指针寄存器EIP，使cpu执行swap函数的指令代码</span><br></pre></td></tr></table></figure>

<p>Leave等价于：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">movl %ebp %esp</span><br><span class="line">popl %ebp</span><br></pre></td></tr></table></figure>

<p>Ret指令的内部操作是：</p>
<ol>
<li>栈顶字单元出栈，其值赋给IP寄存器。</li>
</ol>
<p>实例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">add</span><span class="params">(<span class="keyword">int</span> x,<span class="keyword">int</span> y)</span></span>&#123;	</span><br><span class="line">    <span class="keyword">int</span> z;	z=x+y;	</span><br><span class="line">    <span class="keyword">if</span>(z&gt;=<span class="number">10</span>)		</span><br><span class="line">        <span class="keyword">return</span> z;	</span><br><span class="line">    add(x,y);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;	</span><br><span class="line">    <span class="keyword">int</span> a=<span class="number">2</span>,b=<span class="number">3</span>;	</span><br><span class="line">    <span class="keyword">int</span> c=add(a,b);	</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>汇编:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#add函数</span><br><span class="line">   0x0000555555555129 &lt;+0&gt;:		endbr64 </span><br><span class="line">   0x000055555555512d &lt;+4&gt;:		push   %rbp</span><br><span class="line">   0x000055555555512e &lt;+5&gt;:		mov    %rsp,%rbp</span><br><span class="line">   0x0000555555555131 &lt;+8&gt;:		sub    $0x20,%rsp</span><br><span class="line">   0x0000555555555135 &lt;+12&gt;:	mov    %edi,-0x14(%rbp)</span><br><span class="line">   0x0000555555555138 &lt;+15&gt;:	mov    %esi,-0x18(%rbp)</span><br><span class="line">   0x000055555555513b &lt;+18&gt;:	mov    -0x14(%rbp),%edx</span><br><span class="line">   0x000055555555513e &lt;+21&gt;:	mov    -0x18(%rbp),%eax</span><br><span class="line">   0x0000555555555141 &lt;+24&gt;:	add    %edx,%eax</span><br><span class="line">   0x0000555555555143 &lt;+26&gt;:	mov    %eax,-0x4(%rbp)</span><br><span class="line">   0x0000555555555146 &lt;+29&gt;:	cmpl   $0x9,-0x4(%rbp)</span><br><span class="line">   0x000055555555514a &lt;+33&gt;:	jle    0x555555555151 &lt;add+40&gt;</span><br><span class="line">   0x000055555555514c &lt;+35&gt;:	mov    -0x4(%rbp),%eax</span><br><span class="line">   0x000055555555514f &lt;+38&gt;:	jmp    0x555555555160 &lt;add+55&gt;</span><br><span class="line">   0x0000555555555151 &lt;+40&gt;:	mov    -0x18(%rbp),%edx</span><br><span class="line">   0x0000555555555154 &lt;+43&gt;:	mov    -0x14(%rbp),%eax</span><br><span class="line">   0x0000555555555157 &lt;+46&gt;:	mov    %edx,%esi</span><br><span class="line">   0x0000555555555159 &lt;+48&gt;:	mov    %eax,%edi</span><br><span class="line">   0x000055555555515b &lt;+50&gt;:	callq  0x555555555129 &lt;add&gt;</span><br><span class="line">   0x0000555555555160 &lt;+55&gt;:	leaveq </span><br><span class="line">   0x0000555555555161 &lt;+56&gt;:	retq  </span><br><span class="line">#main函数</span><br><span class="line">   0x0000555555555162 &lt;+0&gt;:		endbr64 </span><br><span class="line">   0x0000555555555166 &lt;+4&gt;:		push   %rbp</span><br><span class="line">   0x0000555555555167 &lt;+5&gt;:		mov    %rsp,%rbp</span><br><span class="line">   0x000055555555516a &lt;+8&gt;:		sub    $0x10,%rsp</span><br><span class="line">   0x000055555555516e &lt;+12&gt;:	movl   $0x2,-0xc(%rbp)</span><br><span class="line">   0x0000555555555175 &lt;+19&gt;:	movl   $0x3,-0x8(%rbp)</span><br><span class="line">   0x000055555555517c &lt;+26&gt;:	mov    -0x8(%rbp),%edx</span><br><span class="line">   0x000055555555517f &lt;+29&gt;:	mov    -0xc(%rbp),%eax</span><br><span class="line">   0x0000555555555182 &lt;+32&gt;:	mov    %edx,%esi</span><br><span class="line">   0x0000555555555184 &lt;+34&gt;:	mov    %eax,%edi</span><br><span class="line">   0x0000555555555186 &lt;+36&gt;:	callq  0x555555555129 &lt;add&gt;</span><br><span class="line">   0x000055555555518b &lt;+41&gt;:	mov    %eax,-0x4(%rbp)</span><br><span class="line">   0x000055555555518e &lt;+44&gt;:	mov    $0x0,%eax</span><br><span class="line">   0x0000555555555193 &lt;+49&gt;:	leaveq </span><br><span class="line">   0x0000555555555194 &lt;+50&gt;:	retq   </span><br></pre></td></tr></table></figure>

<h3 id="3-6数组分配与访问"><a href="#3-6数组分配与访问" class="headerlink" title="3.6数组分配与访问"></a>3.6数组分配与访问</h3><h4 id="3-6-1-基本原则"><a href="#3-6-1-基本原则" class="headerlink" title="3.6.1 基本原则"></a>3.6.1 基本原则</h4><img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/20.png" class="">

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">movl	(%rdx,%rcx,4),	%eax//rcx是i，rdx是首地址，计算的是地址xE+4i的值</span><br></pre></td></tr></table></figure>

<h4 id="3-6-2-指针运算"><a href="#3-6-2-指针运算" class="headerlink" title="3.6.2 指针运算"></a>3.6.2 指针运算</h4><img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/21.png" class="">


<h4 id="3-6-3-嵌套数组"><a href="#3-6-3-嵌套数组" class="headerlink" title="3.6.3 嵌套数组"></a>3.6.3 嵌套数组</h4><p>​    假设一个二维数组的一维长度是<strong>C</strong>，一个元素长度为<strong>L</strong></p>
<p>​    <strong>&amp;D[i][j] = xD + L( C*i + j )</strong></p>
<p>​    汇编将A[i][j]复制到%eax寄存器里：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">leaq	(%rsi,%rsi,2),	%rax	//3*i</span><br><span class="line">leaq	(%rdi,%rax,4),	%rax	//xA+12i</span><br><span class="line">movl	(%rax,%rdx,4),	%eax	//M[xA+12i+4j]</span><br></pre></td></tr></table></figure>

<p>一个可变长数组A[n][n]必须在n参数后面，这样函数在遇到这个数组时就可以计算出数组的维度</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">var_ele</span><span class="params">(<span class="keyword">long</span> n,<span class="keyword">int</span> A[n][n],<span class="keyword">long</span> i,<span class="keyword">long</span> j)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> A[i][j];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var_ele:</span><br><span class="line">	imulq	%rdx,	%rdi		//n*i</span><br><span class="line">	leaq	(%rsi,%rdi,4),	%rax//xA+4(n*i)</span><br><span class="line">	movl	(%rax,%rcx,4),	%eax//M[XA+4(n*i)+4*j]</span><br><span class="line">	ret</span><br></pre></td></tr></table></figure>

<h3 id="3-7-异质的数据结构"><a href="#3-7-异质的数据结构" class="headerlink" title="3.7 异质的数据结构"></a>3.7 异质的数据结构</h3><h4 id="3-7-1-结构体"><a href="#3-7-1-结构体" class="headerlink" title="3.7.1 结构体"></a>3.7.1 结构体</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rec</span>&#123;</span></span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">int</span> j;</span><br><span class="line">    <span class="keyword">int</span> a[<span class="number">2</span>];</span><br><span class="line">    <span class="keyword">int</span> *p;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这个结构体包括了4个字段；两个4字节int、一个由两个int元素组成的数组和一个8字节的整型指针，总共24字节。</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/22.png" class="">


<p>若要r-&gt;i复制给r-&gt;j</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">movl	(%rdi),	%eax	//Get r-&gt;i</span><br><span class="line">movl	%eax,	4(%rdi)	//存储到r-&gt;j</span><br></pre></td></tr></table></figure>

<h4 id="3-7-2-数据对齐"><a href="#3-7-2-数据对齐" class="headerlink" title="3.7.2 数据对齐"></a>3.7.2 数据对齐</h4><p>许多计算机系统对基本数据类型的合法地址做出了一些限制，要求某种类型对象的地址必须必须是某个值K（通常是2、4、8）的倍数。这种对齐限制简化了形成处理器和内存系统之间的硬件设计。</p>
<p><strong>对齐原则</strong>：任何K字节的基本对象的地址必须是K的倍数。</p>
<p>如下</p>
<img src="/2021/04/12/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%80/23.png" class="">


<p>编辑器在汇编代码中放入命令**.align 8**,指明了全局数据所需的对齐。保证了后面的数据的起始地址是8的倍数。</p>
]]></content>
      <categories>
        <category>CSAPP</category>
      </categories>
  </entry>
  <entry>
    <title>深入理解计算机系统三</title>
    <url>/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/</url>
    <content><![CDATA[<h2 id="第八章：异常控制流"><a href="#第八章：异常控制流" class="headerlink" title="第八章：异常控制流"></a>第八章：异常控制流</h2><p><strong>控制流：</strong></p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/78.png" class="">

<h4 id="8-1-异常"><a href="#8-1-异常" class="headerlink" title="8.1 异常"></a>8.1 异常</h4><p>​    异常是异常控制流的一种形式，一部分由硬件实现，一部分由操作系统实现，这一节中主要是对异常和异常处理有一个一般性的了解。异常就是控制流中的突变，用来相应处理器状态中的某些变化。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/79.png" class="">

<p>在任何情况下，当处理器检测到有事件发生，会通过一张叫<strong>异常表</strong>的跳转表，进行一个间接过程调用（异常），到一个专门设计用来处理这类时间的操作系统子程序（<strong>异常处理程序</strong>）。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/80.png" class="">

<p>当异常处理程序完成以后，根据引起异常的事件类型会发生以下三种情况的一种：</p>
<ol>
<li><p>处理程序将控制返回给当前的指令Icurr，即当事件发生时正在执行的指令。</p>
</li>
<li><p>处理程序将控制返回给Inext，如果没有发生异常将会执行的下一条指令。</p>
</li>
<li><p>处理程序终止被中断的程序。</p>
</li>
</ol>
<h5 id="8-1-1-异常处理"><a href="#8-1-1-异常处理" class="headerlink" title="8.1.1 异常处理"></a>8.1.1 异常处理</h5><p>系统中为每种类型的异常分配了唯一的非负整数的异常号，一些是处理器的设计者分配的，其他是操作系统内核设计者分配的。</p>
<ul>
<li>​    前者包括被0除，缺页，内存访问违例，断点及算术溢出。</li>
<li>​    后者包括系统调用和来自外部IO设备的信号。</li>
</ul>
<p>系统启动时，会初始化一张异常表的跳转表，使条目k包含异常k的处理程序地址。</p>
<p>在运行时，处理器检测到发生了一个事件，并确定了相应的<strong>异常号</strong>k，随后，处理器触发异常，方法是执行间接过程调用，通过异常表的条目k转到相应的处理程序。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/80.png" class="">

<p>异常表的起始地址放在一个叫做<strong>异常表基质寄存器</strong>的特殊CPU寄存器中。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/81.png" class="">

<p>异常处理过程类似于调用但有一些不同：</p>
<ul>
<li>过程调用时，在跳转到处理程序之前，处理器将返回地址压入栈中，然而根据异常的类型，返回地址要么是当前指令，要么是下一条指令。</li>
<li>处理器也把一些额外的处理器状态压入栈里，在处理程序返回时，重新开始执行被中断的程序可能会需要这些状态。</li>
<li>如果控制从用户程序转移到内核，所有这些项目都被压入栈中，而不是用户栈中。</li>
<li>异常处理程序运行在内核模式下，意味着它们对所有的系统资源都有完全的访问权限。</li>
</ul>
<h5 id="8-1-2-异常的类型"><a href="#8-1-2-异常的类型" class="headerlink" title="8.1.2 异常的类型"></a>8.1.2 异常的类型</h5><p>异常可以分为四类：中断、陷阱、故障、终止。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/84.jpg" class="">

<p><strong>中断：</strong></p>
<p>中断是<strong>异步</strong>发生的，来自处理器外部的I/O设备的信号的结果。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/82.png" class="">

<p>在当前指令完成执行之后，处理器注意到中断引脚的电压变高了，就从系统总线读取异常号，然后调用适当的中断程序。当处理程序返回时，它就将控制返回给下一条指令。结果就好像程序继续执行，好像没有发生过中断一样。</p>
<p>剩下的异常类型（陷阱、故障和终止）是同步执行的，是执行当前指令的结果，我们把这类指令叫做<strong>故障指令</strong>。</p>
<p><strong>陷阱和系统调用:</strong></p>
<p>陷阱是有意的异常，是执行一条指令的结果。</p>
<p>陷阱最重要的用途是在用户程序和内核之间提供一个像过程一样的接口，叫做<strong>系统调用</strong>。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/82.5.png" class="">

<p>​    用户程序经常需要向内核请求服务。为了允许对这些服务的受控访问，处理器提供了一条特殊的“syscall n”指令，当用户程序想要请求服务n时，可以执行这条指令。执行syscall指令会导致一个到异常处理程序的陷阱，这个 处理程序解析参数，并调用适当的内核程序。</p>
<p>​    函数调用是在用户模式，系统调用在内核模式。</p>
<p><strong>故障和终止：</strong></p>
<p>故障是由错误情况引起的，它可能被故障处理程序修正。如果被修正，则将控制转移到当前指令。</p>
<p>如果不可修正，则终止，终止处理程序不会将控制返回给应用程序。将返回一个abort例程，该例程将终止程序。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/83.png" class="">

<h5 id="8-1-3-Linux-x86-64系统中的异常"><a href="#8-1-3-Linux-x86-64系统中的异常" class="headerlink" title="8.1.3 Linux/x86-64系统中的异常"></a>8.1.3 Linux/x86-64系统中的异常</h5><img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/85.png" class="">

<ol>
<li><p><strong>Linux/x86-64故障和终止：</strong></p>
<ul>
<li>除法错误。</li>
<li>一般保护故障。通常是一个程序引用了一个未定义的虚拟内存空间，或者程序试图写一个只读的文本。Linux shell一般报告为“段故障”</li>
<li>缺页是会重新执行产生故障的指令的一个异常示例。</li>
<li>机器检查。机器检查是在导致故障的指令执行中检查到致命的硬件错误时发生的。</li>
</ul>
</li>
<li><p><strong>Linux/x86-64系统调用：</strong></p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/86.png" class=""></li>
</ol>
<h4 id="8-2-进程"><a href="#8-2-进程" class="headerlink" title="8.2 进程"></a>8.2 进程</h4><p>​    在现代操作系统上运行一个程序时，会得到一个假象，就好像程序时目前系统中唯一的程序，程序似乎独占处理器和内存等等假象都通过进程的概念提供给我们的。</p>
<pre><code> 进程提供给应用程序的关键抽象：
</code></pre>
<ul>
<li>一个独立的逻辑控制流，它提供一个假象，好像我们的程序独占的使用处理器。</li>
<li>一个私有的地址空间，提供一个假象，好像我们的程序独占的使用内存系统。</li>
</ul>
<h5 id="8-2-1-逻辑控制流"><a href="#8-2-1-逻辑控制流" class="headerlink" title="8.2.1 逻辑控制流"></a>8.2.1 逻辑控制流</h5><p>​    即使在系统中通常有许多其他程序在运行，进程也可以像每个程序提供一种假象，好像它在独占的使用处理器，如果想用调试器单步执行程序，会看到一系列PC值，这些值唯一的对应包含在程序的可执行目标文件中的指令，或是包含在运行时动态连接到程序的共享对象的指令，这个PC值的序列叫做<strong>逻辑控制流</strong>。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/87.png" class="">

<h5 id="8-2-2-并发流"><a href="#8-2-2-并发流" class="headerlink" title="8.2.2 并发流"></a>8.2.2 并发流</h5><p>计算机系统中逻辑流有许多不同形式，异常处理程序、进程、信号处理程序、线程等都是逻辑流的例子。</p>
<p>一个逻辑流的执行在时间上与另一个流重叠，称为<strong>并发流</strong>。</p>
<p>时间片：一个进程执行它的控制流的一部分的每一时间段。</p>
<p>多任务：一个进程和其他进程轮流运行（也叫时间分片）</p>
<p>并发：多个流并发执行的一般现象。</p>
<p>并行流：两个流并发的运行在不同的处理机核或者计算机上，它们并行地运行。</p>
<h5 id="8-2-3-私有地址空间"><a href="#8-2-3-私有地址空间" class="headerlink" title="8.2.3 私有地址空间"></a>8.2.3 私有地址空间</h5><p>私有地址空间—进程为程序提供的假象，好像它独占的使用系统地址空间。一般而言，和这个空间中某个地址相关联的那个存储器字节是不能被其他进程读写的。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/88.png" class="">

<h5 id="8-2-4-用户模式和内核模式"><a href="#8-2-4-用户模式和内核模式" class="headerlink" title="8.2.4 用户模式和内核模式"></a>8.2.4 用户模式和内核模式</h5><p>​    处理器提供一种机制，限制一个应用可以执行的指令以及它可以访问的地址空间范围。通常是通过控制某个寄存器的模式位来提供这种功能，该寄存器描述了进程当前享有的特权。</p>
<ul>
<li>​    设置模式位之后，进程出域内核模式，也叫做超级用户模式，这个进程可以执行指令集中的任何指令，并且可以访问系统中的任何内存位置。</li>
<li>​    没有设置模式位，进程运行在用户模式，用户模式中的进程不允许执行特权指令，比如停止处理器、改变模式位、发起IO操作、不允许引用地址空间中内核区的代码和数据，这样的尝试都会引起故障。用户程序必须通过系统调用接口间接的访问内核代码和数据。</li>
</ul>
<p> linux提供了一种机制，叫做/proc文件系统，允许用户模式进程访问内核数据结构中的内容。</p>
<p>/proc文件系统将许多内核数据的内容输出为一个用户程序可以读的文本文件的层次结构。</p>
<h5 id="8-2-5-上下文切换"><a href="#8-2-5-上下文切换" class="headerlink" title="8.2.5 上下文切换"></a>8.2.5 上下文切换</h5><p>​    操作系统内核使用一种称为上下文切换的较高层次的异常控制流来实现多任务。上下文切换机制是建立在上面讨论过的低层的异常机制上的。</p>
<p>​    内核为每个进程维持一个<strong>上下文</strong>，上下文就是内核重新启动一个被抢占的进程所需要的状态，由一些对象的值组成，包括通用目的寄存器、浮点寄存器、程序计数器、用户栈、状态寄存器、内存栈和各种内核数据结构，比如描述地址空间的页表、包含当前进程信息的进程表，以及包含进程已打开文件的信息的文件表。</p>
<p>下图为一对进程上下文切换的示例，进程A初始运行在用户模式在，直到执行系统调用read陷入内核，内核中的陷阱处理程序请求来自磁盘控制器的DMA传输，并安排在磁盘控制器完成从磁盘到内存的数据传输后，磁盘中断处理器：</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/89.png" class="">

<p>上下文切换：</p>
<ol>
<li>保存当前进程的上下文。</li>
<li>恢复某个先前被强占的进程被保存的上下文。</li>
<li>将控制传递给这个新恢复的进程。</li>
</ol>
<h4 id="8-3-系统调用错误处理"><a href="#8-3-系统调用错误处理" class="headerlink" title="8.3 系统调用错误处理"></a>8.3 系统调用错误处理</h4><img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/90.png" class="">

<h4 id="8-4-进程控制"><a href="#8-4-进程控制" class="headerlink" title="8.4 进程控制"></a>8.4 进程控制</h4><h5 id="8-4-1-获取进程ID"><a href="#8-4-1-获取进程ID" class="headerlink" title="8.4.1 获取进程ID"></a>8.4.1 获取进程ID</h5><p>每个进程都有唯一的正数（非0）进程ID（PID），getpid函数返回调用进程的PID，getppid函数返回他的父进程的PID（创建调用进程的进程）。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">pid_t</span> <span class="title">getpid</span><span class="params">(<span class="keyword">void</span>)</span>；</span></span><br><span class="line"><span class="function"><span class="keyword">pid_t</span> <span class="title">getppid</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br></pre></td></tr></table></figure>

<p>在Linux系统上pid_t被定义为int类型。</p>
<h5 id="8-4-2-创建和终止进程"><a href="#8-4-2-创建和终止进程" class="headerlink" title="8.4.2 创建和终止进程"></a>8.4.2 创建和终止进程</h5><p>从程序员角度，可以认为进程总是处于下面三种状态之一：</p>
<ul>
<li>运行。进程要么在CPU上执行，要么在等待被执行且最终会被内核调度。</li>
<li>停止。进程的执行被挂起，且不会被调度，当收到SIGSTOP、SIGTSTP、SIGTTIN或者SIGTTOU信号时，进程会停止，并保持停止直到它收到一个SIGCONT信号，在这个时刻，进程再次开始运行。</li>
<li>终止。进程永远的停止，进程会因为三种原因终止：<ol>
<li>收到一个信号，该信号的默认行为是终止进程。</li>
<li>从主程序返回。</li>
<li>调用exit函数。</li>
</ol>
</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">exit</span><span class="params">(<span class="keyword">int</span> status)</span></span>;</span><br></pre></td></tr></table></figure>

<p>exit函数以status退出状态来终止进程（另一种设置退出状态的方法是从主程序返回一个整数值）。父进程调用fork函数创建一个新的运行的子进程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">pid_t</span> <span class="title">fork</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br></pre></td></tr></table></figure>

<p>​    新创建的子进程几乎但不完全与父进程相同，子进程得到与父进程用户级虚拟地址空间相同的但是独立的一个副本，包括代码和数据段、堆、共享库以及用户栈。子进程还获得与父进程任何打开文件描述符相同的副本，这就意味着父进程调用fork时，子进程可以读写父进程中打开的任何文件父进程和新创建的子进程最大的区别在于他们有不同的PID。<br>​     fork函数有趣的在于它调用一次，但是会返回两次，一次是在调用进程（父进程）中，一次是在新创建的子进程中。在父进程中fork返回的是子进程的PID，在子进程中fork返回0。因为子进程的PID总是非0，返回值就明确的分辨出程序是在父进程还是在子进程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&quot;csapp.h&quot;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">pid_t</span> pid;</span><br><span class="line">	<span class="keyword">int</span> x=<span class="number">1</span>;</span><br><span class="line">	pid=Fork();</span><br><span class="line">	<span class="keyword">if</span>(pid==<span class="number">0</span>)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="comment">//子进程</span></span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">&quot;child:x = %d\n&quot;</span>,++x);</span><br><span class="line">		<span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;parent :x=%d\n &quot;</span>,--x);</span><br><span class="line">	<span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/91.png" class="">

<p> 从上述可以看到：</p>
<ol>
<li><p>调用一次，返回两次。fork函数被父进程调用一次，却返回两次。一次是返回到父进程，一次是返回到新创建的子进程。</p>
</li>
<li><p>并发执行。父进程和子进程是并发运行的独立进程，内核能以任意方式交替执行它们的逻辑控制流中的指令。</p>
</li>
<li><p>相同但是独立的地址空间。</p>
</li>
<li><p>共享文件。子进程共享了父进程打开的文件，stdout。所以子进程输出也是指向屏幕。</p>
</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">   Fork();</span><br><span class="line">   Fork();</span><br><span class="line">   <span class="built_in">printf</span>(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line">   <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/92.png" class="">

<h5 id="8-4-3-回收子进程"><a href="#8-4-3-回收子进程" class="headerlink" title="8.4.3 回收子进程"></a>8.4.3 回收子进程</h5><p>​    当一个进程由于某种原因终止时，内核并不是立即把它从系统中清除，相反进程被保持在一种已终止的状态中，直到被父进程回收，当父进程回收已终止的子进程时，内核将子进程的退出状态传递给父进程，然后抛弃已终止的进程。从此时开始，该进程就不存在了。一个终止了但没有被回收的进程称为僵尸进程。</p>
<p>​    如果一个父进程终止了，内核会安排init进程称为它的孤儿进程（如果父进程先退出,子进程还没退出那么子进程将被 托孤给init进程）的养父。init进程的PID为1，是系统在启动时由内核创建的，他不会终止，是所有进程的祖先。如果父进程没有回收它的僵尸进程就终止了，那么内核会安排init进程回收它们，长时间运行的程序，比如shell或者服务器，总是应该回收它们的僵尸子进程，即使僵尸进程没有运行，仍然会消耗系统的内存资源。一个进程可以通过调用waitpid函数等待它的子进程终止或停止。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"> <span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"> <span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"> </span><br><span class="line"><span class="comment">//成功返回子进程pid，WNOHANG则为0,其他错误-1</span></span><br><span class="line"><span class="function"><span class="keyword">pid_t</span> <span class="title">waitpid</span><span class="params">(<span class="keyword">pid_t</span> pid, <span class="keyword">int</span> *statusp, <span class="keyword">int</span> options)</span></span>;</span><br></pre></td></tr></table></figure>

<p>默认情况下，options=0时，waitpid挂起调用进程的执行，直到它的等待集合中一个子进程终止，如果等待集合中的一个进程在刚调用的时候就已经终止，那么waitpid立即返回。在这两种情况下，waitpid返回导致waitpid返回的已终止子进程的PID。此时，已终止的子进程已经被回收，内核会从系统中删除掉它的所有痕迹。</p>
<ol>
<li><p>判定等待集合的成员</p>
<ul>
<li>如果pid&gt;0，那么等待集合就是一个单独的子进程，它的进程ID等于pid</li>
<li>如果pid=-1，那么等待集合就是由父进程所有的子进程组成</li>
</ul>
</li>
<li><p>修改默认行为</p>
<p>可以通过将options设置为常量WNOHANG、WUNTRACED和WCONTINUED的各种组合修改默认行为。</p>
<ul>
<li>WNOHANG：如果等待集合中的任何子进程都还没有终止，那么就立即返回，返回值为0，默认的行为是挂起调用进程，直到有子进程终止，在等待子进程终止的同时如果还有想做的工作，这个选项会有用。</li>
<li>WUNTRACED：挂起调用进程的执行，直到等待集合中一个进程变成已终止或者已经被停止，返回的PID为呆滞返回的已终止或被停止的子进程的PID，默认的行为是只返回已终止的子进程。当想要金叉已终止和被停止的子进程时会有用。</li>
<li>WCONTINUED：挂起调用进程的执行，直到等待集合中一个正在运行的进程终止，或等待集合中和一个被停止的进程收到SIGCONT信号重新开始执行。</li>
</ul>
<p>可以用或运算把这些选项组合起来。</p>
</li>
<li><p>检查已回收子进程的退出状态</p>
<p>如果statusp参数是非空的，那么waitpid就会在status中放上关于导致返回子进程的状态信息，status是statusp指向的值。wait.h中定义了status参数的几个宏。</p>
<ul>
<li>WIFEXITED：如果子进程调用exit或者返回正常终止，就返回真。</li>
<li>WEXITSTATUS：返回一个正常终止的子进程的退出状态，只有在WIFEXITED()返回为真时，才定义这个状态。</li>
<li>WIFSIGNALED：如果子进程是因为一个未被捕获的信号终止的，那么就返回真。</li>
<li>WTERMSIG：返回导致子进程终止的信号的编号，只有在WIFSIGNALED()返回为真时，才定义这个状态。</li>
<li>WIFSTOPPED：如果引起返回的子进程当前是停止的，那么返回真。</li>
<li>WSTOPSIG：返回引起子进程停止的信号的编号。只有在WIFSTOPPED()返回为真才定义这个状态。</li>
<li>WIFCONTINUED：如果子进程收到SIGCONT信号重新启动，则返回真。</li>
</ul>
</li>
<li><p>错误条件</p>
<p>如果调用进程没有子进程，那么waitpid返回-1，并设置errno为ECHILD。如果waitpid函数被一个信号中断，那么返回-1，并设置erron为EINTR。</p>
</li>
<li><p>wait函数</p>
<p>wait函数是waitpid函数的简单版本。</p>
<p>调用wait(&amp;status)等价于调用waitpid(-1,&amp;status,0)。</p>
</li>
</ol>
<h5 id="8-4-4-让进程休眠"><a href="#8-4-4-让进程休眠" class="headerlink" title="8.4.4 让进程休眠"></a>8.4.4 让进程休眠</h5><p>sleep函数将一个进程挂起一段时间。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="title">sleep</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">int</span> secs)</span></span>;</span><br></pre></td></tr></table></figure>

<p>如果请求的时间量已经到了，sleep返回0，否则返回还剩下的要休眠的秒数，后一种情况是可能的，如果sleep函数被一个信号中断而过早的返回，还有一个有用的函数是pause函数，该函数让调用函数休眠，直到进程收到一个信号。</p>
<h5 id="8-4-5-加载并运行程序"><a href="#8-4-5-加载并运行程序" class="headerlink" title="8.4.5 加载并运行程序"></a>8.4.5 加载并运行程序</h5><p>​    execve函数在当前进程的上下文加载并运行一个新程序。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execve</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *filename, <span class="keyword">char</span> *<span class="keyword">const</span> argv[], <span class="keyword">char</span> *<span class="keyword">const</span> envp[])</span></span>;</span><br></pre></td></tr></table></figure>

<p>​    execve函数加载并运行可执行目标文件filename，且带参数列表argv和环境变量列表envp。只有当出现错误时，例如找不到filename，execve才会返回到调用程序，所以，与fork一次调用两次返回不同，execve调用一次不返回。</p>
<p>​    argv变量指向一个以null结尾的指针数组，其中每个指针都指向一个参数字符串，按惯例，argv[0]是可执行文件的名字，环境变量的列表是由一个类似的数据结构表示的，envp变量指向一个以null结尾的指针数组，其中每个指针指向一个环境变量字符串。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/93.png" class="">

<p>在exceve加载了filename之后，调用一些启动代码，启动代码设置栈，并将控制传递给新程序的主函数，该主函数有如下形式：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv, <span class="keyword">char</span> **envp)</span></span>;</span><br></pre></td></tr></table></figure>

<p>​    当main开始执行时，用户的栈组织结构如下图所示，从栈底（高地址）往栈顶（低地址）看，首先是参数和环境字符串，栈网上紧随其后的是以null结尾的指针数组，其中每个指针都指向栈中一个环境变量字符串，全局变量environ指向这些指针中的第一个envp[0]，紧跟环境变量数组之后的是以null结尾的argv[]数组，其中每个元素都指向栈中的一个参数字符串，在栈顶部是系统启动函数libc_start_main的栈帧。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/94.png" class="">

<p>main函数又3个参数：</p>
<ol>
<li>argc,它给出argv[]数组中非空指针的数目</li>
<li>argv，指向argv[]数组中的第一条目</li>
<li>envp，指向envp[]数组中的第一个条目</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;ctype.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">char</span> **environ;    </span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAXLINE    1024   <span class="comment">/* max line size */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAXARGS     128   <span class="comment">/* max args on a command line */</span></span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">unix_error</span><span class="params">(<span class="keyword">char</span> *msg)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">eval</span><span class="params">(<span class="keyword">char</span> *cmdline)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">builtin_cmd</span><span class="params">(<span class="keyword">char</span> **argv)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">parseline</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *cmdline, <span class="keyword">char</span> **argv)</span></span>; </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> cmdline[MAXLINE];</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;tsh&gt; &quot;</span>);</span><br><span class="line">        fgets(cmdline,MAXLINE,<span class="built_in">stdin</span>);</span><br><span class="line">	<span class="keyword">if</span> (feof(<span class="built_in">stdin</span>)) &#123; </span><br><span class="line">	    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	eval(cmdline);</span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">eval</span><span class="params">(<span class="keyword">char</span> *cmdline)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> *argv[MAXLINE];</span><br><span class="line">    <span class="keyword">char</span> buf[MAXLINE];</span><br><span class="line">    <span class="keyword">int</span> bg;                 <span class="comment">/*should the job run in bg or fg?*/</span></span><br><span class="line">    <span class="keyword">pid_t</span> pid;</span><br><span class="line">    </span><br><span class="line">    stpcpy(buf,cmdline);</span><br><span class="line">    bg = parseline(buf,argv);</span><br><span class="line">    <span class="keyword">if</span>(argv[<span class="number">0</span>]==<span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="keyword">return</span>;     <span class="comment">/*ignore empty line*/</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(!builtin_cmd(argv))&#123;                         <span class="comment">/*not a build in cmd*/</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>((pid = fork())==<span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">                <span class="keyword">if</span>(execve(argv[<span class="number">0</span>],argv,environ)&lt;<span class="number">0</span>)&#123;</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;%s: Command not found. \n&quot;</span>,argv[<span class="number">0</span>]);</span><br><span class="line">                <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(!bg)</span><br><span class="line">        &#123;</span><br><span class="line">        	<span class="keyword">int</span> status;</span><br><span class="line">        	<span class="keyword">if</span>(waitpid(pid, &amp;status, <span class="number">0</span>)&lt;<span class="number">0</span>)</span><br><span class="line">        		unix_error(<span class="string">&quot;waitfg:waitpid\n&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        	<span class="built_in">printf</span>(<span class="string">&quot;%d %s&quot;</span>,pid,cmdline);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">parseline</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *cmdline, <span class="keyword">char</span> **argv)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">char</span> <span class="built_in">array</span>[MAXLINE]; <span class="comment">/* holds local copy of command line */</span></span><br><span class="line">    <span class="keyword">char</span> *buf = <span class="built_in">array</span>;          <span class="comment">/* ptr that traverses command line */</span></span><br><span class="line">    <span class="keyword">char</span> *delim;                <span class="comment">/* points to first space delimiter */</span></span><br><span class="line">    <span class="keyword">int</span> argc;                   <span class="comment">/* number of args */</span></span><br><span class="line">    <span class="keyword">int</span> bg;                     <span class="comment">/* background job? */</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">strcpy</span>(buf, cmdline);</span><br><span class="line">    buf[<span class="built_in">strlen</span>(buf)<span class="number">-1</span>] = <span class="string">&#x27; &#x27;</span>;  <span class="comment">/* replace trailing &#x27;\n&#x27; with space */</span></span><br><span class="line">    <span class="keyword">while</span> (*buf &amp;&amp; (*buf == <span class="string">&#x27; &#x27;</span>)) <span class="comment">/* ignore leading spaces */</span></span><br><span class="line">	buf++;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Build the argv list */</span></span><br><span class="line">    argc = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (*buf == <span class="string">&#x27;\&#x27;&#x27;</span>) &#123;</span><br><span class="line">	buf++;</span><br><span class="line">	delim = <span class="built_in">strchr</span>(buf, <span class="string">&#x27;\&#x27;&#x27;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">	delim = <span class="built_in">strchr</span>(buf, <span class="string">&#x27; &#x27;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (delim) &#123;</span><br><span class="line">	argv[argc++] = buf;</span><br><span class="line">	*delim = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">	buf = delim + <span class="number">1</span>;</span><br><span class="line">	<span class="keyword">while</span> (*buf &amp;&amp; (*buf == <span class="string">&#x27; &#x27;</span>)) <span class="comment">/* ignore spaces */</span></span><br><span class="line">	       buf++;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (*buf == <span class="string">&#x27;\&#x27;&#x27;</span>) &#123;</span><br><span class="line">	    buf++;</span><br><span class="line">	    delim = <span class="built_in">strchr</span>(buf, <span class="string">&#x27;\&#x27;&#x27;</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span> &#123;</span><br><span class="line">	    delim = <span class="built_in">strchr</span>(buf, <span class="string">&#x27; &#x27;</span>);</span><br><span class="line">	&#125;</span><br><span class="line">    &#125;</span><br><span class="line">    argv[argc] = <span class="literal">NULL</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (argc == <span class="number">0</span>)  <span class="comment">/* ignore blank line */</span></span><br><span class="line">	<span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* should the job run in the background? */</span></span><br><span class="line">    <span class="keyword">if</span> ((bg = (*argv[argc<span class="number">-1</span>] == <span class="string">&#x27;&amp;&#x27;</span>)) != <span class="number">0</span>) &#123;</span><br><span class="line">	argv[--argc] = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> bg;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">builtin_cmd</span><span class="params">(<span class="keyword">char</span> **argv)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!<span class="built_in">strcmp</span>(argv[<span class="number">0</span>],<span class="string">&quot;quit&quot;</span>))</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span>(!<span class="built_in">strcmp</span>(argv[<span class="number">0</span>],<span class="string">&quot;&amp;&quot;</span>))</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;     <span class="comment">/* not a builtin command */</span>    <span class="comment">/* not a builtin command */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">unix_error</span><span class="params">(<span class="keyword">char</span> *msg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">fprintf</span>(<span class="built_in">stdout</span>, <span class="string">&quot;%s: %s\n&quot;</span>, msg, strerror(errno));</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="8-5-信号"><a href="#8-5-信号" class="headerlink" title="8.5 信号"></a>8.5 信号</h4><p>信号是一种更高层的软件形式的异常，它允许进程和内核中断其他进程。一个信号就是一条小消息，它通知进程系统中发生一个某种类型的事件。每种信号对应某种系统事件，低层的硬件异常是由内核异常处理程序处理的，正常情况下，对用户进程而言是不可见的，信号提供一种机制，通知用户进程发生了这些异常，比如，一个进程试图除以0，那么内核就会发送一个SIGFPE信号，如果一个进程执行一条非法指令，那么内核就发送给它一个SIGSEGV信号。下图为linux信号。</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/95.png" class="">

<h2 id="第九章：虚拟内存"><a href="#第九章：虚拟内存" class="headerlink" title="第九章：虚拟内存"></a>第九章：虚拟内存</h2><p>什么是虚拟内存？</p>
<p>​    内存容易被破坏，如果一个进程写入了另一个进程使用的内存，就有可能会造成崩溃。虚拟内存是对主存概念的抽象概念。</p>
<p>用虚拟内存的好处：</p>
<ul>
<li>根据特定的需要在磁盘和主存之间告诉高速传输数据，从而高效使用缓存</li>
<li>为每个进程提供相同的地址空间，从而简化内存管理</li>
<li>保护每个内存空间的进程不被其他进程破坏</li>
</ul>
<h4 id="9-1-物理和虚拟寻址"><a href="#9-1-物理和虚拟寻址" class="headerlink" title="9.1 物理和虚拟寻址"></a>9.1 物理和虚拟寻址</h4><p>早期PC：物理寻址</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/96.png" class="">

<p>近代PC：虚拟寻址</p>
<p>​    CPU生成虚拟地址访问主存，虚拟地址被送到主存之前首先通过MMU转换为物理地址</p>
<img src="/2021/07/04/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%B8%89/97.png" class="">



<h4 id="9-2-地址空间"><a href="#9-2-地址空间" class="headerlink" title="9.2 地址空间"></a>9.2 地址空间</h4><p>非负整数地址的有序集合，在地址空间中的整数通常是连续的，我们称之为<strong>线性地址空间</strong>。</p>
<h4 id="9-3-虚拟内存作为缓存工具"><a href="#9-3-虚拟内存作为缓存工具" class="headerlink" title="9.3 虚拟内存作为缓存工具"></a>9.3 虚拟内存作为缓存工具</h4><p>概念上而言，可以将虚拟内存（VM）理解为一个由存放在磁盘上的m个连续的字节大小的单元所组成的数组。</p>
<p>VM系统将虚拟内存分割为虚拟页，同时将物理内存分割为物理页，每个页大小为2^p字节。</p>
<p>在任意时刻，虚拟页面的集合都被分为三个不相交的子集：</p>
<ul>
<li>未分配的VM系统还未分配（或者创建）的页。未分配的块没有</li>
</ul>
]]></content>
      <categories>
        <category>CSAPP</category>
      </categories>
  </entry>
  <entry>
    <title>深入理解计算机系统二</title>
    <url>/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/</url>
    <content><![CDATA[<h2 id="第四章：处理器体系结构"><a href="#第四章：处理器体系结构" class="headerlink" title="第四章：处理器体系结构"></a>第四章：处理器体系结构</h2><h4 id="4-1-1-程序员可见的状态"><a href="#4-1-1-程序员可见的状态" class="headerlink" title="4.1.1 程序员可见的状态"></a>4.1.1 程序员可见的状态</h4><ul>
<li>寄存器RF：共有十五个，去掉了%r15。</li>
<li>其中%rsp作为栈指针</li>
<li>条件码CC：ZF SF OF 少了 CF</li>
<li>程序计数器PC</li>
<li>内存DMEM：用虚拟地址引用内存位置，看成一个很大的字节数组</li>
<li>状态码Stat：表明程序执行的总体状态 正常运行、异常（ex.读取非法内存地址）</li>
</ul>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/24.png" class="">


<h4 id="4-1-2-Y86-64指令"><a href="#4-1-2-Y86-64指令" class="headerlink" title="4.1.2 Y86-64指令"></a>4.1.2 Y86-64指令</h4><ul>
<li>X86-64指令集的子集</li>
<li>只含有8字节整数操作（字长64位）</li>
</ul>
<p>–</p>
<ul>
<li>X86-64的movq指令分为了irmovq，rrmovq，mrmovq和rmmovq</li>
<li>i是立即数immediate，r是内存memory，r是寄存器register</li>
<li>一个字母是源操作数，一个字母是目的操作数</li>
<li>不允许内存传送到内存，也不允许立即数到内存</li>
</ul>
<p>–</p>
<ul>
<li><p>有四个整数操作指令</p>
<p>OPq：addq，subq，andq，xorq</p>
</li>
<li><p>只对寄存器数据进行操作</p>
</li>
<li><p>会设置条件码</p>
</li>
</ul>
<p>–</p>
<ul>
<li>有7个跳转指令jxx：jmp，jlem，jl，je，jne，jge，jg。</li>
<li>根据分支指令的类型和条件代码来设置选择分治</li>
<li>分支条件和x86-64相同</li>
</ul>
<p>–</p>
<ul>
<li><p>6个条件传送指令：</p>
<p>cmovle，cmovl，comve，comvne，comvge，comvg</p>
</li>
</ul>
<p>–</p>
<ul>
<li>call指令会返回地址入栈，然后调到目的地址。ret指令会从这样的调用中返回。</li>
<li>pushq，popq实现入栈和出栈</li>
<li>halt指令停滞指令执行，相当于x86-64的hlt指令。执行该指令会导致处理器暂停，并将Stat状态码设置为HLT。</li>
</ul>
<h4 id="4-1-3-指令编码"><a href="#4-1-3-指令编码" class="headerlink" title="4.1.3 指令编码"></a>4.1.3 指令编码</h4><img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/25.png" class="">


<p>首字节区分不同指令。首字节高位表示指令代码，低位表示功能。代码的值从0x0到0xB。功能的值只在同一组指令共享代码时起作用。</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/26.png" class="">


<p>15个寄存器都在一个相对应范围在0x0到0xE之间。编号和x86-64相同</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/27.png" class="">


<p>有个指令只有一个字节长，比如halt，nop，ret等。</p>
<p>有的指令因为需要操作数，则会更长一些。原因有如下两种：</p>
<ol>
<li>可能附加的寄存器指示符字节。用于指定1~2个寄存器，被称为rA和rB。可以用于源寄存器和目的寄存器或用于地址计算的基址寄存器。</li>
<li>有些指令会附加一个8字节常数constant word。<ol>
<li>可以作为irmovq的立即数数据</li>
<li>可以作为rmmovq和mrmovq的地址指示符的偏移量。</li>
<li>分支指令和调用指令的目的地址。（绝对地址，而不用PC的相对寻址）</li>
<li>采用小端法编码。指令按照反汇编格式书写的时候，这些字节就以相反的顺序出现。</li>
</ol>
</li>
</ol>
<p>让我们尝试生成下述汇编指令的字节码。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">rmmovq %rsp,0x123456789abcd(%rdx)</span><br><span class="line">4 0 4 2 cd ab 89 67 45 23 01 00</span><br></pre></td></tr></table></figure>

<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/28.png" class="">


<h4 id="4-1-4-Y86-64异常"><a href="#4-1-4-Y86-64异常" class="headerlink" title="4.1.4 Y86-64异常"></a>4.1.4 Y86-64异常</h4><p>状态码Stat有四种可能值</p>
<ol>
<li>程序执行正常</li>
<li>处理器执行了halt指令</li>
<li>处理器试图从一个非法内存地址都或者写</li>
<li>遇到非法的指令代码</li>
</ol>
<h2 id="第五章：优化程序性能"><a href="#第五章：优化程序性能" class="headerlink" title="第五章：优化程序性能"></a>第五章：优化程序性能</h2><h4 id="5-1-优化程序编译器的能力与局限性"><a href="#5-1-优化程序编译器的能力与局限性" class="headerlink" title="5.1 优化程序编译器的能力与局限性"></a>5.1 优化程序编译器的能力与局限性</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">twiddle1</span><span class="params">(<span class="keyword">long</span> *xp,<span class="keyword">long</span> *yp)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    *xp+=*yp;</span><br><span class="line">    *xp+=*yp;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">twiddle2</span><span class="params">(<span class="keyword">long</span> *xp,<span class="keyword">long</span> *yp)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    *xp+=<span class="number">2</span>* *yp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>二者功能几乎相同，对比发现</p>
<ul>
<li>对于函数1，进行了六次读写操作，而对于函数2只进行了三次独写操作</li>
<li>那么编译器会不会将函数1优化成函数2呢？</li>
</ul>
<p>答案是不会，</p>
<p>如果xp与yp指向相同的地址，就会出现问题</p>
<h4 id="5-2-表示程序性能"><a href="#5-2-表示程序性能" class="headerlink" title="5.2 表示程序性能"></a>5.2 表示程序性能</h4><p>引入计量标准：每元素周期数（CPE）</p>
<p>4GHz，时钟周期为时钟频率的倒数（0.25ns）</p>
<p>用时钟周期表示，计量值指示了运行指令的个数</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">psum1</span><span class="params">(<span class="keyword">float</span> a[],<span class="keyword">float</span> p[],<span class="keyword">long</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> i;</span><br><span class="line">    p[<span class="number">0</span>]=a[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">1</span>;i&lt;n;i++)</span><br><span class="line">        p[i]=p[i<span class="number">-1</span>]+a[i];</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">psum2</span><span class="params">(<span class="keyword">float</span> a[],<span class="keyword">float</span> p[],<span class="keyword">long</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> i;</span><br><span class="line">    p[<span class="number">0</span>]=a[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">1</span>;i&lt;n<span class="number">-1</span>;i+=<span class="number">2</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">float</span> mid_val=p[i<span class="number">-1</span>]+a[i];</span><br><span class="line">        p[i] = mid_val;</span><br><span class="line">        p[i+<span class="number">1</span>]=mid_val+a[i+<span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(i&lt;n)</span><br><span class="line">        p[i]=p[i+<span class="number">1</span>]+a[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/29.png" class="">


<p>循环展开法：每次迭代的元素个数不同</p>
<h4 id="5-3-消除循环的低效率"><a href="#5-3-消除循环的低效率" class="headerlink" title="5.3 消除循环的低效率"></a>5.3 消除循环的低效率</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">combine1</span><span class="params">(vec_ptr v,<span class="keyword">data_t</span> *dest)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> i;</span><br><span class="line">    *dest=IDENT;</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;vec_length(v);i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">data_t</span> val;</span><br><span class="line">        get_vec_element(v,i,&amp;val);</span><br><span class="line">        *dest=*dest OP val;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">combine2</span><span class="params">(vec_ptr v,<span class="keyword">data_t</span> *dest)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> i;</span><br><span class="line">    length=vec_length(v)</span><br><span class="line">    *dest=IDENT;</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;length;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">data_t</span> val;</span><br><span class="line">        get_vec_element(v,i,&amp;val);</span><br><span class="line">        *dest=*dest OP val;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>计算长度的函数只要调用一次就好，所以在开始时进行一次，然后存储下来就好。减少结果重复的不必要运算。</p>
<p>编译器会尝试执行代码移动，但它有时候难以发现函数的副作用，因而编译器的优化会很谨慎。</p>
<h4 id="5-4-减少不必要的内存引用"><a href="#5-4-减少不必要的内存引用" class="headerlink" title="5.4 减少不必要的内存引用"></a>5.4 减少不必要的内存引用</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">data_t</span> *<span class="title">get_vec_start</span><span class="params">(vec_ptr v)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> v-&gt;data;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">combine3</span><span class="params">(vec_ptr v, <span class="keyword">data_t</span> *dest)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> i;</span><br><span class="line">    <span class="keyword">long</span> length=vec_length(v);</span><br><span class="line">    <span class="keyword">data_t</span> *data=get_vec_start(v);</span><br><span class="line">    </span><br><span class="line">    *dest=IDENT;</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;length;i++)&#123;</span><br><span class="line">        *dest = *dest OP data[i];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;<span class="comment">//消除了循环中的函数调用,直接使用数组</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">combine4</span><span class="params">(vec_ptr v, <span class="keyword">data_t</span> *dest)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> i;</span><br><span class="line">    <span class="keyword">long</span> length=vec_length();</span><br><span class="line">    <span class="keyword">data_t</span> *data=get_vec_start(v);</span><br><span class="line">    <span class="keyword">data_t</span> acc=IDENT;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;length;i++)&#123;</span><br><span class="line">        acc=acc OP data[i];</span><br><span class="line">    &#125;</span><br><span class="line">    *dest=acc;</span><br><span class="line">&#125;<span class="comment">//指针运算：寄存器与内存之间多次读写，非常耗时</span></span><br><span class="line"><span class="comment">//临时变量用寄存器保存累计值，效率显著提高</span></span><br></pre></td></tr></table></figure>

<h4 id="5-7-理解现代处理器"><a href="#5-7-理解现代处理器" class="headerlink" title="5.7 理解现代处理器"></a>5.7 理解现代处理器</h4><ul>
<li>生成代码应该考虑到对目标处理器进行调整</li>
<li>现代微处理器了不起的功绩之一是：使得多条指令可以并行的运行，同时又呈现出一种简单顺序执行的表象</li>
</ul>
<h5 id="5-7-1-整体操作"><a href="#5-7-1-整体操作" class="headerlink" title="5.7.1 整体操作"></a>5.7.1 整体操作</h5><img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/30.png" class="">


<ol>
<li><p>寄存器重命名</p>
<p>指令执行发生例外或转移指令猜测错误而取消后面的指令时可以保证现场的精确</p>
<p>思路：当一条指令写一个结果寄存器时不直接写道这个结果寄存器，而是先写道一个中间寄存器过渡一下，当这个指令提交的时候再写到结果寄存器中</p>
</li>
<li><p>寄存器的更新</p>
<p>仅当指令退役时才发生。指令退役的两种情况：</p>
<ul>
<li>指令操作完成，且所有分支预测被认为正确</li>
<li>某个分支预测错误，清空指令，放弃运算结果</li>
</ul>
</li>
</ol>
<h4 id="5-7-2-功能单元的性能"><a href="#5-7-2-功能单元的性能" class="headerlink" title="5.7.2 功能单元的性能"></a>5.7.2 功能单元的性能</h4><p>延迟：完成运算需要的总时间</p>
<p>发射：两个同类型运算间隔时间</p>
<ul>
<li>发射时间为1：流水线化的功能单元（如浮点加法包含三个阶段，三个周期延迟，<strong>处理指数值</strong>，<strong>对小数相加</strong>，<strong>进行舍入</strong>）各阶段完成一部分运算，对于不同操作数，只需要完成自己的部分。</li>
</ul>
<p>吞吐量：发射时间倒数</p>
<p>利用<strong>延迟界限</strong>和<strong>吞吐量界限</strong>描述程序最大性能</p>
<ul>
<li>延迟界限：代码中数据相关限制了处理器利用指令级并行的能力，给出了任何必须按照严格顺序完成合并运算的函数所需的最小PCE值</li>
<li>吞吐量界限： 刻画了处理器功能单元的原始计算能力，这个界限是程序性能的终极限制，给出了CPE的最小界限</li>
</ul>
<h4 id="5-7-3-处理器操作抽象模型"><a href="#5-7-3-处理器操作抽象模型" class="headerlink" title="5.7.3 处理器操作抽象模型"></a>5.7.3 处理器操作抽象模型</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.L25:</span><br><span class="line">	vmulsd	(%rdx), %xmm0, %xmm0</span><br><span class="line">	addq	$8	,%rdx</span><br><span class="line">	cmpq	%rax,	%rdx</span><br><span class="line">	jne		,L25</span><br></pre></td></tr></table></figure>

<p>数据流图:</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/31.png" class="">


<p>某些操作产生值可以不对应于寄存器，如load，cmp</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/32.png" class="">


<p>乘法路径为<strong>关键路径</strong>，延迟更长</p>
<h4 id="5-8-循环展开"><a href="#5-8-循环展开" class="headerlink" title="5.8 循环展开"></a>5.8 循环展开</h4><p>通过赞加每次迭代计算的元素的数量，减少循环的迭代次数</p>
<p>从两个反面提高程序性能：</p>
<ol>
<li>减少了不直接有助于程序结构的操作的数量，例如循环索引计算和条件分支</li>
<li>提供了一些方法，可以进一步变化代码，减少整个计算中关键路径上的操作数量</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">combine5</span><span class="params">(vec_ptr v, <span class="keyword">data_t</span> *dest)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> i;</span><br><span class="line">    <span class="keyword">long</span> length=vec_length(v);</span><br><span class="line">    <span class="keyword">long</span> limit=length<span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">data_t</span> *data=get_vec_start(v);</span><br><span class="line">    <span class="keyword">data_t</span> acc=IDENT;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;limit;i+=<span class="number">2</span>)&#123;</span><br><span class="line">        acc=(acc OP data[i]) OP data[i+<span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(;i&lt;length;i++)&#123;</span><br><span class="line">        acc=acc OP data[i];</span><br><span class="line">    &#125;</span><br><span class="line">    *dest=acc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="5-9-提高并行性"><a href="#5-9-提高并行性" class="headerlink" title="5.9 提高并行性"></a>5.9 提高并行性</h4><p>目前为止，代码还没有能够利用上功能单元在加法和乘法”流水线“的优势</p>
<p>原因：需要等待前一个周期算出结果才能开始下一个周期的运算，会有一个合并操作的延迟</p>
<h5 id="5-9-1-多个累计变量"><a href="#5-9-1-多个累计变量" class="headerlink" title="5.9.1 多个累计变量"></a>5.9.1 多个累计变量</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">combine6</span><span class="params">(vec_ptr v, <span class="keyword">data_t</span> *dest)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> i;</span><br><span class="line">    <span class="keyword">long</span> length=vec_length(v);</span><br><span class="line">    <span class="keyword">long</span> limit=length<span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">data_t</span> *data=get_vec_start(v);</span><br><span class="line">    <span class="keyword">data_t</span> acc0=IDENT;</span><br><span class="line">    <span class="keyword">data_t</span> acc1=IDENT;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;limit;i+=<span class="number">2</span>)&#123;</span><br><span class="line">        acc0=acc0 OP data[i];</span><br><span class="line">        acc1=acc1 OP data[i+<span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(;i&lt;length;i++)&#123;</span><br><span class="line">        acc0=acc0 OP data[i];</span><br><span class="line">    &#125;</span><br><span class="line">    *dest=acc0 OP acc1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>两个累计变量，一个算奇数乘积，一个算偶数乘积，最后将两者乘起来。</p>
<p>同时，还运用了循环展开，称为2*2的循环展开</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/33.png" class="">


<h5 id="5-9-2-重新结合变换"><a href="#5-9-2-重新结合变换" class="headerlink" title="5.9.2 重新结合变换"></a>5.9.2 重新结合变换</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//combine7</span></span><br><span class="line">acc=(acc OP data[i]) OP data[i+<span class="number">1</span>];</span><br><span class="line"><span class="comment">//变换为</span></span><br><span class="line">acc=acc OP (data[i] OP data[i+<span class="number">1</span>]);</span><br></pre></td></tr></table></figure>

<p>对应关键路径如下：</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/34.png" class="">


<p>每次迭代内的第一个乘法不需要等待上一个迭代的累计值就可以运行</p>
<h4 id="5-10-代码优化小结"><a href="#5-10-代码优化小结" class="headerlink" title="5.10 代码优化小结"></a>5.10 代码优化小结</h4><img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/35.png" class="">


<h4 id="5-11-一些限制因素"><a href="#5-11-一些限制因素" class="headerlink" title="5.11 一些限制因素"></a>5.11 一些限制因素</h4><p>关键路径指明执行程序所需时间基本下限</p>
<p>​    若程序中有某条完整的数据链，链上所有延迟之和为T，那么至少需要T个周期才能执行完</p>
<p>吞吐量指明了程序执行时间下限</p>
<p>​    N个某种运算的计算，C个执行单位，发射时间为I，则吞吐量下限为：N*I/C</p>
<h5 id="5-11-1-寄存器溢出"><a href="#5-11-1-寄存器溢出" class="headerlink" title="5.11.1 寄存器溢出"></a>5.11.1 寄存器溢出</h5><p>并行度过大，超过寄存器数量，这时候某些值就会被放到内存中去（通常在运行堆栈中），这时候由于存取过程，使用时间反而更长。</p>
<h5 id="5-11-2-分支预测和预测错误处罚"><a href="#5-11-2-分支预测和预测错误处罚" class="headerlink" title="5.11.2 分支预测和预测错误处罚"></a>5.11.2 分支预测和预测错误处罚</h5><p>分支预测并且预测错误，处理器必须丢弃所有投机执行的结果，在正确的位置重新开始取指令</p>
<h6 id="通用原则："><a href="#通用原则：" class="headerlink" title="通用原则："></a>通用原则：</h6><ol>
<li><p>不要过分关心可预测的分支</p>
<p>现代处理器的分支预测逻辑非常善于辨别不同的分支指令有规律的模式和长期的趋势，只要不是分支错误，通常不需要太多关注，例如合并函数结束循环的分支通常会被预测为选择分支，只在最后一次成为运算错误被处罚。</p>
</li>
<li><p>书写适合用条件传送实现的代码</p>
<p>使用功能式的风格实现函数，例如三元运算。</p>
</li>
</ol>
<p>命令式的程序风格：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//正常程序</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">minmax1</span><span class="params">(<span class="keyword">long</span> a[],<span class="keyword">long</span> b[],<span class="keyword">long</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">long</span> i;</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(a[i]&gt;b[i])&#123;</span><br><span class="line">            <span class="keyword">long</span> t=a[i];</span><br><span class="line">            a[i]=b[i];</span><br><span class="line">            b[i]=t;</span><br><span class="line">        &#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//命令式</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">minmax1</span><span class="params">(<span class="keyword">long</span> a[],<span class="keyword">long</span> b[],<span class="keyword">long</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">long</span> i;</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;n;i++)&#123;</span><br><span class="line">        <span class="keyword">long</span> min=a[i]&lt;b[i]?a[i]:b[i];</span><br><span class="line">        <span class="keyword">long</span> max=a[i]&lt;b[i]?b[i]:a[i];</span><br><span class="line">        a[i]=min;</span><br><span class="line">        b[i]=max;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="5-12-理解内存性能"><a href="#5-12-理解内存性能" class="headerlink" title="5.12 理解内存性能"></a>5.12 理解内存性能</h4><h5 id="5-12-1-加载的性能"><a href="#5-12-1-加载的性能" class="headerlink" title="5.12.1 加载的性能"></a>5.12.1 加载的性能</h5><p>所有的现代处理器都包含一个或多个高速缓存（cache）存储器，以对这样少量的存储器提供快速的访问。</p>
<p>我们知道，现代处理器有专门的功能单元来执行加载和存储操作，这些单元有内部的缓冲区来保存未完成的内存操作请求集合。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">ELE</span>&#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ELE</span> *<span class="title">next</span>;</span></span><br><span class="line">    <span class="keyword">long</span> data;</span><br><span class="line">&#125;list_ele, *list_ptr;</span><br><span class="line"><span class="function"><span class="keyword">long</span> <span class="title">list_len</span><span class="params">(list_ptr ls)</span></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> len=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(ls)&#123;</span><br><span class="line">		len++;</span><br><span class="line">        ls=ls-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> len;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//链表函数。其性能受限于加载操作的延迟</span></span><br></pre></td></tr></table></figure>

<p>直到上一次迭代完成，下一次迭代才能开始。函数的CPE完全由加载时间的延迟来决定。</p>
<h5 id="5-12-2-存储的性能"><a href="#5-12-2-存储的性能" class="headerlink" title="5.12.2 存储的性能"></a>5.12.2 存储的性能</h5><p>我们分析了大部分对存储器的引用都是<strong>加载操作</strong>的函数，也就是从存储位置读到寄存器中。</p>
<p>与之对应的是存储操作：它将一个寄存器写到存储器。这个操作的性能，尤其是与加载操作的相互关系，包括了一些很细微的问题。</p>
<p>写/读相关：一个存储器读的结果依赖于一个最近的存储器写。写/读相关导致处理速度下降。</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/36.png" class="">


<p>存储单元包含一个存储缓冲区，他包含<strong>已经被发射到存储单元</strong>而又<strong>没完成的存储操作的地址和数据</strong>，这里的完成包括更新数据高速缓存。提供这样一个缓冲区，使得一系列<strong>存储操作不必等待每个操作都更新高速缓存就能够执行</strong>。</p>
<p>当一个加载操作发生时，它必须<strong>检查存储缓冲区</strong>中的条目，看看有没有地址相匹配。如果有地址相匹配（在写的字节与在读的字节有相同的地址）它就取出相应的数据条目作为加载操作的结果。</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/37.png" class="">


<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.L3:</span><br><span class="line">	movq	%rax,	(%rsi)</span><br><span class="line">	movq 	(%rdi),	%rax</span><br><span class="line">	addq	$1	,	%rax</span><br><span class="line">	subq	$1	,	%rdx</span><br><span class="line">	jne		.L3</span><br></pre></td></tr></table></figure>

<p>s _addr操作的地址计算必须在s_data操作之前。此外 movq     (%rdi),    %rax 需要的load操作必须检查所有未完成的存储操作的地址，这个操作与s_addr操作形成了数据相关。</p>
<p>虚弧线表示这个相关是有条件的：</p>
<ol>
<li>当两个操作地址不同，那么两个操作可以独立运行；</li>
<li>当两个操作地址相同，load操作必须等到s_data操作完成并将结果放到存储缓冲区中。</li>
</ol>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/38.png" class="">


<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/39.png" class="">


<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/40.png" class="">


<h4 id="5-13-性能提高技术（算法）"><a href="#5-13-性能提高技术（算法）" class="headerlink" title="5.13 性能提高技术（算法）"></a>5.13 性能提高技术（算法）</h4><ol>
<li>高级设计。为遇到的问题选择合适的算法和数据结构。避免使用那些会渐进的产生糟糕性能的算法或编码技术。</li>
<li>基本编码原则。避免限制优化的因素，这样编译器就能产生高效的代码。<ol>
<li>消除连续的函数调用。在可能时，将计算移到循环外。考虑有选择的妥协程序的模块性来获得更大的效率。</li>
<li>消除不必要的存储器引用。引入临时变量来保存中间结果。</li>
</ol>
</li>
<li>低级优化<ol>
<li>展开循环，降低开销，并且使得进一步的优化成为可能</li>
<li>通过使用例如多个累计变量和重新组合等技术，找到提高指令级并行。</li>
<li>用功能的风格重写条件操作，使得编译采用条件数据传送。</li>
</ol>
</li>
</ol>
<h4 id="5-14-确认和消除性能瓶颈（结合硬件）"><a href="#5-14-确认和消除性能瓶颈（结合硬件）" class="headerlink" title="5.14 确认和消除性能瓶颈（结合硬件）"></a>5.14 确认和消除性能瓶颈（结合硬件）</h4><p>在处理大程序的时候很难定位需要优化的地方，就需要使用到代码剖析程序。</p>
<p>程序剖析可以在现实的基准数据上运行实际程序的同时，进行剖析。</p>
<p>剖析程序GPROF可以确定每个函数花费多少CPU时间。计算每个函数被调用的次数。</p>
<h2 id="第六章：存储器层次结构"><a href="#第六章：存储器层次结构" class="headerlink" title="第六章：存储器层次结构"></a>第六章：存储器层次结构</h2><p><strong>存储器系统</strong>是一个具有不同容量、成本和访问时间的存储设备的层次结构。CPU寄存器保存着最常用的数据。</p>
<p>靠近CPU的小的、快速的<strong>高速缓存存储器</strong>作为一部分存储在相对慢速的主存储器中数据和指令的缓冲区域。</p>
<p>主存缓存存储在容量较大的、慢速磁盘上的数据，而这些磁盘常常又作为存储在通过网络连接的其他机器的磁盘或磁带上的数据的缓冲区域。</p>
<h4 id="6-1-存储技术"><a href="#6-1-存储技术" class="headerlink" title="6.1 存储技术"></a>6.1 存储技术</h4><h5 id="6-1-1-随机访问存储器"><a href="#6-1-1-随机访问存储器" class="headerlink" title="6.1.1 随机访问存储器"></a>6.1.1 随机访问存储器</h5><p>分为静态RAM和动态RAM</p>
<ol>
<li>静态：双稳态，只要有电，数据不会变，抗干扰</li>
<li>动态：将数据存储为对电容的充电，对干扰非常敏感，周期性地刷新</li>
</ol>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/59.png" class="">

<p><strong>传统的DRAM</strong></p>
<p>​    DRAM芯片中的单元被分成d个<strong>超单元</strong>，每个超单元都由w个DRAM单元组成。一个d X w的DRAM总共存储了d*w位信息。超单元被组织成一个r行c列的长方形阵列，这里rc=d。每个超单元有形如（i，j）的地址。</p>
<p>​    信息通过称为引脚的外部连接器流入和流出芯片。</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/60.png" class="">


<p>内存模块：</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/61.png" class="">


<p>几种增强的DRAM：</p>
<ol>
<li>快页模式DRAM（FPM DRAM）：不舍弃同行其他列数据</li>
<li>扩展数据输出DRAM：比FPM DRAM更快（缩短了读取行和列数据的间隔）</li>
<li>同步DRAM：CPU发出的读取指令到达内存控制器和到达CPU芯片的时间几乎同步</li>
<li>双倍速率同步DRAM（DDR SDRAM）：通过提高有效带宽的很小的预取缓冲区的大小来划分的：DDR（2位），DDR2（4位），DDR4（8位）。</li>
<li>视频RAM：允许在同一时间被所有硬件访问</li>
</ol>
<p>非易失性存储器：ROM</p>
<ol>
<li>可编程ROM</li>
<li>可擦写ROM</li>
<li>闪存ROM</li>
</ol>
<p>存储在ROM中的程序称为固件（比如计算机主板上的基本输入/输出系统BIOS（Basic Input/output System)）</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/62.png" class="">


<h5 id="6-1-2-磁盘存储"><a href="#6-1-2-磁盘存储" class="headerlink" title="6.1.2 磁盘存储"></a>6.1.2 磁盘存储</h5><p><strong>磁盘构造</strong></p>
<p>磁盘由多个盘片（platter）构成，每个盘片都有两面。盘片表面覆盖着磁性材料用于记录信息，盘片中央有一个旋转主轴控制盘片以固定的旋转速率（rotational rate）旋转,旋转速度是磁盘性能的一个参数，通常为5400~15000转每分。</p>
<p>磁盘的每个盘面是由一组称之为磁道（track）同心圆构成，每个磁道又被划分为多个扇区（sector）。每一个扇区中包含相等的数据位（通常为512字节），数据编码在磁盘的磁性材料中。扇区之间有一些间隙（gap）分隔开，间隙中不存储数据位，间隙存储用来标识扇区的格式化位。</p>
<p>另外还可以用柱面（cylinder）来描述多个盘片驱动器的构造，柱面指的是所有盘片表面上到主轴中心距离相等的磁道集合。比如磁盘有3个盘片，6个面，每个面上距离中心距离相等的磁道称之为一个柱面。</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/63.png" class="">


<p><strong>磁盘容量</strong></p>
<p>一个磁盘上可以记录的最大位数称为它的最大容量，或者简称为容量。磁盘容量是由以下技术因素决定的：</p>
<ul>
<li>记录密度：磁道一英寸的段中可以放入的位数。</li>
<li>磁道密度：从盘片中心出发半径上一英寸的段内可以有的磁道数。</li>
<li>面密度：记录密度与磁道密度的乘积。</li>
</ul>
<p>假如某个磁盘有5个盘片，每个扇区512字节，每个面20000条磁道，每条磁道平均300个扇区，磁盘容量计算如下：</p>
<p>磁盘容量=512字节x300扇区x20000磁道x2表面x5盘片=30 720 000 000字节=30.72G</p>
<p><strong>磁盘操作</strong></p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/64.png" class="">


<ul>
<li>寻道时间：读/写头找到磁道的时间</li>
<li>旋转时间：从找到磁道开始到读/写头找到扇区的时间</li>
<li>传送时间：读/写头读写完整个扇区的时间</li>
</ul>
<p><strong>磁盘控制器与逻辑磁盘块</strong></p>
<p>一个B个扇区大小的逻辑块序列，编号为0~B-1。磁盘封装中有一个小的硬件、固件设备，称为磁盘控制器，维护着逻辑块号和实际（物理）磁盘扇区之间的映射关系。</p>
<p>当操作系统想要执行一个I/O操作时，例如读一个磁盘扇区的数据到主存，操作系统会发送一个命令到磁盘控制器，让它读某个逻辑块号。控制器上的固件执行一个快速表查找，将一个逻辑块号翻译成一个（盘面，磁道，扇区）的三元组，这个三元组唯一标识了对应的物理扇区。控制器上的硬件会解释这个三元组，将读/写头移动到适当的柱面，等待扇区移动到读/写头下，将读/写头感知到的位放到控制器上的一个小缓冲区中，然后将它们复制到主存中。</p>
<p><strong>连接IO设备</strong></p>
<ol>
<li>通用串行总线</li>
<li>图形卡</li>
<li>主机总线适配器</li>
</ol>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/65.png" class="">


<p><strong>访问磁盘</strong>：</p>
<p>地址空间有一块IO设备通信用的地址</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/66.png" class="">


<h5 id="6-1-3-固态硬盘"><a href="#6-1-3-固态硬盘" class="headerlink" title="6.1.3 固态硬盘"></a>6.1.3 固态硬盘</h5><p>固态硬盘（SSD）是一种基于闪存的存储技术。</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/67.png" class="">


<p>SSD：读比写更快。</p>
<p>一个闪存由B个块组成，一个块有P页。数据是以页为单位读写的，只有一页所属的块整个背擦除后，才能写这一页。不过一旦一个块被擦除了，块中每一个页都可以不需要再进行擦除就写一次。闪存块会在擦除过程中磨损。</p>
<p>随机访问很快，因为SSD是由半导体存储器构成，没有移动的部件，所以随机访问的时间比旋转磁盘要快，能耗更低。</p>
<p>随机写很慢，有两个原因。首先擦除块需要相对较长的时间。其次，如果写操作试图修改一个包含已经有数据的页p,那么这个块中的其他有用数据都要被复制到一个新的块中，然后才能对这个块重写。</p>
<h5 id="6-1-4-存储技术趋势"><a href="#6-1-4-存储技术趋势" class="headerlink" title="6.1.4 存储技术趋势"></a>6.1.4 存储技术趋势</h5><ol>
<li>不同的存储技术有不同价格和性能这种</li>
<li>不同存储技术的价格和性能属性以截然不同的速率变化着</li>
<li>REAM和磁盘的性能滞后于CPU的性能</li>
</ol>
<h4 id="6-2-局部性"><a href="#6-2-局部性" class="headerlink" title="6.2 局部性"></a>6.2 局部性</h4><p>一个编写良好的计算机程序常常具有良好的局部性。也就是，它们倾向于引用邻近于其他最近引用过的数据的数据项，或者最近引用过的数据项本身。这种倾向性，被称为<strong>局部性原理</strong>。</p>
<p>局部性原理有两种：<strong>时间局部性</strong>和<strong>空间局部性</strong></p>
<p>在一个具有良好时间局部性的程序中，被引用过一次的内存位置很可能在不远的将来再被多次引用。在一个具有良好的空间局部性的程序中，如果一个内存位置被引用了一次，那么程序很可能在不远的将来引用附近的一个内存位置。</p>
<h5 id="6-2-1-对程序数据引用的局部性"><a href="#6-2-1-对程序数据引用的局部性" class="headerlink" title="6.2.1 对程序数据引用的局部性"></a>6.2.1 对程序数据引用的局部性</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sumarrayows</span><span class="params">(<span class="keyword">int</span> a[M][N])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i, j, sum = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;M;i++)&#123;</span><br><span class="line">		<span class="keyword">for</span>(j=<span class="number">0</span>;j&lt;N;j++)&#123;</span><br><span class="line">            sum += a[i][j];</span><br><span class="line">		&#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面就是一个具有良好局部性的程序，因为是按照数组元素在内存中的存储顺序来访问他们。</p>
<p>这种步长（两次内存访问位置之间的距离）为1的引用模式被称为顺序引用模式。</p>
<p>如果将i和j位置互换，则空间局部性就会变得很差。</p>
<h5 id="6-2-2-取指令的局部性"><a href="#6-2-2-取指令的局部性" class="headerlink" title="6.2.2 取指令的局部性"></a>6.2.2 取指令的局部性</h5><p>因为程序指令是存放在内存中的，CPU必须取出这些指令，所以我们能够评价一个程序关于取指令的局部性。for循环里的指令是按照连续的内存顺序执行的，因此循环具有良好的空间局部性。</p>
<h5 id="6-2-3-局部性小结"><a href="#6-2-3-局部性小结" class="headerlink" title="6.2.3 局部性小结"></a>6.2.3 局部性小结</h5><ul>
<li>重复引用相同变量的程序具有良好的局部性。</li>
<li>对于具有步长为k的引用模式的程序，步长越小，空间局部性越好。</li>
<li>对于取指令来说，循环具有良好的时间和空间局部性。循环体越小，循环迭代次数越多，局部性越好。</li>
</ul>
<h4 id="6-3-存储器层次结构"><a href="#6-3-存储器层次结构" class="headerlink" title="6.3 存储器层次结构"></a>6.3 存储器层次结构</h4><p>基于两个事实：</p>
<ol>
<li>不同存储技术的访问时间差很大，CPU和主存之间的速度差距在增大。</li>
<li>良好的程序具有良好的局部性。</li>
</ol>
<p>人们想到一种组织存储器系统的方法：存储器层次结构。</p>
<h5 id="6-3-1-存储器结构中的缓存"><a href="#6-3-1-存储器结构中的缓存" class="headerlink" title="6.3.1 存储器结构中的缓存"></a>6.3.1 存储器结构中的缓存</h5><p>存储器层次结构的中心思想，对于每个k，位于k层的更快更小的存储设备作为位于k+1层的更大更慢的存储设备的缓存。依次类推，直到最小的缓存：CPU寄存器组。</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/68.png" class="">


<ol>
<li><p>缓存命中</p>
<p>当程序需要第k+1层的某个对象d时，它首先在当前存储在第k层的一个块中查找d。如果d刚好缓存在第k层中，就是我们所说的缓存命中。例如，一个有良好时间局部性的程序可以从块14中读出一个数据对象，得到一个对k层的缓存命中。</p>
</li>
<li><p>缓存不命中</p>
<p>另一方面，如果第k层中没有缓存对象d，那么就是我们所说的缓存不命中。当发生缓存不命中时，第k层的缓存从第k+1层缓存中取出包含d的那个块，如果第k层的缓存已经满了，可能就会覆盖现存的一个块。</p>
<p>覆盖一个现存的块的过程称为<strong>替换</strong>或者<strong>驱逐</strong>这个块。被驱逐的这个块有时也被称为<strong>牺牲块</strong>。决定该替换哪个块时由缓存的<strong>替换策略</strong>来控制的。</p>
</li>
<li><p>缓存不命中的种类</p>
<ul>
<li><p><strong>强制性不命中/冷不命中</strong></p>
<p>第k层缓存是空的则这一层叫<strong>冷缓存</strong>，如果空的缓存存入了一个数据，就叫冷缓存的<strong>暖身</strong>。</p>
<p>硬件缓存往往使用很严格的放置策略，这个策略将k+1层的某个块限制在第k层块的一个小的子集中。如上图第k+1层的0、4、8、12会映射到第k层的块0。设置一个第k+1层的块i必须放在第k层的块i mod 4中这样的放置策略。</p>
</li>
<li><p><strong>冲突不命中</strong>：映射的缓存块一样。</p>
</li>
<li><p>容量不命中：程序通常是按照一系列阶段（如循环）来运行的，每个阶段访问缓存块的某个相对稳定不变的集合称为这个阶段的<strong>工作集</strong>。当工作集的大小超过缓存的大小时，缓存会经历<strong>容量不命中</strong>。换句话说，就是缓存小了，不能处理这个工作集。</p>
</li>
</ul>
</li>
</ol>
<h4 id="6-4-高速缓存存储器"><a href="#6-4-高速缓存存储器" class="headerlink" title="6.4 高速缓存存储器"></a>6.4 高速缓存存储器</h4><p>早期计算机系统的存储器层次结构只有三层：CPU寄存器，DRAM主存储器和磁盘存储</p>
<p>由于CPU和主存之间逐渐增大的差距。系统设计者被迫在CPU寄存器文件和主存之间插入了一个小的<strong>SRAM高速缓存寄存器</strong>，称为<strong>L1高速缓存（一级缓存）</strong>。</p>
<h5 id="6-4-1-通用的高速缓存存储器组织结构"><a href="#6-4-1-通用的高速缓存存储器组织结构" class="headerlink" title="6.4.1 通用的高速缓存存储器组织结构"></a>6.4.1 通用的高速缓存存储器组织结构</h5><p>将一个存储器以字节为单位划分为S个组， E个高速缓存行， B个字节数据块；<br>在一个高速缓存行中，有一个<strong>有效位</strong>指明这行数据是否具有意义， 还有t = m-(b+s) 个标记位，指明当前存储在这个行中的块的身份。（例如， 上一层的块0， 4， 8， 12）都可以存在这一层的块0上，标志位指出具体是哪一块存在这里）；<br>高速缓存结构使用（S,E, B, m）来描述。 高速缓存的大小C是所有块的综合 = S×E×B；<br>当一条加载指令指示CPU从主存地址A读取一个字，他将这个地址发送给高速缓存；<br>参数S， B将地址分为三部分。<strong>组索引</strong>指明在哪一个组S； 然后在这个组中找寻与标记相匹配的那一个<strong>高速缓存行E</strong>，最后根据<strong>块偏移</strong>在高速缓存行中找到具体的字。</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/69.png" class="">


<h5 id="6-4-2-直接映射高速缓存"><a href="#6-4-2-直接映射高速缓存" class="headerlink" title="6.4.2 直接映射高速缓存"></a>6.4.2 直接映射高速缓存</h5><p>每个组中只含有一个高速缓存行的结构， 称为直接映射高速缓存。</p>
<ol>
<li><p>直接映射高速缓存中的组选择</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/70.png" class=""></li>
</ol>
<ol start="2">
<li><p>直接映射高速缓存中的行匹配</p>
</li>
<li><p>直接映射高速缓存中的字选择</p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/71.png" class=""></li>
</ol>
<ol start="4">
<li><p>直接映射高速缓存中的行替换</p>
<p>如果缓存不命中，那么它需要从存储器层次结构中的下一层取出被请求的块，然后将新的块存储在组索引位指示的组中的一个高速缓存行中。对于高速映射缓存来说，每个组只包含有一行，替换策略非常简单：用新取出的行代替当前的行。</p>
</li>
<li><p>综合：运行中的直接映射高速缓存</p>
<ul>
<li>标记位和索引位连起来唯一地标识了内存中的每个块。</li>
<li>因为有8个内存块，但是只有4个高速缓存，所以多个块会映射到同一个高速缓存。</li>
<li>映射到同一个高速缓存的块由标记位唯一地标识</li>
</ul>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/73.jpg" class=""></li>
</ol>
<h5 id="6-4-3-组相联高速缓存"><a href="#6-4-3-组相联高速缓存" class="headerlink" title="6.4.3 组相联高速缓存"></a>6.4.3 组相联高速缓存</h5><p>直接映射高速缓存中冲突不命中造成的问题源于每个组只有一行。组相联高速缓存放松了这个限制。每个组有E行的高速缓存通常称为E路高速缓存。</p>
<p><strong>组相联高速缓存中的组选择</strong></p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/74.jpg" class="">


<p><strong>组相联高速缓存中的行匹配和字选择</strong></p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/75.jpg" class="">


<p><strong>组相联高速缓存不命中时的行替换</strong></p>
<p>在缓存不命中时，高速缓存必须将内存中取出这个字的块然后进行行替换，如果有空行直接替换空行。</p>
<p>如果没有，则运用一些复杂的策略（利用局部性原理）：<strong>最不常使用策略</strong>和<strong>最近少使用策略</strong>进行行替换。</p>
<h5 id="6-4-4-全相联高速缓存"><a href="#6-4-4-全相联高速缓存" class="headerlink" title="6.4.4 全相联高速缓存"></a>6.4.4 全相联高速缓存</h5><img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/76.png" class="">


<p><strong>组相联高速缓存中的行匹配和字选择</strong></p>
<img src="/2021/05/06/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%B3%BB%E7%BB%9F%E4%BA%8C/77.jpg" class="">


<h5 id="6-4-5-有关写的问题"><a href="#6-4-5-有关写的问题" class="headerlink" title="6.4.5 有关写的问题"></a>6.4.5 有关写的问题</h5><p>假设我们要写一个已经缓存了的字w（写命中），在高速缓存更新了它的w的副本之后，怎么更新w在层次结构中紧接着低一层的副本？</p>
<p><strong>直写</strong>：就是立即将w的高速缓存块写回到紧接着的低一层中。虽然简单，但是每次写都会引起总线流量</p>
<p><strong>写回</strong>：尽可能地推迟更新，只有当替换算法要驱逐这个块时，才把它写到紧接着的低一层中。由于局部性，写回能显著地减少总线流量，但是缺点是增加了复杂性。高速缓存必须为每个高速缓存行维护一个额外的修改位，表明这个高速缓存块是否被修改过。</p>
<p>如何处理写不命中？</p>
<p><strong>写分配</strong>：加载相应的低一层中的块到高速缓存，然后更新这个高速缓存块</p>
<p><strong>非写分配</strong>：避开高速缓存，直接将这个字写到低一层中。</p>
<p>直写高速缓存通常是非写分配的。写回高速缓存通常是写分配的</p>
<h5 id="6-4-6-高速缓存参数的性能影响"><a href="#6-4-6-高速缓存参数的性能影响" class="headerlink" title="6.4.6 高速缓存参数的性能影响"></a>6.4.6 高速缓存参数的性能影响</h5><ul>
<li>不命中率：在一个程序执行或程序的一部分执行期间，内存引用不命中的比率。不命中数量/引用数量</li>
<li>命中率：1-不命中率</li>
<li>命中时间：从高速缓存传送一个字到CPU的时间，包括组选择、行确认、字选择的时间。</li>
<li>不命中处罚：由于不命中所需要的额外的时间。</li>
</ul>
<ol>
<li><p><strong>高速缓存大小的影响</strong></p>
<p>一方面：较大的高速缓存可能提高命中率。</p>
<p>另一方面：较大的高速缓存可能增加命中时间。</p>
</li>
<li><p><strong>块大小的影响</strong></p>
<p>一方面：较大的块能利用程序可能存在的空间局部性，提高命中率。</p>
<p>另一方面：块越大，高速缓存行越小，这会损害时间局部性比空间局部性好的程序的命中率。</p>
</li>
<li><p><strong>相联度的影响</strong></p>
<p>高的相联度降低了高速缓存由于出现冲突不命中出现抖动的可能性，但会造成更高的成本，还会增加命中时间和不命中处罚。</p>
</li>
<li><p><strong>写策略的影响</strong></p>
<p>直写高速缓存比较容易实现，而且能使用独立于高速缓存的写缓冲区，用来更新内存。</p>
<p>写回引起的传送少。</p>
</li>
</ol>
<h4 id="6-5-编写高速缓存友好的代码"><a href="#6-5-编写高速缓存友好的代码" class="headerlink" title="6.5 编写高速缓存友好的代码"></a>6.5 编写高速缓存友好的代码</h4><p>核心在于利用局部性。</p>
<ol>
<li>让最常见的情况运行得快。</li>
<li>尽量减小循环内部的缓冲不命中数量</li>
</ol>
]]></content>
      <categories>
        <category>CSAPP</category>
      </categories>
  </entry>
  <entry>
    <title>物理层</title>
    <url>/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/</url>
    <content><![CDATA[<h2 id="第二章"><a href="#第二章" class="headerlink" title="第二章"></a>第二章</h2><h4 id="2-1-1-物理层的基本概念"><a href="#2-1-1-物理层的基本概念" class="headerlink" title="2.1.1 物理层的基本概念"></a>2.1.1 物理层的基本概念</h4><h5 id="基本概念："><a href="#基本概念：" class="headerlink" title="基本概念："></a>基本概念：</h5><p>​        物理层解决如何在连接各种计算机的传输媒体上<strong>传输数据比特流</strong>，而不是指具体的传输媒体。</p>
<p>​        物理层的主要任务：确定与传输媒体<strong>接口</strong>有关的一些特性。=》定义标准</p>
<ol>
<li><p>机械特性：定义物理连接的特性，规定物理连接时所采用的<strong>规格</strong>、<strong>接口形状</strong>、<strong>引线数目</strong>、<strong>引脚数量</strong>和<strong>排列情况</strong>。</p>
</li>
<li><p>电气特性：规定传输二进制位时，线路上的信号的<strong>电压范围</strong>、<strong>阻抗匹配</strong>、<strong>传输速率</strong>、<strong>距离限制</strong>等。</p>
</li>
<li><p>功能特性：指明某条线上出现的<strong>某一电平表示何种意义</strong>，接口部件的信号线的用途。</p>
<p>例如：描述一个物理层接口引脚处于高电平时的含义等。</p>
</li>
<li><p>规程特性：（过程特性）定义各条物理线路的工作规程和时序的关系。</p>
</li>
</ol>
<h4 id="2-1-2-数据通信基础知识"><a href="#2-1-2-数据通信基础知识" class="headerlink" title="2.1.2 数据通信基础知识"></a>2.1.2 数据通信基础知识</h4><h5 id="典型的数据通信模型"><a href="#典型的数据通信模型" class="headerlink" title="典型的数据通信模型"></a>典型的数据通信模型</h5><img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/14.png" class="">

<p>源系统：信源，发送器</p>
<p>传输系统：传输系统</p>
<p>目的系统：接收器，信宿</p>
<h5 id="数据通信相关术语"><a href="#数据通信相关术语" class="headerlink" title="数据通信相关术语"></a>数据通信相关术语</h5><p>通信的目的是<strong>传送信息</strong>。</p>
<p>数据：传送信息的实体，通常是有意义的符号序列</p>
<p>信号：数据的电气/电磁的表现，是数据在传输过程中的存在形式。</p>
<p>​    数字信号：代表消息的参数取值是离散的。</p>
<p>​    模拟信号：代表消息的参数取值是连续的。</p>
<p>信源：产生和发送数据的源头。</p>
<p>信宿：接收数据的终点。</p>
<p>信道：信号的传输媒介。一般用来表示向某一个方向传送信息的介质，因此一条通信线路往往包含一条发送信道和接收信道。</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/15.png" class="">

<h5 id="三种通信方式"><a href="#三种通信方式" class="headerlink" title="三种通信方式"></a>三种通信方式</h5><p>从通信双方信息的交互方式来看，可以有三种基本方式：</p>
<ol>
<li>单工通信：只有一个方向的通信而没有反方向的交互，仅需要一条信道</li>
<li>半双工通信：通信的双方都可以发送或接收消息，但任何一方都不能同时发送和接收，需要两条信道</li>
<li>全双工通信：通信双方可以同时发送和接受信息，也需要两条信道 </li>
</ol>
<h5 id="两种数据传输方式"><a href="#两种数据传输方式" class="headerlink" title="两种数据传输方式"></a>两种数据传输方式</h5><p>传输方式：串行传输，并行传输</p>
<p>​    串行传输：速度慢，费用低，适合远距离</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/16.png" class="">

<p>​    并行传输：速度快，费用高，适合近距离（常用于计算机内部的数据传输）</p>
<img src="C:/Users/86189/blog/source/_posts/17.png"  />

<h4 id="2-1-3-码元、波特、速率、带宽"><a href="#2-1-3-码元、波特、速率、带宽" class="headerlink" title="2.1.3 码元、波特、速率、带宽"></a>2.1.3 码元、波特、速率、带宽</h4><h5 id="码元"><a href="#码元" class="headerlink" title="码元"></a>码元</h5><p>​    指用一个固定时长的信号波形（数字脉冲），代表不同离散值的基本波形，是数字通信中数字信号的计量单位，这个时长内的信号称为k进制码元，而该时长称为码元宽度。当码元的离散状态有M个时（M大于2），此时码元为M进制码元。</p>
<p>​    1码元可以携带多个比特的信息量。</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/18.png" class="">

<p>K进制码元——4进制码元=&gt;码元的离散状态有4个=&gt;4中高低不同的信号波形    00、01、10、11    此时一个码元代表2比特内容</p>
<h5 id="速率、波特、带宽"><a href="#速率、波特、带宽" class="headerlink" title="速率、波特、带宽"></a>速率、波特、带宽</h5><p><strong>速率</strong>也叫数据率，是指数据的传输速率，表示单位时间内传输的数据量。可以用<strong>码元传输速率</strong>和<strong>信息传输速率</strong>表示。</p>
<ol>
<li><strong>码元传输速率</strong>：别名码元速率、波形速率、调制速率、符号速率等。他表示单位时间内数字通信系统所传输的码元个数（也可称为<strong>脉冲个数</strong>或<strong>信号变化的次数</strong>），单位试试<strong>波特（Baud）</strong>。1波特表示数字通信系统每秒传输一个码元。这里的码元可以是多进制的，也可以是二进制的，但码元速率与进制数无关。</li>
<li><strong>信息传输速率</strong>：别名信息速率、比特率等，表示单位时间内数字通信系统传输的<strong>二进制码元数</strong>（即<strong>比特数</strong>），单位是比特/秒（b/s）</li>
</ol>
<p>关系：</p>
<p>​    若一个码元携带n bit的信息量，则M Baud的<strong>码元传输速率</strong>所对应的<strong>信息传输速率</strong>为M*n bit/s。</p>
<p><strong>带宽</strong>：表示单位时间内从网络中的某一点到另一点所能通过的“<strong>最高数据率</strong>”，常用来表示网络的通信线路所能传输数据的能力。单位b/s。</p>
<h4 id="2-1-4-奈氏准则和香农定理"><a href="#2-1-4-奈氏准则和香农定理" class="headerlink" title="2.1.4 奈氏准则和香农定理"></a>2.1.4 奈氏准则和香农定理</h4><h5 id="失真："><a href="#失真：" class="headerlink" title="失真："></a>失真：</h5><img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/19.png" class="">

<p>影响失真程度的因素：</p>
<ol>
<li>码元传输速率</li>
<li>信号传输距离</li>
<li>噪声干扰</li>
<li>传输媒体质量</li>
</ol>
<h5 id="失真的一种现象–码间串扰"><a href="#失真的一种现象–码间串扰" class="headerlink" title="失真的一种现象–码间串扰"></a>失真的一种现象–码间串扰</h5><img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/20.png" class="">

<p>信号带宽是信道能通过的<strong>最高频率</strong>和<strong>最低频率</strong>之差</p>
<p><strong>码间串扰</strong>：<strong>接收端</strong>收到的信号波形失去了码元之间清晰界限的现象</p>
<h5 id="奈式准则："><a href="#奈式准则：" class="headerlink" title="奈式准则："></a><strong>奈式准则</strong>：</h5><p>​    在理想低通（无噪声，宽带受限）条件下，为了避免码间串扰，<strong>极限码元传输速率</strong>为2W Baud， W是信道带宽，单位是Hz。</p>
<p><strong>极限数据传输率</strong> :</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/21.png" class="">

<p>​    传输速率慢：那么我们可以清晰识别每一个码元。</p>
<p>​    传输速率快：因为码间串扰，所有不能清晰识别。</p>
<ol>
<li>信道的频带越宽（即通过的信号高频分量越多），就可以用更高的速率进行码元的有效传输。</li>
<li>奈式准则给出了马原传输速率的限制，但并没有对信息传输速率给出限制。</li>
<li>由于码元传输速率受到奈氏准则的制约，所以要提高数据的传输速率，就必须设法使每个码元都能携带更多比特的信息量，这就需要多元制的调制方法。</li>
</ol>
<h5 id="香农定理："><a href="#香农定理：" class="headerlink" title="香农定理："></a>香农定理：</h5><p>​    在带宽受限且有噪声的信道中，为了不产生误差，信息的数据传输速率有上限值。</p>
<p><strong>噪声</strong>：存在于所有的电子设备和通信信道中。由于噪声<strong>随机产生</strong>，它的瞬时值有时会很大，因此噪声会使接收端对码元的判决产生错误。但是噪声的影响是相对的，若信号较强，那么噪声的影响就相对较小。因此，<strong>信噪比</strong>就很重要。</p>
<p>信噪比=信号的平均功率/噪声的平均功率。常记作S/N，并用分贝（dB）作为度量单位，即：</p>
<p>​    信噪比（dB）= 10log10（S/N）</p>
<p>信道的极限数据传输速率：</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/22.png" class="">

<ol>
<li>信道的带宽或者信道中的信噪比越大，则信息的极限传输速率越高。</li>
<li>对一定的传输带宽和一定打的信噪比，信息传输速率的上限就确定了。</li>
<li>只要信息的传输速率低于信道的极限传输速率，就一定能找到某种方法来实现无差错的传输。</li>
<li>香农定理得出的为极限信息传输速率，实际信道能达到的传输速率要比它低不少。</li>
<li>从香农定理可以看出，若信道带宽W或信噪比S/N没有上限（不可能），那么信道的极限信息传输速率也就没有上限。</li>
</ol>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/23.png" class="">

<h4 id="2-1-5-编码与调制"><a href="#2-1-5-编码与调制" class="headerlink" title="2.1.5 编码与调制"></a>2.1.5 编码与调制</h4><h5 id="基带信号与宽带信号"><a href="#基带信号与宽带信号" class="headerlink" title="基带信号与宽带信号"></a>基带信号与宽带信号</h5><p>信道：信号的传输媒介。一般用来表示向某一个方向传送信息的介质，因此一条信道线路往往包含一条发送信道和一条接收信道。</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/24.png" class="">

<p>信道上传输的信号：</p>
<ul>
<li><p>基带信号</p>
<p>将数字信号1和0直接用两种不同的电压表示，再送到数字信道上去传输（基带传输）。<strong>来自信源的信号</strong>，像计算机输出的代表各种文字或图像文件的数据信号都属于基带信号。基带信号就是<strong>发出了直接表达要传输的信息的信号</strong>。</p>
</li>
<li><p>宽带信号</p>
<p>将基带信号进行调制后形成的频分复用模拟信号，再传送到<strong>模拟信道</strong>上去传输（宽带传输）。把基带信号经过<strong>载波调制</strong>后，把信号的频率范围搬移到较高的频段以便于再信道中传播。</p>
</li>
</ul>
<p>传输距离较近时，采用基带传输方式</p>
<p>传输距离较近时，采用宽带传输方式</p>
<h5 id="编码与调制"><a href="#编码与调制" class="headerlink" title="编码与调制"></a>编码与调制</h5><img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/25.png" class="">

<h6 id="数字数据编码为数字信号"><a href="#数字数据编码为数字信号" class="headerlink" title="数字数据编码为数字信号"></a>数字数据编码为数字信号</h6><img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/26.png" class="">

<ol>
<li><p><strong>非归零编码（NRZ）</strong></p>
<p>高1低0：编码容易实现，但没有检错功能，且无法判断一个码元的开始和结束，以至于收发双方<strong>难以保持同步</strong></p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/27.png" class=""></li>
<li><p><strong>曼彻斯特编码</strong></p>
<p>将一个码元分成两个相等的间隔，前一个间隔为低电平后一个间隔为高电平表示码元1；码元0正好相反。也可以采用相反的规定。该编码的特点是在每一个码元的中间出现电平跳变，位中间的跳变<strong>即作时钟信号，又作数据信号</strong>，但它所占的频带宽度是原始基带宽度的两倍。</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/30.png" class="">

<p>​    每一个码元都被调成两个电平，数据传输速率只有<strong>调制速率</strong>（码元传输速率）的1/2。</p>
</li>
<li><p><strong>差分曼彻斯特编码</strong></p>
<p>同1异0</p>
<p>若码元为1，则前半个码元的电平与上一个码元后半个码元的电平相同。若为0，则相反。特点是：在每个码元的中间，都有一次电平的跳转，可以实现自同步，且抗干扰性强于曼彻斯特编码。</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/31.png" class=""></li>
<li><p>归零编码（RZ）</p>
<p>信号电平在一个码元之内都要恢复到零的这种编码方式</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/28.png" class=""></li>
<li><p>反向不归零编码（NRZI）</p>
<p>信号电平翻转表示0，信号电平不变表示1</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/29.png" class=""></li>
<li><p>4B/5B编码</p>
<p>比特流中插入额外的比特以打破一连串的0或1，就是5个比特来编码4个比特的数据，之后再传给接收方，因此称为4B/5B。编码效率是80%。</p>
<p>只采用16钟不同的4位码，其他的16种作为控制码或保留。</p>
</li>
</ol>
<h6 id="数字数字调制为模拟信号"><a href="#数字数字调制为模拟信号" class="headerlink" title="数字数字调制为模拟信号"></a>数字数字调制为模拟信号</h6><p>数字数据调制技术在发送端将数字信号转换为模拟信号，而在接收端将模拟信号还原为数字信号，分别对应于调制解调器的调制和解调过程。</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/32.png" class="">

<p>调幅+调相（QAM）</p>
<h6 id="模拟数据编码为数字信号"><a href="#模拟数据编码为数字信号" class="headerlink" title="模拟数据编码为数字信号"></a>模拟数据编码为数字信号</h6><p>计算机内部处理的是二进制数据，处理的都是数字音频，所有需要将模拟音频通过采样、量化转化成有限个数字表示的离散序列（即实现音频数字化）</p>
<p>最典型的例子就是对音频信号进行编码的<strong>脉码调制（PCM）</strong>，在计算机应用种，能够达到最高保真水平的就是PCM编码，被广泛用于素材保存及音乐欣赏，CD、DVD以及我们常见的WAV文件中均有应用。它主要包括三步：<strong>抽样、量化、编码</strong></p>
<ol>
<li><p>抽样：对模拟信号周期性扫描，把时间上连续的信号变成时间上离散的信号。</p>
<p>采样定理：<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/33.png" class=""></p>
</li>
<li><p>量化：把抽样取得的电平幅值按照一定的分级标度转化为对应的数字值，并取整数，这就把连续的电平幅值转化为离散的数字量。</p>
</li>
<li><p>编码：把量化的结果转化为与之对应的二进制编码</p>
</li>
</ol>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/34.png" class="">

<h6 id="模拟数据调制为模拟信号"><a href="#模拟数据调制为模拟信号" class="headerlink" title="模拟数据调制为模拟信号"></a>模拟数据调制为模拟信号</h6><p>为了实现传输有效性，可能需要较高的频率。这种调制方式还可以使用频分复用技术，充分利用带宽资源。在电话机和本地交换机所传输的信号是采用模拟信号传输模拟数据的方式：模拟的声音数据是加载到模拟的载波信号中传输的。</p>
<h5 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h5><img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/35.png" class="">

<h4 id="2-2-物理层传输介质"><a href="#2-2-物理层传输介质" class="headerlink" title="2.2 物理层传输介质"></a>2.2 物理层传输介质</h4><h5 id="传输介质及分类"><a href="#传输介质及分类" class="headerlink" title="传输介质及分类"></a>传输介质及分类</h5><p>​    传输介质也称传输媒体/传输媒介，它就是数据传输系统中在发送设备和接收设备之间的<strong>物理通路</strong>。</p>
<p>传输媒体并不是物理层：</p>
<p>传输媒体在物理层的下面，因为物理层是体系结构的第一层，因此有时称传输媒体为0层。在传输媒体中传输的是信号，但传输媒体并不知道所传输的信号代表什么意思。但物理层规定了<strong>电气特性</strong>，因此能够识别所传送的比特流。</p>
<p>传输介质：</p>
<ul>
<li>导向性传输介质-&gt;电磁波被导向固体媒介（铜线/光纤）传播。</li>
<li>非导向性传输介质-&gt;自由空间，介质可以是空气、真空、海水等。</li>
</ul>
<h5 id="导向性传输介质–1-双绞线"><a href="#导向性传输介质–1-双绞线" class="headerlink" title="导向性传输介质–1.双绞线"></a>导向性传输介质–1.双绞线</h5><p>双绞线是古老、又最常用的传输介质，它由<strong>两根</strong>采用一定规则<strong>并排绞合</strong>的、<strong>相互绝缘</strong>的<strong>铜导线</strong>组成。</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/36.png" class="">

<p>绞合可以减少相邻导线的电磁干扰。</p>
<p>为了进一波提高抗电磁干扰能力，可在双绞线的外面再加上一个由金属丝编织成的屏蔽层，这就是<strong>屏蔽双绞线（SIP）</strong>，无屏蔽层的双绞线就称为<strong>非屏蔽双绞线（UTP）</strong>。</p>
<p>特点：</p>
<ul>
<li>双绞线价格便宜，是最常用的传输介质之一，在局域网和传统电话网中普遍使用。</li>
<li>模拟传输和数字传输都可以使用双绞线，其通信距离一般为几公里到十公里。</li>
<li>距离太远时，对于<strong>模拟传输</strong>，要用<strong>放大器</strong>放大衰减的信号；对于<strong>数字传输</strong>，要用<strong>中继器</strong>将失真的信号整形。</li>
</ul>
<h5 id="导向性传输介质–2-同轴电缆"><a href="#导向性传输介质–2-同轴电缆" class="headerlink" title="导向性传输介质–2.同轴电缆"></a>导向性传输介质–2.同轴电缆</h5><p>同轴电缆由导体<strong>铜质芯线</strong>、<strong>绝缘层</strong>、<strong>网状编织屏蔽层</strong>和<strong>塑料外层</strong>构成。按特性阻抗数值的不同，通常将同轴电缆分为两类；50Ω同轴电缆和75Ω同轴电缆。其中，50Ω同轴电缆主要用于传送基带数字信号，又称为<strong>基带同轴电缆</strong>，它在局域网中得到广泛应用；75Ω同轴电缆主要用于传送宽带信号，又称为<strong>宽带同轴电缆</strong>，它主要用于有线电视系统。</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/37.png" class="">

<p>由于外导体屏蔽层的作用，同轴电缆<strong>抗干扰特性</strong>比双绞线好，被广泛用于传输较高速率的数据，其传输距离更远，但价格比双绞线贵。</p>
<h5 id="导向性传输介质–3-光纤"><a href="#导向性传输介质–3-光纤" class="headerlink" title="导向性传输介质–3.光纤"></a>导向性传输介质–3.光纤</h5><p>光纤通信就是利用光导纤维（简称光纤）传递<strong>光脉冲</strong>来进行通信。有光脉冲为1，无光脉冲为0.而可见光的频率大约为<strong>10的8次方MHz</strong>，因此光纤通信系统的<strong>带宽远远大于</strong>目前其他各种传输媒体的带宽。</p>
<p>光纤主要由<strong>纤芯</strong>（<strong>实心的！</strong>）和<strong>包层</strong>构成，光波通过纤芯进行传导，包层较纤芯有较低的折射率。当光线从高折射率的介质射向低折射率的介质时，其折射角将大于入射角。因此，当入射角够大就会出现<strong>全反射</strong>，即光线碰到包层时候就会折射回纤芯、这个过程不断重复，光也就沿着光纤传输下去。</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/38.png" class="">

<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/39.png" class="">

<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/40.png" class="">

<p>光缆：一根光缆少则只有一根光纤，多则包括十到数百根光纤。</p>
<p>光纤的特点：</p>
<ol>
<li>传输<strong>损耗小</strong>，中继距离长，对远距离传输特别经济</li>
<li><strong>抗雷电和电磁干扰性能好</strong></li>
<li>无串音干扰，保密性好，也<strong>不易被窃听或截取数据</strong></li>
<li><strong>体积小，重量轻。</strong></li>
</ol>
<h5 id="非导向性传输介质："><a href="#非导向性传输介质：" class="headerlink" title="非导向性传输介质："></a>非导向性传输介质：</h5><ul>
<li><p>无线单波</p>
<ul>
<li>信号向所有方向传播</li>
<li>较强的穿透能力，可远距离传输，广泛用于通信领域（手机通信）</li>
</ul>
</li>
<li><p>微波</p>
<ul>
<li><p>信号固定方向传播</p>
</li>
<li><p>微波通信频率较高、频段范围宽，因此数据率很高。</p>
<ul>
<li><p>地面微波接力通信</p>
</li>
<li><p>卫星通信</p>
<ul>
<li>优点：</li>
</ul>
<ol>
<li>通信容量大</li>
<li>距离远</li>
<li>覆盖范围广</li>
<li>广播通信和多址通信</li>
</ol>
<ul>
<li>缺点：</li>
</ul>
<ol>
<li>传播时延长</li>
<li>受天气影响大</li>
<li>误码率高</li>
<li>成本高</li>
</ol>
</li>
</ul>
</li>
</ul>
</li>
<li><p>红外线、激光</p>
<ul>
<li>信号固定方向传播</li>
<li>把要传输的信号转换为各自的信号格式，即红外光信号和激光信号。再在空间中传播。</li>
</ul>
</li>
</ul>
<h5 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h5><img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/41.png" class="">

<h4 id="2-3-物理层设备"><a href="#2-3-物理层设备" class="headerlink" title="2.3 物理层设备"></a>2.3 物理层设备</h4><h4 id="中继器"><a href="#中继器" class="headerlink" title="中继器"></a>中继器</h4><p><strong>诞生原因：</strong>由于存在损耗，在线路上传输的信号功率会逐渐衰减，衰减到一定程度将造成信号失真，因此会导致接收错误。</p>
<p><strong>中继器功能：</strong>对信号进行再生和还原，对衰减的信号进行放大，保持与原数据相同，以增加信号传输的距离，延长网络的长度。（再生数字信号）</p>
<p><strong>中继器的两端：</strong></p>
<ol>
<li>两端的网络部分是网段，而不是子网，适用于完全相同的两类网络的互连，且两个网段速率要相同。</li>
<li>中继器只将任何电缆段上的数据发送到另一段电缆上，它仅作用于信号的电气部分，并不管数据中是否有错误数据或不适用于网段的数据</li>
<li>两端可连相同媒体，也可连不同媒体</li>
<li>中继器两端的网段一定要是同一个协议（不会存储转发）</li>
</ol>
<p><strong>5-4-3规则：</strong></p>
<p>网络标准中都对信号的延迟范围作了具体的规定，因而中继器只能在规定的范围内进行，否则会网络故障。</p>
<p>5个网段，4个物理层网络设备，只有三个网段可以连接计算机。</p>
<h4 id="集线器（多口中继器）"><a href="#集线器（多口中继器）" class="headerlink" title="集线器（多口中继器）"></a>集线器（多口中继器）</h4><p><strong>功能</strong>：对信号进行<strong>放大转发</strong>，对衰减的信号进行放大，接着转发到其他所有（除输入端口外）处于工作状态的端口上，以增加信号传输的距离，延长网络的长度。不具备信号的定向传送能力，是一个共享式设备。</p>
<img src="/2021/04/26/%E7%89%A9%E7%90%86%E5%B1%82/42.png" class="">

<p>集线器不能分割冲突域</p>
<p>连在集线器上的工作主机平分带宽。</p>
]]></content>
      <categories>
        <category>计算机网络</category>
      </categories>
  </entry>
  <entry>
    <title>计算机网络绪论</title>
    <url>/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/</url>
    <content><![CDATA[<h2 id="第一章"><a href="#第一章" class="headerlink" title="第一章"></a>第一章</h2><h3 id="1-1-1-概念、组成、功能和分类"><a href="#1-1-1-概念、组成、功能和分类" class="headerlink" title="1.1.1 概念、组成、功能和分类"></a>1.1.1 概念、组成、功能和分类</h3><h4 id="计算机网络的概念："><a href="#计算机网络的概念：" class="headerlink" title="计算机网络的概念："></a>计算机网络的概念：</h4><p>​    计算机网络：是一个将分散的、具有独立功能的计算机系统。通过通信设备与线路连接起来，由功能完善的软件实现资源共享和信息传递的系统。</p>
<p>​    计算机网络是<strong>互联的、自治的</strong>计算机集合。</p>
<p>​        互联-互联互通 通信链路</p>
<p>​        自治-无主从关系</p>
<span id="more"></span>

<h4 id="计算机网络的功能："><a href="#计算机网络的功能：" class="headerlink" title="计算机网络的功能："></a>计算机网络的功能：</h4><p>​    1数据通信（连通性）</p>
<p>​    2资源共享    </p>
<p>​         硬件    软件    数据</p>
<p>​    3分布式处理    多台计算机各自承担同一工作任务的不同部分</p>
<p>​    4提高可靠性(替代机)</p>
<p>​    5负载均衡    各计算机之间更亲密</p>
<h4 id="计算机网络的组成："><a href="#计算机网络的组成：" class="headerlink" title="计算机网络的组成："></a>计算机网络的组成：</h4><p>​    1组成部分：硬件、软件、协议</p>
<p>​    2工作方式:</p>
<p>​        边缘部分    用户直接使用（C/S方式；P2P方式）</p>
<p>​        核心部分    为边缘部分服务</p>
<p>​    3功能组成：</p>
<img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/1.png" class="">

<h6 id="资源子网-实现资源共享-数据处理"><a href="#资源子网-实现资源共享-数据处理" class="headerlink" title="资源子网    实现资源共享/数据处理"></a>资源子网    实现资源共享/数据处理</h6><p>​                实现资源共享功能的设备和软件的集合</p>
<h6 id="通信子网-实现数据通信"><a href="#通信子网-实现数据通信" class="headerlink" title="通信子网    实现数据通信"></a>通信子网    实现数据通信</h6><p>​                各种传输介质、通讯设备、相应的网络协议组成</p>
<h4 id="计算机网络的分类："><a href="#计算机网络的分类：" class="headerlink" title="计算机网络的分类："></a>计算机网络的分类：</h4><h6 id="1按分布范围分："><a href="#1按分布范围分：" class="headerlink" title="1按分布范围分："></a>1按分布范围分：</h6><p>​    广域网WAN    城域网MAN    局域网WAN    个人区域网PAN</p>
<h6 id="2按使用者分："><a href="#2按使用者分：" class="headerlink" title="2按使用者分："></a>2按使用者分：</h6><p>​    公用网    专用网</p>
<h6 id="3按交换技术分："><a href="#3按交换技术分：" class="headerlink" title="3按交换技术分："></a>3按交换技术分：</h6><p>​    电路交换    报文交换    分组交换</p>
<h6 id="4按拓扑结构分："><a href="#4按拓扑结构分：" class="headerlink" title="4按拓扑结构分："></a>4按拓扑结构分：</h6><p>​    总线型    星型    环型    网状型</p>
<h6 id="5按传输技术分："><a href="#5按传输技术分：" class="headerlink" title="5按传输技术分："></a>5按传输技术分：</h6><p>​    广播式网络（共享公共通信信道）</p>
<p>​    点对点网络（使用分组存储转发和路由选择机制）</p>
<h3 id="1-1-2-标准化工作及相关知识"><a href="#1-1-2-标准化工作及相关知识" class="headerlink" title="1.1.2 标准化工作及相关知识"></a>1.1.2 标准化工作及相关知识</h3><h4 id="标准换工作"><a href="#标准换工作" class="headerlink" title="标准换工作"></a>标准换工作</h4><h5 id="标准的分类："><a href="#标准的分类：" class="headerlink" title="标准的分类："></a>标准的分类：</h5><p>​        法定标准    由权威机构制定的正式的、合法的标准</p>
<p>​        事实标准    某些公司的产品在竞争中占据了主流，时间长了，这些产品的协议和技术就成了标准</p>
<h5 id="标准化工作："><a href="#标准化工作：" class="headerlink" title="标准化工作："></a>标准化工作：</h5><p>​        <strong>RFC</strong>    因特网标准的形式</p>
<p>​        RFC要上升为因特网正式标准的<strong>四个阶段</strong>：</p>
<p>​            1）因特网草案    这个阶段还不是RFC文档</p>
<p>​            2）建议标准        这个阶段开始成为RFC文档</p>
<p>​            3）草案标准        IETF、IAB认证</p>
<p>​            4）因特网标准</p>
<h4 id="标准化工作的相关组织"><a href="#标准化工作的相关组织" class="headerlink" title="标准化工作的相关组织"></a>标准化工作的相关组织</h4><p>​    国际标准化组织ISO</p>
<p>​    国际电信联盟ITU</p>
<p>​    国际电气电子工程师协会IEEE</p>
<p>​    Internet工程任务组IETE</p>
<h3 id="1-1-3-速率相关的性能指标"><a href="#1-1-3-速率相关的性能指标" class="headerlink" title="1.1.3 速率相关的性能指标"></a>1.1.3 速率相关的性能指标</h3><h4 id="速率"><a href="#速率" class="headerlink" title="速率"></a>速率</h4><p>​    速率即数据率或称数据传输率或比特率。</p>
<p>​    比特    1/0    位</p>
<p>​    连接在计算机网络上的主机在数字信道上传送熟虑位数的速率。</p>
<p>​    单位b/s，kb/s，Mb/s，Gb/s，Tb/s</p>
<img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/2.png" class="">

<h4 id="带宽"><a href="#带宽" class="headerlink" title="带宽"></a>带宽</h4><p>​    ”带宽”原本指某个信号具有的<strong>频带宽度</strong>，即最高频率与最低速率之差，单位是赫兹（HZ）。</p>
<p>​    计算机网络中，带宽用来表示网络的通讯线路传送数据的能力，通常是单位时间从网络的某一点到另一点所能通过的“<strong>最高数据率</strong>”。单位是“比特每秒”。网络设备所支持的最高速度。</p>
<h4 id="吞吐量"><a href="#吞吐量" class="headerlink" title="吞吐量"></a>吞吐量</h4><p>​    表示<strong>单位时间</strong>内通过<strong>某个网络（或信道、接口）</strong>的数据量。单位是比特每秒。    </p>
<p>​    吞吐量受网络的带宽或网络的额定速率的限制。</p>
<h3 id="1-1-4-时延、时延带宽积、RTT和利用率"><a href="#1-1-4-时延、时延带宽积、RTT和利用率" class="headerlink" title="1.1.4 时延、时延带宽积、RTT和利用率"></a>1.1.4 时延、时延带宽积、RTT和利用率</h3><h4 id="时延"><a href="#时延" class="headerlink" title="时延"></a>时延</h4><p>​    指数据从<strong>网络（或链路）</strong>的一段传送到另一端所需的时间。也叫<strong>延迟</strong>或<strong>迟延</strong>。单位是s。</p>
<img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/3.png" class="">

<h4 id="时延带宽积"><a href="#时延带宽积" class="headerlink" title="时延带宽积"></a>时延带宽积</h4><p>​    时延带宽积=<strong>传播时延</strong>X<strong>带宽</strong></p>
<p>​    又称为以比特为单位的链路长度</p>
<h4 id="往返时延RTT"><a href="#往返时延RTT" class="headerlink" title="往返时延RTT"></a>往返时延RTT</h4><p>​    从发送方发送数据开始，到发送方收到接受方的确认，总共经历的时延。</p>
<p>​    RTT越大，在收到确认之前，可以发送的数据越多。</p>
<p>​    <img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/4.png" class=""></p>
<h4 id="利用率"><a href="#利用率" class="headerlink" title="利用率"></a>利用率</h4><p>​    利用率分为信道利用率和网络利用率。</p>
<h5 id="1-信道利用率"><a href="#1-信道利用率" class="headerlink" title="1.信道利用率"></a>1.信道利用率</h5><p>​            =有数据通过时间/（有+无）数据通过时间</p>
<h5 id="2-网络利用率"><a href="#2-网络利用率" class="headerlink" title="2.网络利用率"></a>2.网络利用率</h5><p>​            =信道利用率加权平均值</p>
<h4 id="1-2-1-分层结构、协议、接口、服务"><a href="#1-2-1-分层结构、协议、接口、服务" class="headerlink" title="1.2.1 分层结构、协议、接口、服务"></a>1.2.1 分层结构、协议、接口、服务</h4><h5 id="为什么要分层？"><a href="#为什么要分层？" class="headerlink" title="为什么要分层？"></a>为什么要分层？</h5><p>​        发送前要完成的工作：</p>
<p>​            （1）发起通信的计算机必须将数据通信的通路进行激活。</p>
<p>​            （2）要告诉网络如何识别目的主机。</p>
<p>​            （3）发起通信的计算机要查明目的主机是否开机，并且与网络连接正常。</p>
<p>​            （4）发起通信的计算机要弄清楚，对方计算机中文件管理程序是否已经做好准备工作。</p>
<p>​            （5）确保差错和意外可以解决</p>
<h5 id="怎么分层？"><a href="#怎么分层？" class="headerlink" title="怎么分层？"></a>怎么分层？</h5><h6 id="分层的基本原则："><a href="#分层的基本原则：" class="headerlink" title="分层的基本原则："></a>分层的<strong>基本原则</strong>：</h6><p>​        1.各层直接相互独立，每层只实现一种相对独立的功能</p>
<p>​        2.各层之间界面自然清晰，易于理解，相互交流可能少</p>
<p>​        3.结构上可分隔开。各层都采用最合适的技术实现</p>
<p>​        4.保持下层对上层的独立性，上层单向使用下层提供的服务</p>
<p>​        5.整个分层结构应该促进标准化工作</p>
<h5 id="正式认识分层结构"><a href="#正式认识分层结构" class="headerlink" title="正式认识分层结构:"></a>正式认识分层结构:</h5><p>​        1.实体：第n层中的活动元素称为n层实体。同一层的实体叫对等实体。</p>
<p>​        2.协议：为进行网络中的对等实体数据交换而建立的规则、标准或约定成为网络协议。</p>
<p>​            <img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/5.png" class=""></p>
<p>​        3.接口（访问服务点SAP）：上层使用下层的服务。</p>
<p>​        4.服务：下层为相邻上层提供的功能调用。【垂直】</p>
<h5 id="概念总结"><a href="#概念总结" class="headerlink" title="概念总结:"></a>概念总结:</h5><p>​        网络体系结构是从功能上描述计算机网络结构。</p>
<p>​        计算机网络体系结构简称网络体系结构是分层结构。</p>
<p>​        每层遵守某个/些网络协议以完成本层功能。</p>
<p>​        计算机网络体系结构是计算机网路的各层及其协议的集合。</p>
<p>​        第n层在向n+1层提供服务时，此服务不仅包含第n层本身的功能，还包含由下层服务提供的功能。</p>
<p>​        仅仅在在相邻层间有接口，且所提供服务的具体实现细节对上一层完全屏蔽。</p>
<p>​        体系结构是抽象的，而实现是指能运行的一些软件和硬件。</p>
<h4 id="1-2-2-OSI参考模型"><a href="#1-2-2-OSI参考模型" class="headerlink" title="1.2.2 OSI参考模型"></a>1.2.2 OSI参考模型</h4><p>​    <img src="C:\Users\86189\blog\source\_posts\6.png" style="zoom:50%;" /></p>
<h5 id="ISO-OSI参考模型–怎么来的？"><a href="#ISO-OSI参考模型–怎么来的？" class="headerlink" title="ISO/OSI参考模型–怎么来的？"></a>ISO/OSI参考模型–怎么来的？</h5><p>​        为了解决计算机网络复杂的大问题=》分层结构（按功能）</p>
<p>​        目的：支持异构网络系统的互联互通。</p>
<p>​        国际标准化组织（ISO）于1984年提出开放系统互连（OSI）参考模型。</p>
<img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/7.png" class="">

<h5 id="ISO-OSI参考模型"><a href="#ISO-OSI参考模型" class="headerlink" title="ISO/OSI参考模型"></a>ISO/OSI参考模型</h5><img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/8.png" class="">

<h4 id="1-2-3-OSI参考模型（2）"><a href="#1-2-3-OSI参考模型（2）" class="headerlink" title="1.2.3 OSI参考模型（2）"></a><img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/9.png" class="">1.2.3 OSI参考模型（2）</h4><h5 id="应用层："><a href="#应用层：" class="headerlink" title="应用层："></a>应用层：</h5><p>​            所有能和用户产生网络流量的程序</p>
<p>​            典型应用层服务：</p>
<p>​                文件传输（FTP）</p>
<p>​                电子邮件（SMTP）</p>
<p>​                万维网（HTTP）</p>
<p>​                ······</p>
<h5 id="表示层："><a href="#表示层：" class="headerlink" title="表示层："></a>表示层：</h5><p>​            用于处理两个通信系统中变换消息的表示方式（语法和语义）</p>
<p>​                功能一：数据格式变换</p>
<p>​                功能二：数据加密解密</p>
<p>​                功能三：数据压缩和恢复</p>
<p>​            主要协议：JPEG，ASCII</p>
<h5 id="会话层："><a href="#会话层：" class="headerlink" title="会话层："></a>会话层：</h5><p>​            向表示层实体/用户进程提供建立连接并在连接上有序地传输数据。</p>
<p>​                这是会话，也是<strong>建立同步</strong>（SYN）</p>
<p>​            功能一：建立、管理、终止会话</p>
<p>​            功能二：使用校验点可使会话在通信失效时从校验点/同步点继续回复通信，实现数据同步。</p>
<h5 id="传输层："><a href="#传输层：" class="headerlink" title="传输层："></a>传输层：</h5><p>​            负责主机中两个进程的通信，即端到端的通信。传输单位是<strong>报文段</strong>或<strong>用户数据报</strong>。</p>
<p>​                功能一：可靠传输、不可靠传输</p>
<p>​                功能二：差错控制</p>
<p>​                功能三：流量控制</p>
<p>​                功能四：复用分用</p>
<p>​                                <strong>复用</strong>：多个应用层进程可同时使用下面运输层的服务。</p>
<p>​                                <strong>分用</strong>：运输层把收到的信息分别交付到上面应用层中相应的进程。</p>
<p>​            </p>
<h5 id="网络层："><a href="#网络层：" class="headerlink" title="网络层："></a>网络层：</h5><p>​            主要任务是把分组从源端传到目的端，为分组交换网上的不同主机提供通信服务。网络层传输单位是<strong>数据报</strong>。</p>
<p>​                功能一：路由选择    为了找到数据传输和的<strong>最佳路径</strong></p>
<p>​                功能二：流量控制</p>
<p>​                功能三：差错控制</p>
<p>​                功能四：拥塞控制</p>
<h5 id="数据链路层："><a href="#数据链路层：" class="headerlink" title="数据链路层："></a>数据链路层：</h5><p>​            主要任务是把网络层传下来的数据报组装成<strong>帧</strong>。数据链路层的传输单位是<strong>帧</strong>。</p>
<p>​                功能一：成帧（<strong>定义帧的开始和结束</strong>）</p>
<p>​                功能二：差错控制 <strong>帧错+位错</strong></p>
<p>​                功能三：流量控制</p>
<p>​                功能四：访问（接入）控制 <strong>控制对信道的访问</strong></p>
<h5 id="物理层："><a href="#物理层：" class="headerlink" title="物理层："></a>物理层：</h5><p>​            主要任务是在物理媒体上实现比特流的<strong>透明传输</strong>。物理层传输单位是<strong>比特</strong>。</p>
<p>​            物理层传输单位是比特。</p>
<p>​                功能一：定义接口特性</p>
<p>​                功能二：定义传输模式     <strong>单工</strong>、<strong>半双工</strong>、<strong>双工</strong></p>
<p>​                功能三：定义传输速率</p>
<p>​                功能四：比特同步</p>
<p>​                功能五：比特编码</p>
<h4 id="1-2-4-TCP、IP参考模型和5层参考模型"><a href="#1-2-4-TCP、IP参考模型和5层参考模型" class="headerlink" title="1.2.4 TCP、IP参考模型和5层参考模型"></a>1.2.4 TCP、IP参考模型和5层参考模型</h4><h5 id="OSI参考模型与TCP-IP参考模型"><a href="#OSI参考模型与TCP-IP参考模型" class="headerlink" title="OSI参考模型与TCP/IP参考模型"></a>OSI参考模型与TCP/IP参考模型</h5><img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/10.png" class="">

<h6 id="OSI参考模型与TCP-IP参考模型的相同点"><a href="#OSI参考模型与TCP-IP参考模型的相同点" class="headerlink" title="OSI参考模型与TCP/IP参考模型的相同点"></a>OSI参考模型与TCP/IP参考模型的相同点</h6><p>​            1.都分层</p>
<p>​            2.基于独立的协议栈的概念</p>
<p>​            3可以实现异构网络互联</p>
<h6 id="OSI参考模型与TCP-IP参考模型的不同点"><a href="#OSI参考模型与TCP-IP参考模型的不同点" class="headerlink" title="OSI参考模型与TCP/IP参考模型的不同点"></a>OSI参考模型与TCP/IP参考模型的不同点</h6><p>​            1.OSI定义三点：<strong>服务、协议、接口</strong></p>
<p>​            2.OSI先出现，参考模型先于协议发明，不偏向特定协议</p>
<p>​            3.TCP/IP设计之初就考虑到互联网异构问题，将IP作为重要层次</p>
<p>​        <img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/11.png" class=""></p>
<p>​            <strong>面向连接</strong>：第一是建立连接，发出一个建立连接的请求。第二阶段开始传输数据。最后传输完毕，必须释放连接。</p>
<p>​            <strong>无连接</strong>：直接进行数据传输。</p>
<h5 id="五层参考模型"><a href="#五层参考模型" class="headerlink" title="五层参考模型"></a>五层参考模型</h5><h5 id=""><a href="#" class="headerlink" title=""></a><img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/12.png" class=""></h5><h4 id="5层参考模型的数据封装与解封装"><a href="#5层参考模型的数据封装与解封装" class="headerlink" title="5层参考模型的数据封装与解封装"></a>5层参考模型的数据封装与解封装</h4><img src="/2021/04/01/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%BB%AA%E8%AE%BA/13.png" class="">
]]></content>
      <categories>
        <category>计算机网络</category>
      </categories>
  </entry>
  <entry>
    <title>进程管理</title>
    <url>/2021/09/05/%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h2 id="CPU管理的直观想法"><a href="#CPU管理的直观想法" class="headerlink" title="CPU管理的直观想法"></a>CPU管理的直观想法</h2><h3 id="管理CPU，先使用CPU"><a href="#管理CPU，先使用CPU" class="headerlink" title="管理CPU，先使用CPU"></a>管理CPU，先使用CPU</h3><ul>
<li>CPU的工作原理<ul>
<li>CPU上电以后发生了什么？<ul>
<li>把一个程序存放到一个内存里，设置一个PC指向程序，使得CPU取指令，将指令从内存中取出。</li>
<li>CPU解释执行这个指令。</li>
</ul>
</li>
<li>CPU怎么工作？<ul>
<li>不断的取值，执行</li>
</ul>
</li>
<li>CPU怎么管理？<ul>
<li>设置PC一个初值，PC自加，不断取值执行</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h3><p>IO指令比计算指令慢很多，且IO指令执行时不用CPU。如果线性处理，那么就无法使CPU一直忙碌。CPU的使用效率会很低。</p>
<p><strong>所以</strong>，我们将在执行IO指令时，将CPU切到其他地方执行其他指令。等IO指令执行结束再返回来。</p>
<p>这样，使得多个程序在内存中。</p>
<h3 id="多道程序交替执行"><a href="#多道程序交替执行" class="headerlink" title="多道程序交替执行"></a>多道程序交替执行</h3><p><img src="35.png"></p>
<p><img src="36.png"></p>
<p>一个CPU上交替执行多个程序，成为<strong>并发</strong>。</p>
<h3 id="只要修改PC就可以了吗？"><a href="#只要修改PC就可以了吗？" class="headerlink" title="只要修改PC就可以了吗？"></a>只要修改PC就可以了吗？</h3><ul>
<li>要记录返回的地址，要记录上一个程序所需要的数据</li>
<li>每个程序有了一个存放信息的结构：PCB</li>
<li>运行的程序和静态程序不一样了…</li>
</ul>
<h3 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h3><ul>
<li>运行的程序和静态程序不一样！<ul>
<li>需要描述这些不一样</li>
<li>程序+这些不一样的地方=》一个概念</li>
</ul>
</li>
<li>进程是进行（执行）中的程序<ul>
<li>进程有开始、有结束，程序没有</li>
<li>进程会走走停停，程序没有</li>
<li>进程需要记录当前状态，程序不用</li>
<li>…</li>
</ul>
</li>
</ul>
<h2 id="多进程图像"><a href="#多进程图像" class="headerlink" title="多进程图像"></a>多进程图像</h2><h3 id="多个进程使用CPU的图像"><a href="#多个进程使用CPU的图像" class="headerlink" title="多个进程使用CPU的图像"></a>多个进程使用CPU的图像</h3><ul>
<li>如何使用CPU<ul>
<li>让程序执行起来</li>
</ul>
</li>
<li>如何充分利用CPU<ul>
<li>启动多个程序，交替执行</li>
</ul>
</li>
<li>启动了的程序就是进程，所以是多个进程推进<ul>
<li>操作系统只需要把这些进程<strong>记录</strong>好、要按照合理的次序推进（分配资源、进行调度）</li>
</ul>
</li>
</ul>
<p><img src="27.png"></p>
<h3 id="多进程图像从启动开始到关机结束"><a href="#多进程图像从启动开始到关机结束" class="headerlink" title="多进程图像从启动开始到关机结束"></a>多进程图像从启动开始到关机结束</h3><ul>
<li>main中的fork()创建了第一个进程<ul>
<li>init执行了shell</li>
</ul>
</li>
<li>shell再启动其他进程</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>* argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">&quot;%s&quot;</span>,cmd);</span><br><span class="line">        <span class="keyword">if</span>(!fork())</span><br><span class="line">        &#123;</span><br><span class="line">            exec(cmd);</span><br><span class="line">		&#125;</span><br><span class="line">        wait();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="28.png"></p>
<h3 id="多进程图像：多进程如何组织？"><a href="#多进程图像：多进程如何组织？" class="headerlink" title="多进程图像：多进程如何组织？"></a>多进程图像：多进程如何组织？</h3><ul>
<li>有一个进程在执行（运行）<ul>
<li><img src="29.png"></li>
</ul>
</li>
<li>有一些进程等待执行（就绪）<ul>
<li><img src="30.png"></li>
</ul>
</li>
<li>有一些进程在等待某事件（阻塞）<ul>
<li><img src="31.png"></li>
</ul>
</li>
</ul>
<h3 id="多进程的组织：PCB-状态-队列"><a href="#多进程的组织：PCB-状态-队列" class="headerlink" title="多进程的组织：PCB+状态+队列"></a>多进程的组织：PCB+状态+队列</h3><p>进程状态图：</p>
<p><img src="32.png"></p>
<ul>
<li>它能给出进程生存期的清晰描述</li>
<li>它是认识操作系统进程管理的一个窗口</li>
</ul>
<h3 id="多进程图像：多进程如何交替？"><a href="#多进程图像：多进程如何交替？" class="headerlink" title="多进程图像：多进程如何交替？"></a>多进程图像：多进程如何交替？</h3><ol>
<li>启动磁盘读写；</li>
<li>pCur.state=’W’;//阻塞态</li>
<li>pCur放到DiskWaitQueue；</li>
<li>schedule()</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">schedule()</span><br><span class="line">&#123;</span><br><span class="line">    pNew=getNext(ReadyQueue);</span><br><span class="line">    <span class="comment">//从就绪队列中找到下一个进程的PCB</span></span><br><span class="line">    switch_to(pCur,pNew);</span><br><span class="line">    <span class="comment">//将当前进程的PCB切换为当前PCB</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="交替的三个部分：队列操作-调度-切换"><a href="#交替的三个部分：队列操作-调度-切换" class="headerlink" title="交替的三个部分：队列操作+调度+切换"></a>交替的三个部分：队列操作+调度+切换</h3><ul>
<li>进程调度，有很多种算法思想。</li>
<li>FIFO？先来先调度<ul>
<li>FIFO显然是公平的策略</li>
<li>FIFO显然没有考虑进程执行的任务的区别</li>
</ul>
</li>
<li>Priority？<ul>
<li>优先级怎么设定？还可能使得进程饥饿</li>
</ul>
</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">switch_to(pCur,pNew)&#123;</span><br><span class="line">    pCur.ax	= CPU.ax;</span><br><span class="line">    pCur.bx = CPU.bx;</span><br><span class="line">    ...</span><br><span class="line">    pCur.cs = CPU .cs;</span><br><span class="line">    pCur.retpc = CPU.pc;</span><br><span class="line">    <span class="comment">//将当前进程保存</span></span><br><span class="line">    CPU.ax = pNew.ax;</span><br><span class="line">    CPU.bx = pNew.bx;</span><br><span class="line">    ...</span><br><span class="line">    CPU.cs = pNew.cs;</span><br><span class="line">    CPU.retpc = pNew.pc;</span><br><span class="line">    <span class="comment">//将下一个进程的状态赋给CPU</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="多进程图像：多进程如何影响？"><a href="#多进程图像：多进程如何影响？" class="headerlink" title="多进程图像：多进程如何影响？"></a>多进程图像：多进程如何影响？</h3><ul>
<li>多个 进程同时存在于内存会出现下面的问题<ul>
<li><img src="33.png"></li>
</ul>
</li>
<li>解决办法：限制对地址100的读写</li>
<li>如何限制：多进程的地址空间分离，操作系统的内存管理</li>
</ul>
<p><img src="34.png"></p>
<p>通过映射表将多个进程数据的地址分离开。</p>
<h3 id="多进程图像：多进程如何合作？"><a href="#多进程图像：多进程如何合作？" class="headerlink" title="多进程图像：多进程如何合作？"></a>多进程图像：多进程如何合作？</h3><ul>
<li>想象打印工作过程<ul>
<li>应用程序提交打印任务</li>
<li>打印任务被放进打印队列</li>
<li>打印进程从队列中提取任务</li>
<li>打印进程控制打印机打印</li>
</ul>
</li>
</ul>
<p>多个进程对同一个内存进行覆盖时，交替执行会乱套。</p>
<h3 id="核心在于进程同步（合理的推进顺序）"><a href="#核心在于进程同步（合理的推进顺序）" class="headerlink" title="核心在于进程同步（合理的推进顺序）"></a>核心在于进程同步（合理的推进顺序）</h3><p>使得多个进程有一个合理的进程推进顺序，从而实现进程的合作。</p>
<h2 id="用户级线程"><a href="#用户级线程" class="headerlink" title="用户级线程"></a>用户级线程</h2><p><img src="37.png"></p>
<img src="38.png" style="zoom: 67%;" />

<h3 id="是否可以资源不动而切换指令序列？"><a href="#是否可以资源不动而切换指令序列？" class="headerlink" title="是否可以资源不动而切换指令序列？"></a>是否可以资源不动而切换指令序列？</h3><ul>
<li>进程=资源+指令执行序列<ul>
<li>将资源和指令分开</li>
<li>一个资源+多个指令执行序列</li>
<li><img src="39.png"></li>
</ul>
</li>
</ul>
<p>我们只切换指令，而资源内存不用切换</p>
<ul>
<li>线程：保留了并发的优点，避免了进程切换代价</li>
<li>实质就是映射表不变而PC指针变</li>
</ul>
<h3 id="多个执行序列-一个地址空间是否实用？"><a href="#多个执行序列-一个地址空间是否实用？" class="headerlink" title="多个执行序列+一个地址空间是否实用？"></a>多个执行序列+一个地址空间是否实用？</h3><ul>
<li>一个网页浏览器<ul>
<li>一个线程用来从服务器接收数据</li>
<li>一个线程用来显示文本</li>
<li>一个线程用来处理图片</li>
<li>一个线程用来显示图片</li>
</ul>
</li>
<li>这些线程需要共享资源吗？<ul>
<li>接收数据都放在缓冲区里</li>
<li>所有的文本、图片都显示在一个屏幕上</li>
</ul>
</li>
</ul>
<h3 id="开始实现这样的浏览器…"><a href="#开始实现这样的浏览器…" class="headerlink" title="开始实现这样的浏览器…"></a>开始实现这样的浏览器…</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">WebExplorer</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> URL[] = <span class="string">&quot;http://cms.hit.edu.cn&quot;</span>;</span><br><span class="line">    <span class="keyword">char</span> buffer[<span class="number">1000</span>];</span><br><span class="line">    <span class="comment">//创建两个线程</span></span><br><span class="line">    pthread_creat(..., Getdata, URL, buffer);</span><br><span class="line">    pthread_creat(..., Show, buffer);</span><br><span class="line">    </span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">GetData</span><span class="params">(<span class="keyword">char</span> *URL, <span class="keyword">char</span> *p)</span></span>&#123;...&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Show</span><span class="params">(<span class="keyword">char</span> *p)</span></span>&#123;...&#125;</span><br></pre></td></tr></table></figure>

<p><img src="40.png"></p>
<h3 id="核心是Yield"><a href="#核心是Yield" class="headerlink" title="核心是Yield"></a>核心是Yield</h3><ul>
<li>需要知道切换时进程的样子</li>
<li>Create就是制造出第一次切换时的样子</li>
</ul>
<p><img src="42.png" alt="42"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="number">100</span>:A()</span><br><span class="line">&#123;</span><br><span class="line">    B();</span><br><span class="line">    <span class="number">104</span>:</span><br><span class="line">&#125;</span><br><span class="line"><span class="number">200</span>:B()</span><br><span class="line">&#123;</span><br><span class="line">    Yield();</span><br><span class="line">    <span class="number">204</span>:</span><br><span class="line">&#125;</span><br><span class="line"><span class="number">300</span>:C()</span><br><span class="line">&#123;</span><br><span class="line">    D();</span><br><span class="line">    <span class="number">304</span>:</span><br><span class="line">&#125;</span><br><span class="line"><span class="number">400</span>:D()</span><br><span class="line">&#123;</span><br><span class="line">    Yield();</span><br><span class="line">    <span class="number">404</span>:</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="42.png"></p>
<h3 id="两个执行序列与一个栈"><a href="#两个执行序列与一个栈" class="headerlink" title="两个执行序列与一个栈"></a>两个执行序列与一个栈</h3><p><img src="41.png"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Yield</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    找到<span class="number">300</span>;</span><br><span class="line">    jmp <span class="number">300</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Yield</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    找到<span class="number">204</span>;</span><br><span class="line">    jmp <span class="number">204</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后B()执行完，需要返回到A()，但目前栈中esp-&gt;404，会ret到D()中，从而出现问题。</p>
<h3 id="解决方法：每个线程都要有自己的栈"><a href="#解决方法：每个线程都要有自己的栈" class="headerlink" title="解决方法：每个线程都要有自己的栈"></a>解决方法：每个线程都要有自己的栈</h3><p><img src="43.png"></p>
<ul>
<li>Yield切换要先切换栈</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Yield</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    TCB2.esp=esp;</span><br><span class="line">    esp=TCB1.esp;</span><br><span class="line">    <span class="comment">//jmp 204;*不需要用jmp，直接用栈中的204ret*</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="两个线程的样子：两个TCB、两个栈、切换的PC在栈中"><a href="#两个线程的样子：两个TCB、两个栈、切换的PC在栈中" class="headerlink" title="两个线程的样子：两个TCB、两个栈、切换的PC在栈中"></a>两个线程的样子：两个TCB、两个栈、切换的PC在栈中</h3><ul>
<li>ThreadCreate的核心就是用程序做出这三样东西</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ThreadCreate</span><span class="params">(A)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    TCB *tcb=<span class="built_in">malloc</span>();</span><br><span class="line">    *<span class="built_in">stack</span>=<span class="built_in">malloc</span>();</span><br><span class="line">    *<span class="built_in">stack</span>=A;</span><br><span class="line">    tcb.esp=<span class="built_in">stack</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="44.png"></p>
<h3 id="用户级线程和内核级线程？"><a href="#用户级线程和内核级线程？" class="headerlink" title="用户级线程和内核级线程？"></a>用户级线程和内核级线程？</h3><p><img src="45.png"></p>
<p>而如果进程1进入了内核，并且阻塞，则会进入进程2，进程1中的其他线程都会不执行。</p>
<p><img src="46.png"></p>
<h2 id="内核级线程"><a href="#内核级线程" class="headerlink" title="内核级线程"></a>内核级线程</h2><h3 id="和用户级相比，核心级线程有什么不同？"><a href="#和用户级相比，核心级线程有什么不同？" class="headerlink" title="和用户级相比，核心级线程有什么不同？"></a>和用户级相比，核心级线程有什么不同？</h3><ul>
<li> ThreadCreate是系统调用，内核管理TCB，内核负责切换线程</li>
<li>如何让切换成型？–内核栈，TCB<ul>
<li>用户栈是否还要用？<ul>
<li>执行的代码仍在用户态，还要进行函数调用</li>
</ul>
</li>
<li>一个栈到一套栈；两个栈到两套栈</li>
<li>TCB关联内核栈，那用户栈怎么办？<ul>
<li>根据TCB切换一套栈</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><img src="47.png"></p>
<h3 id="用户栈和内核栈之间的关联"><a href="#用户栈和内核栈之间的关联" class="headerlink" title="用户栈和内核栈之间的关联"></a>用户栈和内核栈之间的关联</h3><p><img src="48.png"></p>
<p>一旦INT中断启动，就启用内核栈。</p>
<p>当中断结束IRET，回到用户栈。</p>
<h3 id="看A-B-C-D"><a href="#看A-B-C-D" class="headerlink" title="看A()B()C()D()"></a>看A()B()C()D()</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="number">100</span>:A()&#123;</span><br><span class="line">    B();</span><br><span class="line">    <span class="number">104</span>:</span><br><span class="line">&#125;</span><br><span class="line"><span class="number">200</span>:B()&#123;</span><br><span class="line">    read();</span><br><span class="line">    <span class="number">204</span>:</span><br><span class="line">&#125;</span><br><span class="line"><span class="number">300</span>:read()&#123;</span><br><span class="line">    <span class="keyword">int</span> <span class="number">0x80</span>;</span><br><span class="line">    <span class="number">304</span>:</span><br><span class="line">&#125;</span><br><span class="line">system_call:</span><br><span class="line">	call sys_read;</span><br><span class="line"><span class="number">1000</span>:</span><br><span class="line"><span class="number">2000</span>:sys_read()&#123;&#125;</span><br></pre></td></tr></table></figure>

<p><img src="49.png"></p>
<h3 id="开始内核中的切换：switch-to"><a href="#开始内核中的切换：switch-to" class="headerlink" title="开始内核中的切换：switch_to"></a>开始内核中的切换：switch_to</h3><p><img src="50.png"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sys_read()&#123;</span><br><span class="line">	启动磁盘读；</span><br><span class="line">    将自己变成阻塞；</span><br><span class="line">    找到next；</span><br><span class="line">    switch_to(cur,next);    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>switch_to</strong>:仍然是通过<strong>TCB</strong>找到内核栈指针；然后通过<strong>ret</strong>切到某个内核程序；最后再用<strong>CS:PC</strong>切到用户程序。</p>
<h3 id="上面的？都是什么？"><a href="#上面的？都是什么？" class="headerlink" title="上面的？都是什么？"></a>上面的？都是什么？</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="number">100</span>:A()&#123;<span class="comment">//线程S代码</span></span><br><span class="line">    ...</span><br><span class="line">       <span class="keyword">int</span> <span class="number">0x80</span>;</span><br><span class="line">    ...</span><br><span class="line"><span class="number">2000</span>:sys_read()&#123;...&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>？？？:sys_read函数的函数的某个地方</li>
<li>？？:中断之前的某个地方</li>
<li>？？？:sys_xxx函数中的某个地方</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="number">500</span>:C()&#123;<span class="comment">//线程T代码</span></span><br><span class="line">    ...</span><br><span class="line">interrupt:</span><br><span class="line">    call sys_xxx;</span><br><span class="line"><span class="number">3000</span>:</span><br><span class="line"><span class="number">4000</span>:sys_xxx()&#123;...&#125;</span><br></pre></td></tr></table></figure>

<p>当T创建时：</p>
<ul>
<li>？？:500，函数C()的开始地址</li>
<li>？？？？:一段能完成第二级返回的代码，一段包含iret的代码,从中断中返回</li>
</ul>
<h3 id="内核线程switch-to的五段论"><a href="#内核线程switch-to的五段论" class="headerlink" title="内核线程switch_to的五段论"></a>内核线程switch_to的五段论</h3><p><img src="51.png"></p>
<p>第一级切换：</p>
<p><img src="52.png"></p>
<p>第二级切换：</p>
<p><img src="53.png"></p>
<p>最后，S、T非同一进程，所以还要进行地址切换。首先要切换地址映射表；</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">TCB[cur].ldtr = %ldtr</span><br><span class="line">%ldtr = TCB[next].ldtr</span><br><span class="line">    <span class="comment">//内存管理</span></span><br></pre></td></tr></table></figure>

<h3 id="ThreadCreate"><a href="#ThreadCreate" class="headerlink" title="ThreadCreate!"></a>ThreadCreate!</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ThreadCreate</span><span class="params">(...)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    TCB tcb=get_free_page();</span><br><span class="line">    *krlstack=...;</span><br><span class="line">    *userstack传入;</span><br><span class="line">    填写两个<span class="built_in">stack</span>;</span><br><span class="line">    tcb.esp=krlstack;</span><br><span class="line">    tcb.状态=就绪;</span><br><span class="line">    tcb入队;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="用户级线程和核心级线程的对比"><a href="#用户级线程和核心级线程的对比" class="headerlink" title="用户级线程和核心级线程的对比"></a>用户级线程和核心级线程的对比</h3><p><img src="54.png"></p>
<h2 id="内核级线程实现"><a href="#内核级线程实现" class="headerlink" title="内核级线程实现"></a>内核级线程实现</h2><h3 id="核心级线程的两套栈，核心是内核栈"><a href="#核心级线程的两套栈，核心是内核栈" class="headerlink" title="核心级线程的两套栈，核心是内核栈"></a>核心级线程的两套栈，核心是内核栈</h3><p><img src="55.png"></p>
<h3 id="从进入内核开始，某个中断开始"><a href="#从进入内核开始，某个中断开始" class="headerlink" title="从进入内核开始，某个中断开始"></a>从进入内核开始，某个中断开始</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">main()&#123;</span><br><span class="line">	A();</span><br><span class="line">    B();</span><br><span class="line">&#125;</span><br><span class="line">A()</span><br><span class="line">&#123;</span><br><span class="line">    fork();<span class="comment">//fork是系统调用，会引起中断</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> <img src="56.png"></p>
<p>一旦执行int指令，将找到内核栈，然后通过内核栈找到用户栈。</p>
<h3 id="切换五段论的中断入口和中断出口"><a href="#切换五段论的中断入口和中断出口" class="headerlink" title="切换五段论的中断入口和中断出口"></a>切换五段论的中断入口和中断出口</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sched_init</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    set_system_gate(<span class="number">0x80</span>,&amp;system_call);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>初始化时将各种中断处理设置好</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">_system_call:</span><br><span class="line">	push %ds..%fs</span><br><span class="line">	pushl %eax...</span><br><span class="line">	call sys_fork	#执行过程中可能引起切换</span><br><span class="line">	pushl %eax</span><br><span class="line">#将用户态的东西保存起来</span><br><span class="line">	movl _current,%eax	#当前PCB</span><br><span class="line">	cmpl $0,state(%eax)	#当前PCB的状态，0是运行或就绪，非0是阻塞</span><br><span class="line">	jne	reschedule	#阻塞则跳转reschedule，重新调度</span><br><span class="line">	cmpl $0,counter(%eax)	#当前PCB的时间片是否用完</span><br><span class="line">	je rechedule	#时间片用完也要进行调度</span><br><span class="line">ret_from_sys_call:</span><br><span class="line">	popl %eax	#返回值</span><br><span class="line">	popl %ebx	</span><br><span class="line">	...</span><br><span class="line">	pop %fs	</span><br><span class="line">	...</span><br><span class="line">	iret	#切换用户栈</span><br><span class="line">#中断返回函数</span><br><span class="line">reschedule:</span><br><span class="line">	push $ret_form_sys_call</span><br><span class="line">	jmp	_schedule</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">schedule</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    next=...;<span class="comment">//看调度算法给出下一给线程</span></span><br><span class="line">    switch_to(next);<span class="comment">//next是下一个线程的TCB</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> switch_to(n)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> &#123;</span><span class="keyword">long</span> a,b;&#125;</span><br><span class="line">    _asm_(</span><br><span class="line">    	<span class="string">&quot;movw %%dx,%1\n\t&quot;</span></span><br><span class="line">        <span class="string">&quot;ljmp %0\n\t&quot;</span>	<span class="comment">//长跳转指令，段与段之间的跳转</span></span><br><span class="line">        ::<span class="string">&quot;m&quot;</span>(*&amp;__tmp.a),</span><br><span class="line">        <span class="string">&quot;m&quot;</span>(*&amp;__tmp.b),</span><br><span class="line">        <span class="string">&quot;d&quot;</span>(_TSS(n))	<span class="comment">//n就是next，指向新的段</span></span><br><span class="line">    )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="57.png"></p>
<h3 id="ThreadCreate-1"><a href="#ThreadCreate-1" class="headerlink" title="ThreadCreate"></a>ThreadCreate</h3><ul>
<li>从_sys_fork开始ThreadCreate</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">_sys_fork:</span><br><span class="line">	push %gs</span><br><span class="line">	pushl %esi</span><br><span class="line">	...</span><br><span class="line">	pushl %eax</span><br><span class="line">	#将当前状态值都入栈</span><br><span class="line">	call _copy_process	#复制父进程的所有参数</span><br><span class="line">	addl $20,%esp</span><br><span class="line">	ret</span><br></pre></td></tr></table></figure>

<h3 id="copy-process的细节：创建栈"><a href="#copy-process的细节：创建栈" class="headerlink" title="copy_process的细节：创建栈"></a>copy_process的细节：创建栈</h3><p><img src="58.png"></p>
<p>创建TCB，初始化TSS</p>
<p><img src="59.png"></p>
<h3 id="copy-process的细节：执行前准备"><a href="#copy-process的细节：执行前准备" class="headerlink" title="copy_process的细节：执行前准备"></a>copy_process的细节：执行前准备</h3><p><img src="60.png"></p>
<h3 id="如何执行我们想要的代码"><a href="#如何执行我们想要的代码" class="headerlink" title="如何执行我们想要的代码"></a>如何执行我们想要的代码</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc,<span class="keyword">char</span>* argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">&quot;%s&quot;</span>,cmd);</span><br><span class="line">        <span class="keyword">if</span>(!fork())<span class="comment">//子进程的代码</span></span><br><span class="line">        &#123;</span><br><span class="line">            exec(cmd);</span><br><span class="line">        &#125;</span><br><span class="line">        wait(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>fork()何时返回0，何时不会？<ul>
<li><img src="61.png"></li>
<li><img src="62.png"></li>
<li>子进程里，eax被置为0，然后通过TSS找到了返回地址</li>
<li>父进程里eax则非0</li>
</ul>
</li>
</ul>
<h3 id="结构：子进程进入A-父进程等待…"><a href="#结构：子进程进入A-父进程等待…" class="headerlink" title="结构：子进程进入A,父进程等待…"></a>结构：子进程进入A,父进程等待…</h3><ul>
<li><p>关于exec这个系统调用</p>
<ul>
<li>```c<br>if(fork()){exec(cmd);}<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">```assembly</span><br><span class="line">_system_call:</span><br><span class="line">	push %ds..%fs</span><br><span class="line">	pushl %edx..</span><br><span class="line">	call sys_execve	</span><br><span class="line">_sys_execve:</span><br><span class="line">	lea EIP(%esp),%eax</span><br><span class="line">	pushl %eax	#将ret的地址（cmd指令的位置）压栈，当exec()在内核态iret时，找到这个地址，执行cmd指令</span><br><span class="line">	call _do_execve</span><br><span class="line">EIP = 0x1c</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<p><img src="63.png"></p>
<h3 id="让A执行"><a href="#让A执行" class="headerlink" title="让A执行"></a>让A执行</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">do_exec</span><span class="params">(* eip,...)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    p+=change_ldt(...);</span><br><span class="line">    eip[<span class="number">0</span>]=ex.a_entry;<span class="comment">//ex.a_entry是可执行程序入口地址，产生可执行文件时写入</span></span><br><span class="line">    eip[<span class="number">3</span>]=p;<span class="comment">//修改栈</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">exec</span>&#123;</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> a_magic;</span><br><span class="line">    <span class="keyword">unsigned</span> a_entry;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p><img src="64.png"></p>
<h2 id="操作系统的那颗“树”"><a href="#操作系统的那颗“树”" class="headerlink" title="操作系统的那颗“树”"></a>操作系统的那颗“树”</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>* argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>	i,to,*fp,sum=<span class="number">0</span>;</span><br><span class="line">    to=atoi(argv[<span class="number">1</span>]);</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>;i&lt;=to;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        sum=sum+i;</span><br><span class="line">        <span class="comment">//CPU工作了10毫秒</span></span><br><span class="line">        <span class="built_in">fprintf</span>(fp,<span class="string">&quot;%d&quot;</span>,sum);</span><br><span class="line">        <span class="comment">//CPU停留了10秒钟</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="得让CPU好好运转"><a href="#得让CPU好好运转" class="headerlink" title="得让CPU好好运转"></a>得让CPU好好运转</h3><p><img src="65.png"></p>
<p><img src="66.png"></p>
<h3 id="一个栈-Yield造成的混乱"><a href="#一个栈-Yield造成的混乱" class="headerlink" title="一个栈+Yield造成的混乱"></a>一个栈+Yield造成的混乱</h3><p><img src="67.png"></p>
<h3 id="使用两个栈-两个用户TCB"><a href="#使用两个栈-两个用户TCB" class="headerlink" title="使用两个栈+两个用户TCB"></a>使用两个栈+两个用户TCB</h3><p><img src="68.png"></p>
<p>Yield()找到下一个TCB—&gt;找到新的栈—&gt;切到新的栈</p>
<h3 id="不能一直执行用户态"><a href="#不能一直执行用户态" class="headerlink" title="不能一直执行用户态"></a>不能一直执行用户态</h3><p><img src="69.png"></p>
<p>如果进程1进入了内核，并且阻塞，则会进入进程2，进程1中的其他线程都会不执行。</p>
<h3 id="引入内核栈的切换"><a href="#引入内核栈的切换" class="headerlink" title="引入内核栈的切换"></a>引入内核栈的切换</h3><p><img src="70.png"></p>
<h3 id="目标：交替的打出A和B"><a href="#目标：交替的打出A和B" class="headerlink" title="目标：交替的打出A和B"></a>目标：交替的打出A和B</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">#AB.<span class="function">c</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!fork())&#123;<span class="keyword">while</span>(<span class="number">1</span>)<span class="built_in">printf</span>(<span class="string">&quot;A&quot;</span>);&#125;</span><br><span class="line">    <span class="keyword">if</span>(!fork())&#123;<span class="keyword">while</span>(<span class="number">1</span>)<span class="built_in">printf</span>(<span class="string">&quot;B&quot;</span>);&#125;</span><br><span class="line">    wait();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">main()&#123;</span><br><span class="line">	mov __NR_fork,%eax</span><br><span class="line">	int 0x80</span><br><span class="line">100:</span><br><span class="line">	mov %eax,res</span><br><span class="line">	cmpl res,0</span><br><span class="line">	jne 208</span><br><span class="line">200:</span><br><span class="line">	printf(&quot;A&quot;)</span><br><span class="line">208:</span><br><span class="line">	...</span><br><span class="line">304:</span><br><span class="line">	wait()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="INT进入内核"><a href="#INT进入内核" class="headerlink" title="INT进入内核"></a>INT进入内核</h3><p><img src="71.png"></p>
<p><img src="72.png"></p>
<h3 id="开始sys-fork"><a href="#开始sys-fork" class="headerlink" title="开始sys_fork"></a>开始sys_fork</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sys_fork:</span><br><span class="line">	push ...</span><br><span class="line">	call copy_process</span><br><span class="line">	ret</span><br></pre></td></tr></table></figure>

<h3 id="开始copy-process"><a href="#开始copy-process" class="headerlink" title="开始copy_process"></a>开始copy_process</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">copy_process</span><span class="params">(...<span class="keyword">long</span> eip=<span class="number">100</span>,)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    p=(PCB *)get_free_page();</span><br><span class="line">    p-&gt;tss.esp0=p+<span class="number">4</span>k;</span><br><span class="line">    p-&gt;tss.esp=esp;</span><br><span class="line">    p-&gt;tss.eax=<span class="number">0</span>;</span><br><span class="line">    p-&gt;tss.eip=eip;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="73.png"></p>
<h3 id="开始返回"><a href="#开始返回" class="headerlink" title="开始返回"></a>开始返回</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">copy_process()&#123;...&#125;</span><br><span class="line">sys_fork:</span><br><span class="line">	...</span><br><span class="line">	call copy_process</span><br><span class="line">	...</span><br><span class="line">	ret</span><br><span class="line">system_call:</span><br><span class="line">	...</span><br><span class="line">	call sys_call_table(,%eax,4)</span><br><span class="line">	cmpl $0,state(current)</span><br><span class="line">	jne reschedule</span><br><span class="line">	iret</span><br><span class="line">main:</span><br><span class="line">	int 0x80</span><br><span class="line">100:</span><br><span class="line">	mov %eax,res</span><br><span class="line">	cmpl res,0;</span><br></pre></td></tr></table></figure>

<h3 id="main继续执行"><a href="#main继续执行" class="headerlink" title="main继续执行"></a>main继续执行</h3><p>又一次fork()，再一次int 0x80</p>
<p>在内核中再产生一个PCB和内核栈…</p>
<p><img src="74.png"></p>
<p>???：是B()函数的执行地址</p>
<h3 id="wait（）"><a href="#wait（）" class="headerlink" title="wait（）"></a>wait（）</h3><p>接下来，wait()函数，将父进程变成阻塞态。</p>
<h3 id="schedule（）"><a href="#schedule（）" class="headerlink" title="schedule（）"></a>schedule（）</h3><p>父进程阻塞，将当前进程变成A或者B</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">schedule</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>((*P)-&gt;satae==TASK_RUNNING&amp;&amp;(*p)-&gt;counter&gt;c)</span><br><span class="line">    &#123;</span><br><span class="line">        c=(*p)-&gt;counter,next=i;</span><br><span class="line">    	...</span><br><span class="line">    &#125;</span><br><span class="line">     switch_to(next);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="swtich-to-切换"><a href="#swtich-to-切换" class="headerlink" title="swtich_to()切换"></a>swtich_to()切换</h3><p><img src="75.png"></p>
<h3 id="怎么调度：时钟中断"><a href="#怎么调度：时钟中断" class="headerlink" title="怎么调度：时钟中断"></a>怎么调度：时钟中断</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sched_init</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    set_intr_gate(<span class="number">0x20</span>,&amp;timer_interrupt);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void _timer_interrupt:</span><br><span class="line">	...</span><br><span class="line">	call do_timer</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">do_timer</span><span class="params">(...)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>((--current)-&gt;counter&gt;<span class="number">0</span>)<span class="keyword">return</span>;</span><br><span class="line">    current-&gt;counter=<span class="number">0</span>;</span><br><span class="line">    schedule();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="CPU调度策略"><a href="#CPU调度策略" class="headerlink" title="CPU调度策略"></a>CPU调度策略</h2><h3 id="多进程图像与CPU调度"><a href="#多进程图像与CPU调度" class="headerlink" title="多进程图像与CPU调度"></a>多进程图像与CPU调度</h3><p><img src="76.png"></p>
<h3 id="CPU调度（进程调度）的直观想法"><a href="#CPU调度（进程调度）的直观想法" class="headerlink" title="CPU调度（进程调度）的直观想法"></a>CPU调度（进程调度）的直观想法</h3><ol>
<li>FIFO<ul>
<li>先进先调度</li>
<li>如果一个简单的任务也要等很久，就不太合理</li>
</ul>
</li>
<li>Priority<ul>
<li>任务短可以适当优先</li>
<li>如果都是短任务，那么长任务就得不到处理</li>
</ul>
</li>
</ol>
<h3 id="如何设计调度算法使得进程满意"><a href="#如何设计调度算法使得进程满意" class="headerlink" title="如何设计调度算法使得进程满意"></a>如何设计调度算法使得进程满意</h3><ul>
<li>尽快结束任务：周转时间短</li>
<li>用户操作尽快响应：响应时间短</li>
<li>系统内耗时间少：吞吐量大</li>
</ul>
<h3 id="需要折中处理"><a href="#需要折中处理" class="headerlink" title="需要折中处理"></a>需要折中处理</h3><ul>
<li>吞吐量和响应时间之间有矛盾<ul>
<li>响应时间小=》切换次数多=》系统内耗大=》吞吐量小</li>
</ul>
</li>
<li>前台任务和后台任务的关注点不同<ul>
<li>前台任务关注响应时间、后台任务关注周转时间</li>
</ul>
</li>
<li>IO约束型任务和CPU约束型任务各有各的特点</li>
</ul>
<h3 id="各种基本调度算法"><a href="#各种基本调度算法" class="headerlink" title="各种基本调度算法"></a>各种基本调度算法</h3><h4 id="First-Come，First-Service（FCFS）"><a href="#First-Come，First-Service（FCFS）" class="headerlink" title="First Come，First Service（FCFS）"></a>First Come，First Service（FCFS）</h4><ul>
<li>先来先服务</li>
<li><img src="77.png"></li>
<li>缺点：平均周转时间大。</li>
</ul>
<p>因为短的作业越前平均周转时间越短，长的作业越后平均周转时间越长。</p>
<h4 id="SJF：短作业优先"><a href="#SJF：短作业优先" class="headerlink" title="SJF：短作业优先"></a>SJF：短作业优先</h4><ul>
<li>平均周转时间最小</li>
<li>响应时间无法控制，长作业要等好久才能执行</li>
<li><img src="78.png"></li>
</ul>
<h4 id="RR：按时间片来轮转调度"><a href="#RR：按时间片来轮转调度" class="headerlink" title="RR：按时间片来轮转调度"></a>RR：按时间片来轮转调度</h4><p><img src="79.png"></p>
<ul>
<li>时间片大：响应时间太长；时间片小：吞吐量小</li>
<li>折中：时间片10~100ms，切换时间0.1-1ms</li>
</ul>
<h4 id="一个调度算法怎样让多种类型任务同时满意"><a href="#一个调度算法怎样让多种类型任务同时满意" class="headerlink" title="一个调度算法怎样让多种类型任务同时满意"></a>一个调度算法怎样让多种类型任务同时满意</h4><p>直观想法：定义前台任务和后台任务两队列，前台RR，后台SJF。只有当没有前台任务，才调度后台任务。</p>
<p><img src="80.png"></p>
<p>如果一直有前台任务，那么怎么办？</p>
<h3 id="饥饿"><a href="#饥饿" class="headerlink" title="饥饿"></a>饥饿</h3><ul>
<li>如果后台任务长时间没有运行，就会饥饿，提高其优先级。</li>
</ul>
<p>让后台任务优先级动态升高，但后台任务（用SJF调度）一旦执行，前台任务就不能很快的响应。</p>
<p>前后台任务都用时间片，后台任务的SJF如何体现？</p>
<h3 id="其他问题"><a href="#其他问题" class="headerlink" title="其他问题"></a>其他问题</h3><p>如何知道哪些是前台任务，哪些是后台任务，fork时能告诉我们吗？</p>
<p>gcc就一点不需要交互吗？word就不会执行一段批处理吗？</p>
<p>SJF中的短作业优先如何体现？如何判断作业的长度？</p>
<h2 id="一个实际的schedule函数"><a href="#一个实际的schedule函数" class="headerlink" title="一个实际的schedule函数"></a>一个实际的schedule函数</h2><p>void schedule（）：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">	c = <span class="number">-1</span>;</span><br><span class="line">	next = <span class="number">0</span>;</span><br><span class="line">	i = NR_TASKS;</span><br><span class="line">       </span><br><span class="line">	p = &amp;task[NR_TASKS];<span class="comment">//讲从任务列表末尾开始</span></span><br><span class="line">	<span class="keyword">while</span> (--i) &#123;		<span class="comment">//向前移动</span></span><br><span class="line">		<span class="keyword">if</span> (!*--p)</span><br><span class="line">			<span class="keyword">continue</span>;</span><br><span class="line">		<span class="keyword">if</span> ((*p)-&gt;state == TASK_RUNNING &amp;&amp; (*p)-&gt;counter &gt; c)</span><br><span class="line">			c = (*p)-&gt;counter, next = i;</span><br><span class="line">	&#125;<span class="comment">//求大的counter（优先级）（时间片）</span></span><br><span class="line">	<span class="keyword">if</span> (c) <span class="keyword">break</span>;</span><br><span class="line">       <span class="comment">//如果c==0，重置一下counter</span></span><br><span class="line">       <span class="comment">/*</span></span><br><span class="line"><span class="comment">       1.当不存在就绪态进程后，c=-1，执行for循环</span></span><br><span class="line"><span class="comment">       2.for循环使就绪态进程（counter=0）=priority，而使得阻塞态进程counter&gt;priority。这样使得等待时间越长的进程优先级越高</span></span><br><span class="line"><span class="comment">       3.当阻塞态就绪后，会立即获得高优先级</span></span><br><span class="line"><span class="comment">       */</span></span><br><span class="line">	<span class="keyword">for</span>(p = &amp;LAST_TASK ; p &gt; &amp;FIRST_TASK ; --p)</span><br><span class="line">		<span class="keyword">if</span> (*p)</span><br><span class="line">			(*p)-&gt;counter = ((*p)-&gt;counter &gt;&gt; <span class="number">1</span>) +</span><br><span class="line">					(*p)-&gt;priority;</span><br><span class="line">&#125;</span><br><span class="line">switch_to(next); </span><br></pre></td></tr></table></figure>

<h3 id="counter的作用：时间片"><a href="#counter的作用：时间片" class="headerlink" title="counter的作用：时间片"></a>counter的作用：时间片</h3><p>void do_timer(…)</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> ((--current-&gt;counter)&gt;<span class="number">0</span>) <span class="keyword">return</span>;</span><br><span class="line">	current-&gt;counter=<span class="number">0</span>;</span><br><span class="line">	schedule();</span><br></pre></td></tr></table></figure>

<p>每次都让counter–，直到等于0时切换任务</p>
<h3 id="counter的作用：优先级"><a href="#counter的作用：优先级" class="headerlink" title="counter的作用：优先级"></a>counter的作用：优先级</h3><p>每次找counter最大的调度，counter表现了优先级。</p>
<p>这个优先级可以不断的动态调整。I/O操作具有前台进程的特征，具有高优先级。</p>
<h3 id="counter作用的整理"><a href="#counter作用的整理" class="headerlink" title="counter作用的整理"></a>counter作用的整理</h3><ul>
<li>counter保证了响应时间的界限</li>
<li>经过I/O以后，counter就会变大；I/O时间越长，counter越大。从而照顾了前台进程。</li>
<li>后台进程一直按照counter轮转，近似SJF调度</li>
<li>每个进程只维护一个counter变量，简单高效</li>
</ul>
<h2 id="进程同步与信号量"><a href="#进程同步与信号量" class="headerlink" title="进程同步与信号量"></a>进程同步与信号量</h2><h3 id="进程合作：多进程共同完成一个任务"><a href="#进程合作：多进程共同完成一个任务" class="headerlink" title="进程合作：多进程共同完成一个任务"></a>进程合作：多进程共同完成一个任务</h3><p><img src="81.png"></p>
<h3 id="生产者-消费者实例"><a href="#生产者-消费者实例" class="headerlink" title="生产者-消费者实例"></a>生产者-消费者实例</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//共享数据</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BUFFER_SIZE 10</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span>&#123;</span>...&#125;item;</span><br><span class="line">item buffer[BUFFER_SIZE];</span><br><span class="line"><span class="keyword">int</span> in=out=counter=<span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//生产者进程</span></span><br><span class="line"><span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">    <span class="keyword">while</span>(counter==BUFFER_SIZE)</span><br><span class="line">        ;<span class="comment">//缓存区满，生产者要停</span></span><br><span class="line">    buffer[in]=item;</span><br><span class="line">    in=(in+<span class="number">1</span>)%BUFFER_SIZE;</span><br><span class="line">    counter++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//消费者进程</span></span><br><span class="line"><span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">    <span class="keyword">while</span>(counter==<span class="number">0</span>)</span><br><span class="line">        ;<span class="comment">//缓存区空，消费者要停</span></span><br><span class="line">    item=buffer[out];</span><br><span class="line">    out=(out+<span class="number">1</span>)%BUFFER_SIZE;</span><br><span class="line">    counter--;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过进程同步来保证多进程合作的合理有序。</p>
<h3 id="只发信号还不够"><a href="#只发信号还不够" class="headerlink" title="只发信号还不够"></a>只发信号还不够</h3><p><img src="82.png"></p>
<ol>
<li>缓冲区满以后生产者P1生产一个item放入，会sleep</li>
<li>又一个生产者P2生产一个item放入，会sleep</li>
<li>消费者C执行1次循环，counter=BUFFER_SIZE-1，发信号给P1，P1wakeup</li>
<li>消费者C再执行1次循环，counter=BUFFER_SIZE-2 ,P2不能被唤醒</li>
</ol>
<ul>
<li>问题的关键在于消费者仅仅是在缓冲区满的时候知道有生产者在等待</li>
<li>也就是sleep了两个进程，而wakeup的时候只能唤醒一个进程，p2永远不会唤醒</li>
</ul>
<h3 id="从信号到信号量"><a href="#从信号到信号量" class="headerlink" title="从信号到信号量"></a>从信号到信号量</h3><p>不只是等待信号和发信号？对应睡眠和唤醒</p>
<p>还应该记录一些其他信息</p>
<ul>
<li>能记录有几个进程在睡眠中需要被唤醒<ol>
<li>缓冲区满，P1执行，P1sleep，记录下1个进程等待</li>
<li>P2执行，P2sleep，记录下2个进程等待</li>
<li>C执行一次循环，发现2个进程等待，wakeup1个</li>
<li>C再执行1次循环，发现1个进程等待，wakeup1个</li>
</ol>
</li>
</ul>
<h3 id="信号量开始工作。。。"><a href="#信号量开始工作。。。" class="headerlink" title="信号量开始工作。。。"></a>信号量开始工作。。。</h3><ol>
<li>缓冲区满，P1执行，P1sleep，sem=-1</li>
<li>P2执行，P2sleep，sem=-2</li>
<li>C执行1次循环，wakeup P1，sem=-1</li>
<li>C再执行一次循环，wakeup P2，sem=0</li>
<li>C再执行一次循环，sem=1</li>
<li>P3执行，sem=0</li>
</ol>
<p><img src="83.png"></p>
<p><img src="84.png"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">V(semaphore s)</span><br><span class="line">&#123;</span><br><span class="line">    s.value++;</span><br><span class="line">    <span class="keyword">if</span>(s.value &lt;= <span class="number">0</span>)</span><br><span class="line">        wakeup(s.<span class="built_in">queue</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="用信号量解生产者-消费者问题"><a href="#用信号量解生产者-消费者问题" class="headerlink" title="用信号量解生产者-消费者问题"></a>用信号量解生产者-消费者问题</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//用文件定义共享缓冲区</span></span><br><span class="line"><span class="keyword">int</span> fd = open(<span class="string">&quot;buffer.txt&quot;</span>);</span><br><span class="line">write(fd, <span class="number">0</span>, <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line">write(fd, <span class="number">0</span>, <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line"><span class="comment">//信号量的定义和初始化</span></span><br><span class="line">semaphore full=<span class="number">0</span>;</span><br><span class="line">semaphore empty=BUFFER_SIZE;</span><br><span class="line">semaphore mutex=<span class="number">1</span>;</span><br><span class="line"><span class="comment">/*实现互斥，P(mutex)将mutex置为0，其他进程就没法进入缓冲区，V(mutex)再置为1，其他进程就可以进入缓冲区。*/</span></span><br><span class="line"></span><br><span class="line">Producer(item)&#123;</span><br><span class="line">    P(empty);<span class="comment">//缓冲区是否满了,满了停止写入</span></span><br><span class="line">    P(mutex);</span><br><span class="line">    读入in;</span><br><span class="line">    将item写入到in的位置上;</span><br><span class="line">    V(mutex);</span><br><span class="line">    V(full);<span class="comment">//生产者增加内容full++</span></span><br><span class="line">&#125;</span><br><span class="line">Consumer()&#123;</span><br><span class="line">    P(full);<span class="comment">//生产的内容为0，就阻塞，否则full--</span></span><br><span class="line">    P(mutex);</span><br><span class="line">    读入out;</span><br><span class="line">    从文件中的out位置读出到item;</span><br><span class="line">    打印item;</span><br><span class="line">	V(mutex);</span><br><span class="line">    V(empty);<span class="comment">//缓冲区是否为空，空了停止读出</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="信号量临界区保护"><a href="#信号量临界区保护" class="headerlink" title="信号量临界区保护"></a>信号量临界区保护</h2><p>上述方案，在进程切换的过程中，可能会发生信号还没修改就进入了另一个进程从而导致语义错误。</p>
<h3 id="竞争条件（RaceCondition）"><a href="#竞争条件（RaceCondition）" class="headerlink" title="竞争条件（RaceCondition）"></a>竞争条件（RaceCondition）</h3><p>竞争条件：和调度有关的共享数据语义错误。</p>
<ul>
<li>错误由多个进程并发操作共享数据引起</li>
<li>错误和调度顺序有关，难于发现和调试</li>
</ul>
<h3 id="解决竞争条件的直观想法"><a href="#解决竞争条件的直观想法" class="headerlink" title="解决竞争条件的直观想法"></a>解决竞争条件的直观想法</h3><ul>
<li><p>在写共享变量empty时阻止其他进程访问empty</p>
<ul>
<li>生产者P1：检查并给empty上锁</li>
<li>生产者P2：检查empty的锁</li>
<li>生产者P1：给empty开锁</li>
<li>生产者P2：检查并给empty上锁</li>
<li>生产者P2：给empty开锁</li>
</ul>
</li>
<li><p>临界区：一次只允许一个进程进入的该进程的那一段代码</p>
<ul>
<li><img src="85.png"></li>
<li>一个非常重要的工作：找出进程中的临界区代码（读写信号量的代码必须是临界区）</li>
</ul>
</li>
</ul>
<p><img src="86.png"></p>
<h3 id="临界区代码的保护原则"><a href="#临界区代码的保护原则" class="headerlink" title="临界区代码的保护原则"></a>临界区代码的保护原则</h3><ul>
<li>基本原则：<strong>互斥进入</strong>：如果一个进程在临界区中执行，则其他进程不允许进入<ul>
<li>这些进程间的约束条件称为互斥</li>
<li>这保证了是临界区</li>
</ul>
</li>
<li>好的临界区保护原则：<ul>
<li><strong>有空让进</strong></li>
<li><strong>有限等待</strong></li>
</ul>
</li>
</ul>
<h3 id="进入临界区的第一个尝试-轮换法"><a href="#进入临界区的第一个尝试-轮换法" class="headerlink" title="进入临界区的第一个尝试-轮换法"></a>进入临界区的第一个尝试-轮换法</h3><p><img src="87.png"></p>
<ul>
<li> 满足互斥进入要求</li>
<li> 轮换的时候，如果有进程在进入前阻塞，那么现在临界区有空，但另一个进程想进也进不去。</li>
</ul>
<h3 id="又一个尝试-标记法"><a href="#又一个尝试-标记法" class="headerlink" title="又一个尝试-标记法"></a>又一个尝试-标记法</h3><p><img src="88.png"></p>
<ul>
<li> 满足互斥进入要求</li>
<li> 但是，当进程调度过程中会导致两个flag都为1，P0和P1无限等待。</li>
</ul>
<h3 id="非对称标记Peterson算法"><a href="#非对称标记Peterson算法" class="headerlink" title="非对称标记Peterson算法"></a>非对称标记Peterson算法</h3><ul>
<li>带名字的便条+让一个进程更加优先</li>
</ul>
<p><img src="89.png"></p>
<ul>
<li>满足有空让进：如果进程P1不在临界区，则flag[1]=false或turn=0，P0都能进入。</li>
<li>满足有限等待：P0要求进入，flag[0]=true;后面的P1不可能一直进入，因为P1执行一次就会让turn=0。</li>
</ul>
<h3 id="多进程怎么办？面包店算法"><a href="#多进程怎么办？面包店算法" class="headerlink" title="多进程怎么办？面包店算法"></a>多进程怎么办？面包店算法</h3><ul>
<li>如何轮转：每个进程都获得一个序号，序号最小的进入</li>
<li>如何标记：进程离开时序号为0，不为0的序号即标记</li>
</ul>
<p><img src="90.png"></p>
<p>面包店算法：每个进入商店的客户都获得一个号码，号码最小的先得到服务；号码相同时，名字靠前的先服务。</p>
<ul>
<li>互斥进入：总是号最小的进入</li>
<li>有空让进：如果没有进程在临界区，最小序号的进程一定能进入</li>
<li>有限等待：离开临界区的进程再次区号一定排在最后。所有有一个FIFO的思想</li>
</ul>
<h3 id="临界区保护的另一类解法"><a href="#临界区保护的另一类解法" class="headerlink" title="临界区保护的另一类解法"></a>临界区保护的另一类解法</h3><ul>
<li><p>再想一下临界区：只允许一个进程进入，进入另一个进程意味着什么？</p>
<ul>
<li><p>被调度：另一个进程只有被调度才能执行，才可能进入临界区，如何阻止调度？</p>
<ul>
<li><p>关闭中断</p>
</li>
<li><p>```C<br>cli();</p>
<pre><code>临界区
</code></pre>
<p>sti();</p>
<pre><code>剩余区    
</code></pre>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">    - 多核CPU就不能这样操作</span><br><span class="line"></span><br><span class="line">      - 只能关闭一个CPU的终端，另一个CPU还能正常操作</span><br><span class="line"></span><br><span class="line">- 硬件原子指令法</span><br><span class="line"></span><br><span class="line">  - ```c</span><br><span class="line">    boolean TestAndSet(boolean &amp;x)</span><br><span class="line">    //只能有一个进程调用这个函数</span><br><span class="line">    &#123;</span><br><span class="line">        boolean rv=x;</span><br><span class="line">        x=true;//在进入临界区的瞬间，将lock锁上</span><br><span class="line">        return rv</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>```c<br>while(TestAndSet(&amp;lock));<br>//临界区<br>lock=false;<br>//剩余区</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">## 信号量的代码实现</span><br><span class="line"></span><br><span class="line">```c</span><br><span class="line">//producer.c</span><br><span class="line">sd=sem_open(&quot;empty&quot;);</span><br><span class="line">for(i=1 to 5)&#123;</span><br><span class="line">    sem_wait(sd);</span><br><span class="line">    write(fd,&amp;i,4);</span><br><span class="line">&#125;</span><br><span class="line">//sem.c</span><br><span class="line">typedef struct&#123;</span><br><span class="line">    char name[20];</span><br><span class="line">    int value;</span><br><span class="line">    task_struct *queue;</span><br><span class="line">&#125;semtable[20];</span><br><span class="line">sys_sem_open(char *name)&#123;</span><br><span class="line">    在semtable中寻找name对应的；</span><br><span class="line">    没有则创建；</span><br><span class="line">    返回对应的下标</span><br><span class="line">&#125;</span><br><span class="line">sys_sem_wait(int sd)&#123;</span><br><span class="line">    cli();</span><br><span class="line">    if(semtable[sd].value-- &lt; 0)&#123;</span><br><span class="line">        设置自己为阻塞;</span><br><span class="line">        将自己加入semtable[sd].queue中;</span><br><span class="line">        schedule();</span><br><span class="line">    &#125;</span><br><span class="line">    sti();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h3 id="从Linux0-11中学点东西"><a href="#从Linux0-11中学点东西" class="headerlink" title="从Linux0.11中学点东西"></a>从Linux0.11中学点东西</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//读磁盘块</span></span><br><span class="line">bread(<span class="keyword">int</span> dev , <span class="keyword">int</span> block)&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">buffer_head</span> *<span class="title">bh</span>;</span>	<span class="comment">//申请一个空闲缓冲区</span></span><br><span class="line">    ll_rw_block(READ,bh);	<span class="comment">//读</span></span><br><span class="line">    wait_on_buffer(bh);		<span class="comment">//阻塞</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//启动磁盘读以后睡眠。等待磁盘读完由磁盘中断将其唤醒。也是一种同步</span></span><br><span class="line">lock_buffer(buffer_head* bh)&#123;</span><br><span class="line">    cli();</span><br><span class="line">    <span class="keyword">while</span>(bh-&gt;b_lock)<span class="comment">//在阻塞队列全部进程唤醒后，按schedule()找一个进程进入磁盘并上锁，其他进程再次阻塞。</span></span><br><span class="line">        sleep_on(&amp;bh-&gt;b_wait）;</span><br><span class="line">    bh-&gt;b_lock = <span class="number">1</span>;<span class="comment">//上锁</span></span><br><span class="line">    sti();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span> sleep_on(struct task_struct **p)&#123;<span class="comment">//传入等待队列队首的指针</span></span><br><span class="line">    struct task_struct *tmp;</span><br><span class="line">    <span class="comment">//世界上最隐蔽的队列</span></span><br><span class="line">    tmp = *p;		<span class="comment">//在下面进程切换的过程中会切换栈，而tmp在栈中，作为局部变量，可以通过tmp找到下一个PCB，构成一个隐蔽的队列</span></span><br><span class="line">    *p = current;</span><br><span class="line">    <span class="comment">//将自己放入阻塞队列</span></span><br><span class="line">    current-&gt;state = TASK_UNINTERRUPTIBLE;</span><br><span class="line">    schedule();</span><br><span class="line">    <span class="keyword">if</span>(tmp)			<span class="comment">//链式唤醒，一次性全部唤醒，然后由schedule来决定唤醒哪一个进程，</span></span><br><span class="line">        tmp-&gt;state=<span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="94.png"></p>
<h3 id="如何从Linux0-11的这个队列中唤醒？"><a href="#如何从Linux0-11的这个队列中唤醒？" class="headerlink" title="如何从Linux0.11的这个队列中唤醒？"></a>如何从Linux0.11的这个队列中唤醒？</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">read_intr</span><span class="params">(<span class="keyword">void</span>)</span></span>&#123;	<span class="comment">//磁盘中断</span></span><br><span class="line">  	...</span><br><span class="line">    end_request(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line">end_request(<span class="keyword">int</span> uptodate)&#123;</span><br><span class="line">    ...</span><br><span class="line">    unlock_buffer(current-&gt;bh)</span><br><span class="line">&#125;</span><br><span class="line">unlock_buffer(struct buffer_head * bh)&#123;</span><br><span class="line">    bh-&gt;b_lock=<span class="number">0</span>;</span><br><span class="line">    wake_up(&amp;bh-&gt;b_wait);</span><br><span class="line">&#125;</span><br><span class="line">wake_up(struct task_struct **p)&#123;	<span class="comment">//传入阻塞队列队首</span></span><br><span class="line">    <span class="keyword">if</span>(p &amp;&amp; *p)&#123;</span><br><span class="line">        (**p).state=<span class="number">0</span>;				<span class="comment">//队首指向的进程唤醒</span></span><br><span class="line">        *p=<span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="死锁处理"><a href="#死锁处理" class="headerlink" title="死锁处理"></a>死锁处理</h2><h3 id="如果信号量这样使用"><a href="#如果信号量这样使用" class="headerlink" title="如果信号量这样使用"></a>如果信号量这样使用</h3><p><img src="91.png"></p>
<p>将进入区代码，先mutex再empty，先mutex再full</p>
<p>生产者在P（empty）时，需要等待消费者V（empty）,而消费者却在P(mutex)，导致互相等待对方。</p>
<p>死锁：多个进程由于等待对方持有的资源而造成的谁都无法执行的情况。</p>
<p><img src="92.png"></p>
<h3 id="造成死锁的原因"><a href="#造成死锁的原因" class="headerlink" title="造成死锁的原因"></a>造成死锁的原因</h3><ul>
<li>资源互斥使用，一旦占有别人无法使用（互斥使用）</li>
<li>进程占有了一些资源，又不释放，还要再去申请其他资源（不可抢占）</li>
<li>进程必须占有资源，再去申请（请求和保持）</li>
<li>循环等待（资源分配图中存在一个环路）<ul>
<li>各自占有的资源和互相申请的资源形成了环路等待</li>
</ul>
</li>
</ul>
<h3 id="死锁预防"><a href="#死锁预防" class="headerlink" title="死锁预防"></a>死锁预防</h3><ul>
<li>在进程执行前，一次性申请所有需要的资源，不会占有资源再去申请。<ul>
<li>需要预知未来，编程困难</li>
<li>许多资源分配后很长时间才使用， 资源利用率低</li>
</ul>
</li>
<li>对资源类型进行排序，资源申请必须按序进行，不会出现环路<ul>
<li>仍然造成资源的浪费</li>
</ul>
</li>
</ul>
<h3 id="死锁避免"><a href="#死锁避免" class="headerlink" title="死锁避免"></a>死锁避免</h3><ul>
<li><p>如果系统中的所有进程存在一个可完成的执行序列P1….Pn，则称系统处于安全状态。</p>
</li>
<li><p>安全序列：上面的执行序列</p>
<ul>
<li><img src="93.png"></li>
<li>P1、P3、P2、P4、P0</li>
</ul>
</li>
<li><p>银行家算法</p>
<ul>
<li><pre><code class="c">int Available[1..m];
int Allocation[1..n,1..m];
int Need[1..n,1..m];
int Work[1..n,1..m];
bool Finish[1..n];

Work = Available;Finish[1..n] = false;
while(true)&#123;
    for(int i=0;i&lt;n;i++)&#123;
        if(Finish[i]==false &amp;&amp; Need[i]&lt;=Work)&#123;
            Work = Work + Allocation[i];
            Finish[i] = true;
            break;
        &#125;
        else &#123; goto end;&#125;
    &#125;
&#125;
end:    for(i=0;i&lt;n;i++)
            if(Finish[i]==false)
                return deadlock;
</code></pre>
<p>T(n)=O(m*n^2)</p>
</li>
</ul>
</li>
</ul>
<h3 id="死锁检测-恢复"><a href="#死锁检测-恢复" class="headerlink" title="死锁检测+恢复"></a>死锁检测+恢复</h3><ul>
<li>恢复很不容易，进程的改变很难恢复</li>
</ul>
<h3 id="死锁忽略"><a href="#死锁忽略" class="headerlink" title="死锁忽略"></a>死锁忽略</h3><ul>
<li>死锁出现不是确定的，又可以用重启动来处理死锁</li>
</ul>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
  </entry>
</search>
